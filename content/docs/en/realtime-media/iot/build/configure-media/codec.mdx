---
title: "Codec"
description: "Configure audio and video codecs, and use a custom encoder."
---

In audio and video streaming, the choice of an encoding algorithm affects both quality and bit rate. Your goal is to lower the required bit rate while maintaining quality at the desired level. This page shows you how to configure the audio and video codec IoT SDK uses to stream media in your app.

:::info
IoT SDK does not provide audio capture. The device firmware must interface with the microphone hardware and capture raw audio data.
:::

## Prerequisites

In order to follow this procedure you must have:

* Implemented the [Build from scratch](../../build-from-scratch.mdx) project for IoT SDK.

## Audio codec

IoT SDK offers the following built-in audio codecs:

* `AUDIO_CODEC_TYPE_OPUS`
* `AUDIO_CODEC_TYPE_G722`
* `AUDIO_CODEC_TYPE_G711A`
* `AUDIO_CODEC_TYPE_G711U`

You specify an audio codec when you join a channel. Depending on your audio quality requirements you also set the sampling rate and the number of channels. If your app needs a custom encoding algorithm, you disable use of a built-in codec and implement your own encoder.

You set the audio codec, sampling rate, and the number of channels in `channel_options` that you pass to `agora_rtc_join_channel`. To set these parameters according to your requirements, use the `config` object or modify the following lines in `main()`:

```cpp
channel_options.audio_codec_opt.audio_codec_type = config->audio_codec_type;
channel_options.audio_codec_opt.pcm_sample_rate = config->pcm_sample_rate;
channel_options.audio_codec_opt.pcm_channel_num = config->pcm_channel_num;
```

To use your own audio codec, set `channel_options.audio_codec_opt.audio_codec_type` to `AUDIO_CODEC_DISABLED`. When you call `agora_rtc_send_audio_data`, set the `data_type` parameter of `audio_frame_info` to your encoding format. This setting transmits the audio encoding format as is to the receiving end. When you disable use of a built-in audio codec, IoT SDK does not process the audio. The receiving end obtains the encoded audio data and the encoding format through the `on_audio_data` callback and decodes it using a custom decoder.

Starting with v1.9.7, IoT SDK also supports 8 kHz AAC-LC audio, and you can configure the audio packet duration using `pcm_duration`.

## Video codec

IoT SDK sends and receives `H.264` video data by default. For details of supported video data types and how to interoperate with a video codec used by the RTC SDK, see [Interoperate with RTC SDK](../communicate-with-other-sdks/interoperate-with-rtc-sdk.mdx).

Starting with v1.9.7, IoT SDK reports video rotation information so a receiving app can correctly orient rendered video.

## Frame rate

You set the video frame rate in `video_frame_info_t.frame_rate` when sending each frame. Starting with v1.9.7, `frame_rate` is an integer instead of an enum, so you can configure any frame rate from 1 to 60 fps to match your device's capture capabilities and available bandwidth.

## Reference

### Recommended sampling rates and data sizes for audio

The table below shows the recommended sampling rates and corresponding data sizes for Opus, G722, and G711 audio data.

| Audio format | Sampling rate (Hz) | Audio data size (Byte) |
|------|------|-------|
| G711 | 8000 | 320 |
| G722 | 16000 | 640 |
| OPUS | 16000 | 640 |
| OPUS | 48000 | 1920 |

### API reference

- [enum audio_codec_type_e](https://api-ref.agora.io/en/iot-sdk/linux/1.x/agora__rtc__api_8h.html#a307c5bae03eb9a318e247a97e6fb5d88)
