---
title: "changeChatRoomName method"
description: "API docs for the changeChatRoomName method from the ChatRoomManager class, for the Dart programming language."
---

Changes the chat room name.

Only the chat room owner can call this method.

**Throws** A description of the exception. See [ChatError](/zh-CN/api-reference/agora-chat/flutter/agora-chat-sdk/chat-error).

## Parameters

| Name | Description |
| --- | --- |
| `roomId` | The chat room ID. |
| `name` | The new name of the chat room. |

```dart title="Dart"
Future<void> changeChatRoomName(String roomId, String name)
```

<a id="source"></a>

## Implementation

```dart title="Dart"
Future<void> changeChatRoomName(
  String roomId,
  String name,
) async {
  Map req = {"roomId": roomId, "subject": name};
  Map result =
      await _channel.invokeMethod(ChatMethodKeys.changeChatRoomSubject, req);
  try {
    ChatError.hasErrorFromResult(result);
  } on ChatError catch (e) {
    throw e;
  }
}
```
