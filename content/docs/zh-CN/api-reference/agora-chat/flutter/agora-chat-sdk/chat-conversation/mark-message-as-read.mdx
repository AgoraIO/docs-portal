---
title: "markMessageAsRead method"
description: "API docs for the markMessageAsRead method from the ChatConversation class, for the Dart programming language."
---

Marks a message as read.

**Throws** A description of the exception. See [ChatError](/zh-CN/api-reference/agora-chat/flutter/agora-chat-sdk/chat-error).

## Parameters

| Name | Description |
| --- | --- |
| `messageId` | The message ID. |

```dart title="Dart"
Future<void> markMessageAsRead(String messageId)
```

<a id="source"></a>

## Implementation

```dart title="Dart"
Future<void> markMessageAsRead(String messageId) async {
  Map req = this._toJson();
  req['msg_id'] = messageId;
  Map result = await _emConversationChannel.invokeMethod(
      ChatMethodKeys.markMessageAsRead, req);
  try {
    ChatError.hasErrorFromResult(result);
  } on ChatError catch (e) {
    throw e;
  }
}
```
