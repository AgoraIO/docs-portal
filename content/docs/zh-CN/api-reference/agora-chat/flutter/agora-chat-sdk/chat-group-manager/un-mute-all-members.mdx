---
title: "unMuteAllMembers method"
description: "API docs for the unMuteAllMembers method from the ChatGroupManager class, for the Dart programming language."
---

Unmutes all members.

Only the group owner or admin can call this method.

**Throws** A description of the exception. See [ChatError](/zh-CN/api-reference/agora-chat/flutter/agora-chat-sdk/chat-error).

## Parameters

| Name | Description |
| --- | --- |
| `groupId` | The group ID. |

```dart title="Dart"
Future<void> unMuteAllMembers(String groupId)
```

## Implementation [#source]

```dart title="Dart"
Future<void> unMuteAllMembers(String groupId) async {
  Map req = {'groupId': groupId};
  Map result =
      await _channel.invokeMethod(ChatMethodKeys.unMuteAllMembers, req);
  try {
    ChatError.hasErrorFromResult(result);
  } on ChatError catch (e) {
    throw e;
  }
}
```
