---
title: "updateGroupAnnouncement method"
description: "API docs for the updateGroupAnnouncement method from the ChatGroupManager class, for the Dart programming language."
---

Updates the group announcement.

Only the group owner or admin can call this method.

**Throws** A description of the exception. See [ChatError](/zh-CN/api-reference/agora-chat/flutter/agora-chat-sdk/chat-error).

## Parameters

| Name | Description |
| --- | --- |
| `groupId` | The group ID. |
| `announcement` | The group announcement. |

```dart title="Dart"
Future<void> updateGroupAnnouncement(String groupId, String announcement)
```

## Implementation [#source]

```dart title="Dart"
Future<void> updateGroupAnnouncement(
  String groupId,
  String announcement,
) async {
  Map req = {'groupId': groupId, 'announcement': announcement};
  Map result = await _channel.invokeMethod(
      ChatMethodKeys.updateGroupAnnouncement, req);
  try {
    ChatError.hasErrorFromResult(result);
  } on ChatError catch (e) {
    throw e;
  }
}
```
