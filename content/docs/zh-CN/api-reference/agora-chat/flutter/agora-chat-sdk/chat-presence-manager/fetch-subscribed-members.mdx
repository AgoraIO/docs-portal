---
title: "fetchSubscribedMembers method"
description: "API docs for the fetchSubscribedMembers method from the ChatPresenceManager class, for the Dart programming language."
---

Uses pagination to get a list of users whose presence states you have subscribed to.

**Return** Which contains IDs of users whose presence states you have subscribed to. Returns null if you subscribe to no user's presence state.

**Throws** A description of the exception. See [ChatError](/zh-CN/api-reference/agora-chat/flutter/agora-chat-sdk/chat-error).

## Parameters

| Name | Description |
| --- | --- |
| `pageNum` | The current page number, starting from 1. |
| `pageSize` | The number of subscribed users on each page. |

```dart title="Dart"
Future<List<String>> fetchSubscribedMembers({int pageNum = 1, int pageSize = 20})
```

<a id="source"></a>

## Implementation

```dart title="Dart"
Future<List<String>> fetchSubscribedMembers({
  int pageNum = 1,
  int pageSize = 20,
}) async {
  Map req = {'pageNum': pageNum, "pageSize": pageSize};
  Map result = await _channel.invokeMethod(
      ChatMethodKeys.fetchSubscribedMembersWithPageNum, req);
  try {
    ChatError.hasErrorFromResult(result);
    List<String> list = [];
    result[ChatMethodKeys.fetchSubscribedMembersWithPageNum]
        ?.forEach((element) {
      if (element is String) {
        list.add(element);
      }
    });
    return list;
  } on ChatError catch (e) {
    throw e;
  }
}
```
