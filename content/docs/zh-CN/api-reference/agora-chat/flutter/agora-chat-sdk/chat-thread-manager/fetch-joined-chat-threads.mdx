---
title: "fetchJoinedChatThreads method"
description: "API docs for the fetchJoinedChatThreads method from the ChatThreadManager class, for the Dart programming language."
---

Paging to get the list of Chat Threads that the current user has joined from the server

**Return** Returns the result of `ChatCursorResult`, including the cursor for getting data next time and the chat thread object list.

**Throws** A description of the exception. See [ChatError](/zh-CN/api-reference/agora-chat/flutter/agora-chat-sdk/chat-error).

## Parameters

| Name | Description |
| --- | --- |
| `cursor` | The initial value can be empty or empty string. |
| `limit` | The number of fetches at one time. Value range (0, 50]. |

```dart title="Dart"
Future<ChatCursorResult<ChatThread>> fetchJoinedChatThreads({String? cursor, int limit = 20})
```

## Implementation [#source]

```dart title="Dart"
Future<ChatCursorResult<ChatThread>> fetchJoinedChatThreads({
  String? cursor,
  int limit = 20,
}) async {
  Map req = {"pageSize": limit};
  req.setValueWithOutNull("cursor", cursor);
  Map result =
      await _channel.invokeMethod(ChatMethodKeys.fetchJoinedChatThreads, req);
  try {
    ChatError.hasErrorFromResult(result);
    return ChatCursorResult.fromJson(
        result[ChatMethodKeys.fetchJoinedChatThreads],
        dataItemCallback: (map) {
      return ChatThread.fromJson(map);
    });
  } on ChatError catch (e) {
    throw e;
  }
}
```
