---
title: "noDisturb property"
description: "API docs for the noDisturb property from the ChatPushConfigs class, for the Dart programming language."
---

Whether to enable the do-not-disturb mode for push notifications.

- `true`: Yes.
- `false`: No. Sets it by `ChatPushManager.disableOfflinePush`.

```dart title="Dart"
bool noDisturb @Deprecated("Use ChatSilentModeResult property remindType, expireTimestamp and silentModeTime determine whether to enable"), final
```

<a id="source"></a>

## Implementation

```dart title="Dart"
@Deprecated(
    "Use ChatSilentModeResult property remindType, expireTimestamp and silentModeTime determine whether to enable")
final bool noDisturb;
```
