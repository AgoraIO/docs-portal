---
title: "onChatThreadUpdate property"
description: "API docs for the onChatThreadUpdate property from the ChatThreadEventHandler class, for the Dart programming language."
---

Occurs when a message thread is updated.

This callback is triggered when the message thread name is changed or a threaded reply is added or recalled.

Each member of the group to which the message thread belongs can receive the callback.

```dart title="Dart"
(void Function?(ChatThreadEvent event)?)onChatThreadUpdate final
```

## Implementation [#source]

```dart title="Dart"
final void Function(
  ChatThreadEvent event,
)? onChatThreadUpdate;
```
