---
title: "MessageType enum"
description: "API docs for the MessageType enum from the agora_chat_sdk library, for the Dart programming language."
---

The enumeration of message types.

### Inheritance

- [Object](https://api.flutter.dev/flutter/dart-core/Object-class.html)
- [Enum](https://api.flutter.dev/flutter/dart-core/Enum-class.html)
- MessageType

## Constructors [#constructors]

### MessageType [#MessageType]

[MessageType](/zh-CN/api-reference/agora-chat/flutter/agora-chat-sdk/message-type/message-type)

```dart title="Dart"
MessageType()
```

_const._

## Values [#values]

### TXT [#TXT]

```dart title="Dart"
TXT -> const MessageType
```

The text message.

`const MessageType(0)`

### IMAGE [#IMAGE]

```dart title="Dart"
IMAGE -> const MessageType
```

The image message.

`const MessageType(1)`

### VIDEO [#VIDEO]

```dart title="Dart"
VIDEO -> const MessageType
```

The video message.

`const MessageType(2)`

### LOCATION [#LOCATION]

```dart title="Dart"
LOCATION -> const MessageType
```

The location message.

`const MessageType(3)`

### VOICE [#VOICE]

```dart title="Dart"
VOICE -> const MessageType
```

The voice message.

`const MessageType(4)`

### FILE [#FILE]

```dart title="Dart"
FILE -> const MessageType
```

The file message.

`const MessageType(5)`

### CMD [#CMD]

```dart title="Dart"
CMD -> const MessageType
```

The command message.

`const MessageType(6)`

### CUSTOM [#CUSTOM]

```dart title="Dart"
CUSTOM -> const MessageType
```

The custom message.

`const MessageType(7)`

## Properties [#instance-properties]

### hashCode [#hashCode]

[hashCode](https://api.flutter.dev/flutter/dart-core/Object/hashCode.html)

```dart title="Dart"
hashCode -> int
```

The hash code for this object.

_read-only, inherited._

_Inherited._

### index [#index]

[index](https://api.flutter.dev/flutter/dart-core/Enum/index.html)

```dart title="Dart"
index -> int
```

A numeric identifier for the enumerated value.

_read-only, inherited._

_Inherited._

### runtimeType [#runtimeType]

[runtimeType](https://api.flutter.dev/flutter/dart-core/Object/runtimeType.html)

```dart title="Dart"
runtimeType -> Type
```

A representation of the runtime type of the object.

_read-only, inherited._

_Inherited._

## Methods [#instance-methods]

### noSuchMethod [#noSuchMethod]

[noSuchMethod](https://api.flutter.dev/flutter/dart-core/Object/noSuchMethod.html)

```dart title="Dart"
noSuchMethod(Invocation invocation) -> dynamic
```

Invoked when a non-existent method or property is accessed.

_inherited._

_Inherited._

### toString [#toString]

[toString](https://api.flutter.dev/flutter/dart-core/Object/toString.html)

```dart title="Dart"
toString() -> String
```

A string representation of this object.

_inherited._

_Inherited._

## Operators [#operators]

### operator == [#operator ==]

[operator ==](https://api.flutter.dev/flutter/dart-core/Object/operator_equals.html)

```dart title="Dart"
operator ==(Object other) -> bool
```

The equality operator.

_inherited._

_Inherited._

## Constants [#constants]

### values [#values]

[values](/zh-CN/api-reference/agora-chat/flutter/agora-chat-sdk/message-type/values-constant)

```dart title="Dart"
values -> const List<MessageType>
```

A constant List of the values in this enum, in order of their declaration. ` [TXT, IMAGE, VIDEO, LOCATION, VOICE, FILE, CMD, CUSTOM]`
