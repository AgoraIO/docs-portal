---
title: "onMuteListAddedFromChatRoom method"
description: "API docs for the onMuteListAddedFromChatRoom method from the ChatRoomManagerListener class, for the Dart programming language."
---

Occurs when there are chat room member(s) muted (added to mute list).

## Parameters

| Name | Description |
| --- | --- |
| `roomId` | The chatroom ID. |
| `mutes` | The members to be muted. |
| `expireTime` | The mute duration. |

```dart title="Dart"
void onMuteListAddedFromChatRoom(String roomId, List<String> mutes, String? expireTime)
```

<a id="source"></a>

## Implementation

```dart title="Dart"
void onMuteListAddedFromChatRoom(
  String roomId,
  List<String> mutes,
  String? expireTime,
);
```
