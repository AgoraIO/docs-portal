---
title: Rectangle
description: 目标区域相对于整个屏幕或窗口的位置，如不填，则表示整个屏幕或窗口。
_migration:
  type: generated-html
  status: migrated
  sourceUrl: https://doc.shengwang.cn/api-ref/rtc/flutter/API/class_rectangle
  sourcePath: html-docs/rtc/Flutter/API/class_rectangle.html
  generator: oxygen
  warnings: []
---

<a id="class_rectangle__prototype"></a>

```text
class Rectangle {
  const Rectangle({this.x, this.y, this.width, this.height});

  @JsonKey(name: 'x')
  final int? x;
  @JsonKey(name: 'y')
  final int? y;
  @JsonKey(name: 'width')
  final int? width;
  @JsonKey(name: 'height')
  final int? height;
  factory Rectangle.fromJson(Map<String, dynamic> json) =>
      _$RectangleFromJson(json);
  Map<String, dynamic> toJson() => _$RectangleToJson(this);
}
```

<a id="class_rectangle__parameters"></a>

## 属性

<ParameterList>
<a id="x"></a>
<Parameter name="x">

左上角的横向偏移。

</Parameter>
<a id="y"></a>
<Parameter name="y">

左上角的纵向偏移。

</Parameter>
<a id="width"></a>
<Parameter name="width">

目标区域的宽度。

</Parameter>
<a id="height"></a>
<Parameter name="height">

目标区域的高度。

</Parameter>
</ParameterList>
