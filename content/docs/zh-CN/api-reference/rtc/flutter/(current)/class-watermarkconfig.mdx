---
title: WatermarkConfig
description: 用于配置水印信息。
_migration:
  type: generated-html
  status: migrated
  sourceUrl: https://doc.shengwang.cn/api-ref/rtc/flutter/API/class_watermarkconfig
  sourcePath: html-docs/rtc/Flutter/API/class_watermarkconfig.html
  generator: oxygen
  warnings: []
---

<a id="class_watermarkconfig__prototype"></a>

```text
class WatermarkConfig implements AgoraSerializable {
  const WatermarkConfig(
      {this.id,
      this.type,
      this.buffer,
      this.timestamp,
      this.literal,
      this.imageUrl,
      this.options});

  @JsonKey(name: 'id')
  final String? id;

  @JsonKey(name: 'type')
  final WatermarkSourceType? type;

  @JsonKey(name: 'buffer')
  final WatermarkBuffer? buffer;

  @JsonKey(name: 'timestamp')
  final WatermarkTimestamp? timestamp;

  @JsonKey(name: 'literal')
  final WatermarkLiteral? literal;

  @JsonKey(name: 'imageUrl')
  final String? imageUrl;

  @JsonKey(name: 'options')
  final WatermarkOptions? options;

  factory WatermarkConfig.fromJson(Map<String, dynamic> json) =>
      _$WatermarkConfigFromJson(json);

  @override
  Map<String, dynamic> toJson() => _$WatermarkConfigToJson(this);
}
```

<a id="class_watermarkconfig__detailed_desc"></a>

:::info[自从]
自 v6.6.2 版本新增。
:::

<a id="class_watermarkconfig__parameters"></a>

## 属性

<ParameterList>
<a id="id"></a>
<Parameter name="id">

水印的唯一标识符，建议使用 UUID。

</Parameter>
<a id="type"></a>
<Parameter name="type">

水印类型，详见 [WatermarkSourceType](/zh-CN/api-reference/rtc/flutter/enum-watermarksourcetype)。

</Parameter>
<a id="buffer"></a>
<Parameter name="buffer">

水印的缓冲区。详见 [WatermarkBuffer](/zh-CN/api-reference/rtc/flutter/class-watermarkbuffer)。

</Parameter>
<a id="timestamp"></a>
<Parameter name="timestamp">

水印的时间戳。

:::note
注： 仅支持在 Linux 平台使用。
:::

</Parameter>
<a id="literal"></a>
<Parameter name="literal">

水印的文本内容。

:::note
注： 仅支持在 Linux 平台使用。
:::

</Parameter>
<a id="imageurl"></a>
<Parameter name="imageUrl">

水印图像文件的 URL，默认值为 `NULL`。

</Parameter>
<a id="options"></a>
<Parameter name="options">

水印的配置选项。详见 [WatermarkOptions](/zh-CN/api-reference/rtc/flutter/class-watermarkoptions)。

</Parameter>
</ParameterList>
