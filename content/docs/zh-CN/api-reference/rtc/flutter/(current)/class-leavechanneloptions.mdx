---
title: LeaveChannelOptions
description: 离开频道的选项。
_migration:
  type: generated-html
  status: migrated
  sourceUrl: https://doc.shengwang.cn/api-ref/rtc/flutter/API/class_leavechanneloptions
  sourcePath: html-docs/rtc/Flutter/API/class_leavechanneloptions.html
  generator: oxygen
  warnings: []
---

<a id="class_leavechanneloptions__prototype"></a>

```text
class LeaveChannelOptions {
  const LeaveChannelOptions(
      {this.stopAudioMixing, this.stopAllEffect, this.stopMicrophoneRecording});

  @JsonKey(name: 'stopAudioMixing')
  final bool? stopAudioMixing;
  @JsonKey(name: 'stopAllEffect')
  final bool? stopAllEffect;
  @JsonKey(name: 'stopMicrophoneRecording')
  final bool? stopMicrophoneRecording;
  factory LeaveChannelOptions.fromJson(Map<String, dynamic> json) =>
      _$LeaveChannelOptionsFromJson(json);
  Map<String, dynamic> toJson() => _$LeaveChannelOptionsToJson(this);
}
```

<a id="class_leavechanneloptions__parameters"></a>

## 属性

### stopAudioMixing

离开频道时，是否停止播放音乐文件及混音：

- true：（默认）停止播放音乐文件及混音。
- false： 不停止播放音乐文件及混音。

### stopAllEffect

离开频道时，是否停止播放音效：

- true：（默认）停止播放音效。
- false： 不停止播放音效。

### stopMicrophoneRecording

离开频道时，是否停止麦克风采集：

- true：（默认）停止麦克风采集。
- false： 不停止麦克风采集。
