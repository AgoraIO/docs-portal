---
title: WatermarkLiteral
description: 用于配置文字水印。
_migration:
  type: generated-html
  status: migrated
  sourceUrl: https://doc.shengwang.cn/api-ref/rtc/rn/API/class_watermarkliteral
  sourcePath: html-docs/rtc/RN/API/class_watermarkliteral.html
  generator: oxygen
  warnings: []
---

<a id="class_watermarkliteral__prototype"></a>

```text
export class WatermarkLiteral {
fontSize?: number;
strokeWidth?: number;
wmLiteral?: string;
fontFilePath?: string;
}
```

<a id="class_watermarkliteral__detailed_desc"></a>

:::info[自从]
自 v4.6.2 版本新增。
:::

:::note
注： 仅适用于 Linux 平台。
:::

<a id="class_watermarkliteral__parameters"></a>

## 属性

<ParameterList>
<a id="fontsize"></a>
<Parameter name="fontSize">

文字的字体大小。默认值为 10。

</Parameter>
<a id="strokewidth"></a>
<Parameter name="strokeWidth">

文字的描边宽度。默认值为 1。

</Parameter>
<a id="wmliteral"></a>
<Parameter name="wmLiteral">

水印的文字内容。默认值为 `NULL`。

:::note
注： 如果异步使用，请将字符串复制到不会被释放的内存中。
:::

</Parameter>
<a id="fontfilepath"></a>
<Parameter name="fontFilePath">

字体文件的路径。默认值为 `NULL`。字体文件应为 .ttf 格式。如果未设置，SDK 会使用系统默认字体（如果可用）。

:::note
注： 如果异步使用，请将字符串复制到不会被释放的内存中。
:::

</Parameter>
</ParameterList>
