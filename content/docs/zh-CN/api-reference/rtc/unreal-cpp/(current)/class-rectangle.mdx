---
title: Rectangle
description: 目标区域相对于整个屏幕或窗口的位置，如不填，则表示整个屏幕或窗口。
_migration:
  type: generated-html
  status: migrated
  sourceUrl: https://doc.shengwang.cn/api-ref/rtc/unreal-cpp/API/class_rectangle
  sourcePath: html-docs/rtc/Unreal-cpp/API/class_rectangle.html
  generator: oxygen
  warnings: []
---

<a id="class_rectangle__prototype"></a>

```text
struct Rectangle {
    int x;
    int y;
    int width;
    int height;

    Rectangle(): x(0), y(0), width(0), height(0) {}
    Rectangle(int xx, int yy, int ww, int hh): x(xx), y(yy), width(ww), height(hh) {}
};
```

<a id="class_rectangle__parameters"></a>

## 属性

<ParameterList>
<a id="x"></a>
<Parameter name="x">

左上角的横向偏移。

</Parameter>
<a id="y"></a>
<Parameter name="y">

左上角的纵向偏移。

</Parameter>
<a id="width"></a>
<Parameter name="width">

目标区域的宽度。

</Parameter>
<a id="height"></a>
<Parameter name="height">

目标区域的高度。

</Parameter>
</ParameterList>
