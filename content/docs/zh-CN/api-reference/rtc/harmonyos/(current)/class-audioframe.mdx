---
title: AudioFrame
description: 原始音频数据。
_migration:
  type: existing-mdx-normalized
  sourceUrl: https://doc.shengwang.cn/api-ref/rtc/harmonyos/API/class_audioframe
  sourcePath: html-docs/rtc/HarmonyOS/API/class_audioframe.html
  generator: api-center-existing-normalizer
  status: warning
  warnings:
    - code: empty-source-code
      severity: info
      message: 1 empty code block was omitted because the authoritative legacy source provides no signature.
      count: 1
---
<a id="class_audioframe__prototype"></a>

<a id="class_audioframe__parameters"></a>

### `samplesPerChannel`

每个声道的采样点数。

### `bytesPerSample`

每个采样点的字节数。对于 PCM 来说，一般使用 16 bit，即两个字节。

### `channelNums`

声道数量(如果是立体声，数据是交叉的)。

- 1: 单声道
- 2: 双声道

### `sampleRataHz`

每声道每秒的采样点数。

声音数据缓存区（如果是立体声，数据是交叉存储的）。

缓存区数据大小 `buffer` = `samples` × `channels` × `bytesPerSample`。

### `timestamp`

音频帧的时间戳。
