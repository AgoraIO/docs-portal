---
title: "URL 拉流播放"
description: "获取当前订阅的视频层级。"
---

## <span id="toc_rte_player"></span>abrSubscriptionLayer [#api_rteplayerinfo_abrsubscriptionlayer]

获取当前订阅的视频层级。

<a id="api_rteplayerinfo_abrsubscriptionlayer__prototype"></a>

```java
public Constants.AbrSubscriptionLayer abrSubscriptionLayer()
```

<a id="api_rteplayerinfo_abrsubscriptionlayer__detailed_desc"></a>

该方法仅在打开 RTE URL 时有效。

### 返回值 [#api_rteplayerinfo_abrsubscriptionlayer__return_values]

方法调用成功，返回 [AbrSubscriptionLayer](/zh-CN/api-reference/rtc/android/enum-rteabrsubscriptionlayer) 对象。

- [PlayerInfo](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_rteplayerinfo)

## addView [#api_canvas_addview]

添加一个渲染视图。

<a id="api_canvas_addview__prototype"></a>

```java
public void addView(View view, ViewConfig viewConfig) throws RteException
```

#### `自从` [#api_canvas_addview__detailed_desc]

自 v4.4.0 版本新增。

当你需要使用播放器播放视频时，可以调用该方法将视图添加到视频渲染器中，以显示视频内容。

:::info[注]
当前仅支持一个视图。
:::

### 适用场景 [#api_canvas_addview__scenario]

当你需要使用播放器播放视频时，可以调用此方法将视图添加到视频渲染器中，以显示视频内容。

### 调用时机 [#api_canvas_addview__timing]

必须在调用 [setCanvas](/zh-CN/api-reference/rtc/android/play/rte-player#api_player_setcanvas) 之前调用该方法。

### 参数 [#api_canvas_addview__parameters]

#### `view`

`SurfaceView` 对象。

#### `viewConfig`

视图的相关设置。

### 异常 [#api_canvas_addview__exception]

添加渲染视图失败，SDK 会抛出异常，并返回相应的错误信息。你需要捕获异常并进行处理。

- [Canvas](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_canvas)

## audioBitsPerSample [#api_rteplayerinfo_audiobitspersample]

获取每个音频采样点的位数。

<a id="api_rteplayerinfo_audiobitspersample__prototype"></a>

```java
public int audioBitsPerSample()
```

#### `自从` [#api_rteplayerinfo_audiobitspersample__detailed_desc]

自 v4.5.1 版本新增。

:::info[注]
[audioBitsPerSample](/zh-CN/api-reference/rtc/android/play/rte-player#api_rteplayerinfo_audiobitspersample) 仅在打开非 RTM URL 时有效。
:::

### 返回值 [#api_rteplayerinfo_audiobitspersample__return_values]

方法调用成功，返回每个音频采样点的位数，单位为 bit。

- [PlayerInfo](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_rteplayerinfo)

## audioChannels [#api_rteplayerinfo_audiochannels]

获取音频通道数。

<a id="api_rteplayerinfo_audiochannels__prototype"></a>

```java
public int audioChannels()
```

### <span id="api_rteplayerinfo_audiochannels__detailed_desc"></span>返回值 [#api_rteplayerinfo_audiochannels__return_values]

方法调用成功，返回音频通道数。

- [PlayerInfo](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_rteplayerinfo)

## audioSampleRate [#api_rteplayerinfo_audiosamplerate]

获取音频采样率。

<a id="api_rteplayerinfo_audiosamplerate__prototype"></a>

```java
public int audioSampleRate()
```

#### `自从` [#api_rteplayerinfo_audiosamplerate__detailed_desc]

自 v4.5.1 版本新增。

### 返回值 [#api_rteplayerinfo_audiosamplerate__return_values]

方法调用成功，返回音频采样率，单位为 Hz。

- [PlayerInfo](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_rteplayerinfo)

## Canvas [#api_canvas_canvas_canvas]

构造一个 Canvas 对象。

<a id="api_canvas_canvas_canvas__prototype"></a>

```java
public Canvas(Rte rte, CanvasInitialConfig config)
```

#### `自从` [#api_canvas_canvas_canvas__detailed_desc]

自 v4.4.0 版本新增。

### 参数 [#api_canvas_canvas_canvas__parameters]

#### `rte`

用于构造 [Canvas](/zh-CN/api-reference/rtc/android/play/rte-player#api_canvas_canvas_canvas) 的 [Rte](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_rte) 对象，详见 [Rte](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_rte)。

#### `config`

初始化配置对象。你可以传入 `null`。

- [Canvas](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_canvas)

## code [#api_error_code]

获取 API 调用失败时返回的错误码。

<a id="api_error_code__prototype"></a>

```java
public Constants.ErrorCode code()
```

#### `自从` [#api_error_code__detailed_desc]

自 v4.4.0 版本新增。

### 调用时机 [#api_error_code__timing]

你可以在 API 调用失败时调用此方法获取错误码。

### 返回值 [#api_error_code__return_values]

- 方法调用失败时，返回错误码。详见 [ErrorCode](/zh-CN/api-reference/rtc/android/enum-rteerrorcode)。

- [Error](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_error)

## currentUrl [#api_rteplayerinfo_currenturl]

获取当前正在播放的 URL。

<a id="api_rteplayerinfo_currenturl__prototype"></a>

```java
public String currentUrl()
```

### <span id="api_rteplayerinfo_currenturl__detailed_desc"></span>返回值 [#api_rteplayerinfo_currenturl__return_values]

方法调用成功时，返回当前正在播放的 URL。 方法调用失败，返回空字符串。

- [PlayerInfo](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_rteplayerinfo)

## destroy [#api_rte_destroy_rte]

销毁 RTE 对象，释放相关资源。

<a id="api_rte_destroy_rte__prototype"></a>

```java
public synchronized void destroy() throws RteException
```

#### `自从` [#api_rte_destroy_rte__detailed_desc]

自 v4.4.0 版本新增。

### 异常 [#api_rte_destroy_rte__exception]

销毁失败，SDK 会抛出异常，并返回相应的错误信息。你需要捕获异常并进行处理。

- [Rte](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_rte)

## duration [#api_rteplayerinfo_duration]

获取当前媒体资源的时长。

<a id="api_rteplayerinfo_duration__prototype"></a>

```java
public long duration()
```

#### `自从` [#api_rteplayerinfo_duration__detailed_desc]

自 v4.5.1 版本新增。

:::info[注]
该方法仅在播放本地媒体文件或输入在线媒体流时有效。
:::

### 返回值 [#api_rteplayerinfo_duration__return_values]

方法调用成功，返回当前媒体资源的时长，单位为毫秒。

- [PlayerInfo](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_rteplayerinfo)

## errorCode [#api_rteexception_errorcode_rteexception]

获取 RteException 中携带的错误码。

<a id="api_rteexception_errorcode_rteexception__prototype"></a>

```java
public Constants.ErrorCode errorCode()
```

### <span id="api_rteexception_errorcode_rteexception__detailed_desc"></span>返回值 [#api_rteexception_errorcode_rteexception__return_values]

方法调用成功，返回 [ErrorCode](/zh-CN/api-reference/rtc/android/enum-rteerrorcode) 对象。

- [RteException](/zh-CN/api-reference/rtc/android/class-rteexception)

## getAbrFallbackLayer [#api_playerconfig_getabrfallbacklayer]

获取已配置的视频流回退选项。

<a id="api_playerconfig_getabrfallbacklayer__prototype"></a>

```java
public Constants.AbrFallbackLayer getAbrFallbackLayer() throws RteException
```

#### `自从` [#api_playerconfig_getabrfallbacklayer__detailed_desc]

自 v4.4.0 版本新增。

### 调用时机 [#api_playerconfig_getabrfallbacklayer__timing]

你必须在调用 `getConfigs` 之后调用此方法。

### 异常 [#api_playerconfig_getabrfallbacklayer__exception]

如果方法调用失败，SDK 会抛出异常，并返回相应的错误信息。你需要捕获异常并进行处理。

### 返回值 [#api_playerconfig_getabrfallbacklayer__return_values]

方法调用成功，返回已配置的视频流回退选项。详见 [AbrFallbackLayer](/zh-CN/api-reference/rtc/android/enum-rteabrfallbacklayer)。

- [PlayerConfig](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_playerconfig)

## getAbrSubscriptionLayer [#api_playerconfig_getabrsubscriptionlayer]

获取已订阅视频流的质量层级。

<a id="api_playerconfig_getabrsubscriptionlayer__prototype"></a>

```java
public Constants.AbrSubscriptionLayer getAbrSubscriptionLayer() throws RteException
```

#### `自从` [#api_playerconfig_getabrsubscriptionlayer__detailed_desc]

自 v4.4.0 版本新增。

### 调用时机 [#api_playerconfig_getabrsubscriptionlayer__timing]

你需要在调用 `getConfigs` 方法之后调用此方法。

### 异常 [#api_playerconfig_getabrsubscriptionlayer__exception]

如果方法调用失败，SDK 会抛出包含相应错误信息的 [RteException](/zh-CN/api-reference/rtc/android/class-rteexception) 异常。你需要捕获并处理该异常。

### 返回值 [#api_playerconfig_getabrsubscriptionlayer__return_values]

方法调用成功，返回当前设置的视频质量层级。详见 [AbrSubscriptionLayer](/zh-CN/api-reference/rtc/android/enum-rteabrsubscriptionlayer)。

- [PlayerConfig](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_playerconfig)

## getAppId [#api_config_getappid]

获取已设置的 App ID。

<a id="api_config_getappid__prototype"></a>

```java
public String getAppId() throws RteException
```

#### `自从` [#api_config_getappid__detailed_desc]

自 v4.4.0 版本新增。

### 返回值 [#api_config_getappid__return_values]

方法调用成功时，返回已设置的 App ID。 方法调用失败，返回空字符串。

- [Config](/zh-CN/api-reference/rtc/android/class-config)

## getAutoPlay [#api_playerconfig_getautoplay]

获取媒体播放器的自动播放设置。

<a id="api_playerconfig_getautoplay__prototype"></a>

```java
public boolean getAutoPlay() throws RteException
```

#### `自从` [#api_playerconfig_getautoplay__detailed_desc]

自 v4.4.0 版本新增。

### 调用时机 [#api_playerconfig_getautoplay__timing]

此方法必须在调用 `getConfigs` 之后调用。

### 异常 [#api_playerconfig_getautoplay__exception]

获取自动播放设置失败，SDK 会抛出异常，并返回相应的错误信息。你需要捕获异常并进行处理。

### 返回值 [#api_playerconfig_getautoplay__return_values]

- true：获取成功。
- false：获取失败。

- [PlayerConfig](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_playerconfig)

## getConfigs [3/3] [#api_canvas_getconfigs_canvas]

获取播放器当前的视频渲染配置。

<a id="api_canvas_getconfigs_canvas__prototype"></a>

```java
public void getConfigs(CanvasConfig config) throws RteException
```

#### `自从` [#api_canvas_getconfigs_canvas__detailed_desc]

自 v4.4.0 版本新增。

### 调用时机 [#api_canvas_getconfigs_canvas__timing]

你必须在调用 [Canvas](/zh-CN/api-reference/rtc/android/play/rte-player#api_canvas_canvas_canvas) 之后调用此方法。

### 参数 [#api_canvas_getconfigs_canvas__parameters]

#### `config`

[Canvas](/zh-CN/api-reference/rtc/android/play/rte-player#api_canvas_canvas_canvas) 对象的设置，详见 [CanvasConfig](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_canvasconfig)。

### 异常 [#api_canvas_getconfigs_canvas__exception]

如果方法调用失败，SDK 会抛出包含相应错误信息的 [RteException](/zh-CN/api-reference/rtc/android/class-rteexception)。你需要捕获并处理该异常。

- [Canvas](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_canvas)

## getConfigs [2/3] [#api_player_getconfigs_player]

获取当前媒体播放器的配置选项。

<a id="api_player_getconfigs_player__prototype"></a>

```java
public void getConfigs(PlayerConfig config) throws RteException
```

#### `自从` [#api_player_getconfigs_player__detailed_desc]

自 v4.4.0 版本新增。

### 调用时机 [#api_player_getconfigs_player__timing]

该方法必须在调用 [Player](/zh-CN/api-reference/rtc/android/play/rte-player#api_player_player_player) 之后使用。

### 参数 [#api_player_getconfigs_player__parameters]

#### `config`

媒体播放器配置对象，详见 [PlayerConfig](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_playerconfig)。

### 异常 [#api_player_getconfigs_player__exception]

如果方法调用失败，SDK 会抛出异常，并返回相应的错误信息。你需要捕获异常并进行处理。

- [Player](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_player)

## getConfigs [1/3] [#api_rte_getconfigs_rte]

获取 Rte 对象的配置信息。

<a id="api_rte_getconfigs_rte__prototype"></a>

```java
public void getConfigs(Config config) throws RteException
```

#### `自从` [#api_rte_getconfigs_rte__detailed_desc]

自 v4.4.0 版本新增。

### 调用时机 [#api_rte_getconfigs_rte__timing]

该方法必须在调用 [Rte](/zh-CN/api-reference/rtc/android/play/rte-player#api_rte_rte_rte) 之后使用。

### 参数 [#api_rte_getconfigs_rte__parameters]

#### `config`

详见 [Config](/zh-CN/api-reference/rtc/android/class-config)。

### 异常 [#api_rte_getconfigs_rte__exception]

如果方法调用失败，SDK 会抛出 [RteException](/zh-CN/api-reference/rtc/android/class-rteexception) 并返回相应的错误信息。你需要捕获并处理该异常。

- [Rte](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_rte)

## getFromBridge [#api_rte_getfrombridge]

从 RtcEngine 中桥接一个 RTE 对象。

<a id="api_rte_getfrombridge__prototype"></a>

```java
public static synchronized Rte getFromBridge() throws RteException
```

#### `自从` [#api_rte_getfrombridge__detailed_desc]

自 v4.4.0 版本新增。

通过该方法创建的 `RTE` 对象无需通过 [initMediaEngine](/zh-CN/api-reference/rtc/android/play/rte-player#api_rte_initmediaengine) 进行初始化。如果你尚未创建并初始化 `RTC` 引擎，可以先调用 `RTE` 创建一个 `RTE` 对象，再调用 [initMediaEngine](/zh-CN/api-reference/rtc/android/play/rte-player#api_rte_initmediaengine) 进行初始化。

### 调用时机 [#api_rte_getfrombridge__timing]

调用此方法前，请确保已调用 [create [2/2]](/zh-CN/api-reference/rtc/android/initialize#api_irtcengine_initialize) 初始化 `RTC` 引擎。

### 异常 [#api_rte_getfrombridge__exception]

如果方法调用失败，SDK 会抛出 [RteException](/zh-CN/api-reference/rtc/android/class-rteexception) 并返回相应的错误信息。你需要捕获并处理该异常。

### 返回值 [#api_rte_getfrombridge__return_values]

方法调用成功，返回 `RTE` 对象。

- [Rte](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_rte)

## getInfo [#api_player_getinfo]

检索与播放器和媒体流相关的信息。

<a id="api_player_getinfo__prototype"></a>

```java
public void getInfo(PlayerInfo info) throws RteException
```

#### `自从` [#api_player_getinfo__detailed_desc]

自 v4.4.0 版本新增。

### 调用时机 [#api_player_getinfo__timing]

该方法必须在调用 [Player](/zh-CN/api-reference/rtc/android/play/rte-player#api_player_player_player) 之后使用。

### 参数 [#api_player_getinfo__parameters]

#### `info`

与播放器和媒体流相关的信息，详见 [PlayerInfo](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_rteplayerinfo)。

### 异常 [#api_player_getinfo__exception]

如果该方法调用失败，SDK 会抛出异常，并返回相应的错误信息。你需要捕获异常并进行处理。

- [Player](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_player)

## getJsonParameter [#api_config_getjsonparameter_config]

获取当前设置的 JSON 格式的私有参数集。

<a id="api_config_getjsonparameter_config__prototype"></a>

```java
public String getJsonParameter() throws RteException
```

#### `自从` [#api_config_getjsonparameter_config__detailed_desc]

自 v4.4.0 版本新增。

### 异常 [#api_config_getjsonparameter_config__exception]

调用失败，SDK 会抛出 [RteException](/zh-CN/api-reference/rtc/android/class-rteexception) 异常，你需要捕获异常并进行处理。

### 返回值 [#api_config_getjsonparameter_config__return_values]

方法调用成功时，返回已设置的 JSON 格式的参数集。 方法调用失败，返回空字符串。

- [Config](/zh-CN/api-reference/rtc/android/class-config)

## getLoopCount [#api_playerconfig_getloopcount]

获取媒体文件的循环播放次数。

<a id="api_playerconfig_getloopcount__prototype"></a>

```java
public int getLoopCount() throws RteException
```

#### `自从` [#api_playerconfig_getloopcount__detailed_desc]

自 v4.5.1 版本新增。

### 异常 [#api_playerconfig_getloopcount__exception]

获取失败，SDK 会抛出异常，并返回相应的错误信息。你需要捕获异常并进行处理。

### 返回值 [#api_playerconfig_getloopcount__return_values]

方法调用成功，返回媒体文件的循环播放次数。

- [PlayerConfig](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_playerconfig)

## getVideoMirrorMode [#api_canvasconfig_getmirrormode]

获取视频镜像模式。

<a id="api_canvasconfig_getmirrormode__prototype"></a>

```java
public Constants.VideoMirrorMode getVideoMirrorMode() throws RteException
```

#### `自从` [#api_canvasconfig_getmirrormode__detailed_desc]

自 v4.4.0 版本新增。

### 异常 [#api_canvasconfig_getmirrormode__exception]

获取视频镜像模式失败，SDK 会抛出异常，并返回相应的错误信息。你需要捕获异常并进行处理。

### 返回值 [#api_canvasconfig_getmirrormode__return_values]

方法调用成功，返回视频镜像模式，详见 [VideoMirrorMode](/zh-CN/api-reference/rtc/android/enum-rtevideomirrormode)。

- [CanvasConfig](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_canvasconfig)

## getPlaybackSpeed [#api_playerconfig_getplaybackspeed]

获取播放速度参数。

<a id="api_playerconfig_getplaybackspeed__prototype"></a>

```java
public int getPlaybackSpeed() throws RteException
```

#### `自从` [#api_playerconfig_getplaybackspeed__detailed_desc]

自 v4.5.1 版本新增。

### 异常 [#api_playerconfig_getplaybackspeed__exception]

获取播放速度失败，SDK 会抛出异常，并返回相应的错误信息。你需要捕获异常并进行处理。

### 返回值 [#api_playerconfig_getplaybackspeed__return_values]

方法调用成功，返回播放速度的值。

- [PlayerConfig](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_playerconfig)

## getPlayoutVolume [#api_playerconfig_getplayoutvolume]

获取媒体播放器的播放音量。

<a id="api_playerconfig_getplayoutvolume__prototype"></a>

```java
public int getPlayoutVolume() throws RteException
```

#### `自从` [#api_playerconfig_getplayoutvolume__detailed_desc]

自 v4.5.1 版本新增。

### 返回值 [#api_playerconfig_getplayoutvolume__return_values]

方法调用成功，返回媒体播放器的播放音量级别。

- [PlayerConfig](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_playerconfig)

## getPosition [#api_player_getposition]

获取当前的播放位置。

<a id="api_player_getposition__prototype"></a>

```java
public long getPosition() throws RteException
```

#### `自从` [#api_player_getposition__detailed_desc]

自 v4.5.1 版本新增。

### 异常 [#api_player_getposition__exception]

如果方法调用失败，SDK 会抛出一个 [RteException](/zh-CN/api-reference/rtc/android/class-rteexception) 异常，并返回相应的错误信息。你需要捕获异常并进行处理。

### 返回值 [#api_player_getposition__return_values]

方法调用成功，返回当前的播放位置，单位为毫秒。

- [Player](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_player)

## getVideoRenderMode [#api_canvasconfig_getrendermode]

获取当前设置的视频镜像模式。

<a id="api_canvasconfig_getrendermode__prototype"></a>

```java
public Constants.VideoRenderMode getVideoRenderMode() throws RteException
```

#### `自从` [#api_canvasconfig_getrendermode__detailed_desc]

自 v4.4.0 版本新增。

### 调用时机 [#api_canvasconfig_getrendermode__timing]

你需要在调用 `getConfigs` 之后调用此方法。

### 异常 [#api_canvasconfig_getrendermode__exception]

如果方法调用失败，SDK 会抛出异常，并返回相应的错误信息。你需要捕获异常并进行处理。

### 返回值 [#api_canvasconfig_getrendermode__return_values]

方法调用成功，返回当前设置的视频镜像模式。详见 [VideoMirrorMode](/zh-CN/api-reference/rtc/android/enum-rtevideomirrormode)。

- [CanvasConfig](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_canvasconfig)

## getStats [#api_player_getstats]

获取播放器当前播放媒体资源的统计信息。

<a id="api_player_getstats__prototype"></a>

```java
public void getStats(PlayerGetStatsCallback callback)
```

#### `自从` [#api_player_getstats__detailed_desc]

自 v4.4.0 版本新增。

该方法通过回调异步返回播放器的统计信息。

### 调用时机 [#api_player_getstats__timing]

该方法必须在调用 [openWithUrl](/zh-CN/api-reference/rtc/android/play/rte-player#api_player_openwithurl) 之后调用。

### 参数 [#api_player_getstats__parameters]

#### `callback`

用于异步接收统计信息和错误信息的回调接口。详见 [PlayerGetStatsCallback](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_playergetstatscallback)。

- [Player](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_player)

## hasAudio [#api_rteplayerinfo_hasaudio]

判断媒体资源是否包含音频流。

<a id="api_rteplayerinfo_hasaudio__prototype"></a>

```java
public boolean hasAudio()
```

### <span id="api_rteplayerinfo_hasaudio__detailed_desc"></span>返回值 [#api_rteplayerinfo_hasaudio__return_values]

- true：媒体资源中包含音频流。
- false：媒体资源中不包含音频流。

- [PlayerInfo](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_rteplayerinfo)

## hasVideo [#api_rteplayerinfo_hasvideo]

判断是否包含视频流。

<a id="api_rteplayerinfo_hasvideo__prototype"></a>

```java
public boolean hasVideo()
```

### <span id="api_rteplayerinfo_hasvideo__detailed_desc"></span>返回值 [#api_rteplayerinfo_hasvideo__return_values]

true：URL 源包含视频流。 false：URL 源不包含视频流。

- [PlayerInfo](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_rteplayerinfo)

## initMediaEngine [#api_rte_initmediaengine]

初始化 RTE 引擎。

<a id="api_rte_initmediaengine__prototype"></a>

```java
public void initMediaEngine(AsyncCallback callBack) throws RteException
```

#### `自从` [#api_rte_initmediaengine__detailed_desc]

自 v4.4.0 版本新增。

该方法为异步方法，通过回调函数返回初始化结果。你需要在创建 `RTE` 对象并设置 App ID 后调用该方法。

### 参数 [#api_rte_initmediaengine__parameters]

#### `callBack`

异步返回引擎初始化结果的回调。详见 [AsyncCallback](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_asynccallback)。

### 异常 [#api_rte_initmediaengine__exception]

初始化失败，SDK 会抛出异常，并返回相应的错误信息。你需要捕获异常并进行处理。

- [Rte](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_rte)

## isAudioMuted [#api_rteplayerinfo_isaudiomuted]

判断播放器是否停止接收远端音频流。

<a id="api_rteplayerinfo_isaudiomuted__prototype"></a>

```java
public boolean isAudioMuted()
```

#### `自从` [#api_rteplayerinfo_isaudiomuted__detailed_desc]

自 v4.4.0 版本新增。

### 返回值 [#api_rteplayerinfo_isaudiomuted__return_values]

- true：播放器已停止接收远端音频流。
- false：播放器仍在接收远端音频流。

- [PlayerInfo](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_rteplayerinfo)

## isVideoMuted [#api_rteplayerinfo_isvideomuted]

判断播放器是否停止接收远端视频流。

<a id="api_rteplayerinfo_isvideomuted__prototype"></a>

```java
public boolean isVideoMuted()
```

#### `自从` [#api_rteplayerinfo_isvideomuted__detailed_desc]

自 v4.4.0 版本新增。

:::info[注]
[isVideoMuted](/zh-CN/api-reference/rtc/android/play/rte-player#api_rteplayerinfo_isvideomuted) 接口仅在打开 RTE URL 时有效。
:::

### 返回值 [#api_rteplayerinfo_isvideomuted__return_values]

- true：播放器已停止接收远端视频流。
- false：播放器仍在接收远端视频流。

- [PlayerInfo](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_rteplayerinfo)

## message [#api_error_message]

获取详细的错误信息。

<a id="api_error_message__prototype"></a>

```java
public String message()
```

#### `自从` [#api_error_message__detailed_desc]

自 v4.4.0 版本新增。

### 调用时机 [#api_error_message__timing]

当 API 调用失败时，可以调用该方法获取错误信息，以帮助排查问题。

### 返回值 [#api_error_message__return_values]

- 方法调用成功时，返回错误信息字符串。
- 方法调用失败，返回空字符串。

- [Error](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_error)

## muteAudio [#api_player_muteaudio]

开始或停止接收音频流。

<a id="api_player_muteaudio__prototype"></a>

```java
public void muteAudio(boolean mute) throws RteException
```

#### `自从` [#api_player_muteaudio__detailed_desc]

自 v4.4.0 版本新增。

### 调用时机 [#api_player_muteaudio__timing]

该方法需要在调用 [openWithUrl](/zh-CN/api-reference/rtc/android/play/rte-player#api_player_openwithurl) 方法之后调用。

### 相关回调 [#api_player_muteaudio__related]

调用该方法成功后，会触发 [onPlayerInfoUpdated](/zh-CN/api-reference/rtc/android/play/mediaplayer/mediaplayer-observer#callback_imediaplayersourceobserver_onplayerinfoupdated) 回调，上报最新的播放器和媒体拉流信息。

### 参数 [#api_player_muteaudio__parameters]

#### `mute`

是否接收音频流：

- true：不接收音频流。
- false：接收音频流。

### 异常 [#api_player_muteaudio__exception]

方法调用失败时，SDK 会抛出包含错误信息的 [RteException](/zh-CN/api-reference/rtc/android/class-rteexception) 异常。你需要捕获并处理该异常。

- [Player](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_player)

## muteVideo [#api_player_mutevideo]

开始或停止接收远端视频流。

<a id="api_player_mutevideo__prototype"></a>

```java
public void muteVideo(boolean mute) throws RteException
```

#### `自从` [#api_player_mutevideo__detailed_desc]

自 v4.4.0 版本新增。

### 调用时机 [#api_player_mutevideo__timing]

你必须在调用 [openWithUrl](/zh-CN/api-reference/rtc/android/play/rte-player#api_player_openwithurl) 之后调用该方法。

### 相关回调 [#api_player_mutevideo__related]

该方法调用成功后，会触发 [onPlayerInfoUpdated](/zh-CN/api-reference/rtc/android/play/mediaplayer/mediaplayer-observer#callback_imediaplayersourceobserver_onplayerinfoupdated) 回调，用于上报最新的播放器和媒体流信息。

### 参数 [#api_player_mutevideo__parameters]

#### `mute`

是否接收远端视频流：

- true：不接收远端视频流。
- false：接收远端视频流。

### 异常 [#api_player_mutevideo__exception]

如果方法调用失败，SDK 会抛出带有相应错误信息的 [RteException](/zh-CN/api-reference/rtc/android/class-rteexception) 异常。你需要捕获并处理该异常。

- [Player](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_player)

## openWithUrl [#api_player_openwithurl]

打开一个 URL 资源。

<a id="api_player_openwithurl__prototype"></a>

```java
public void openWithUrl(String url, long startTime, AsyncCallback callback)
```

#### `自从` [#api_player_openwithurl__detailed_desc]

自 v4.4.0 版本新增。

该方法支持通过 URL 打开实时流媒体资源。如果你希望加快打开速度，可以在调用该方法前使用 [preloadWithUrl](/zh-CN/api-reference/rtc/android/play/rte-player#api_player_preloadwithurl) 预加载资源。 如果资源打开失败，你会收到 [onStateChanged](/zh-CN/api-reference/rtc/android/play/rte-player#callback_playerobserver_onstatechanged) 回调，播放器状态为 `FAILED`。此时需要先调用 [stop](/zh-CN/api-reference/rtc/android/play/rte-player#api_player_stop_player)，然后再次调用 [openWithUrl](/zh-CN/api-reference/rtc/android/play/rte-player#api_player_openwithurl) 重新打开资源。 如果你关闭了自动播放功能，可以在资源打开后调用 [play](/zh-CN/api-reference/rtc/android/play/rte-player#api_player_play_player) 开始播放。

:::info[注]
该方法目前仅支持以 `rte://` 开头的 URL、CDN 推流 URL 和本地媒体文件。
:::

### 调用时机 [#api_player_openwithurl__timing]

该方法必须在 [Player](/zh-CN/api-reference/rtc/android/play/rte-player#api_player_player_player) 之后调用。

### 相关回调 [#api_player_openwithurl__related]

调用该方法后会触发 [onStateChanged](/zh-CN/api-reference/rtc/android/play/rte-player#callback_playerobserver_onstatechanged) 回调，播放器状态为 `OPENING`，表示正在打开 URL。成功打开后，播放器状态为 `OPEN_COMPLETED`。

### 参数 [#api_player_openwithurl__parameters]

#### `url`

要打开的 URL，可以是 CDN 推流 URL、本地媒体文件，或以 `rte://` 为前缀的 URL。

#### `startTime`

播放的起始位置，单位为毫秒。

#### `callback`

异步通知打开 URL 资源结果的回调。你可以通过该回调的 [onResult](/zh-CN/api-reference/rtc/android/play/rte-player#callback_asynccallback_onresult_asynccallback) 方法获取错误信息。

- [Player](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_player)

## pause [#api_player_pause_player]

暂停播放。

<a id="api_player_pause_player__prototype"></a>

```java
public void pause() throws RteException
```

#### `自从` [#api_player_pause_player__detailed_desc]

自 v4.4.0 版本新增。

调用 [play](/zh-CN/api-reference/rtc/android/play/rte-player#api_player_play_player) 播放 URL 资源后，如需暂停播放，可以调用该方法。如需停止播放，请调用 [stop](/zh-CN/api-reference/rtc/android/play/rte-player#api_player_stop_player)。

### 调用时机 [#api_player_pause_player__timing]

该方法必须在调用 [play](/zh-CN/api-reference/rtc/android/play/rte-player#api_player_play_player) 之后调用。

### 相关回调 [#api_player_pause_player__related]

成功调用该方法后，会触发 [onStateChanged](/zh-CN/api-reference/rtc/android/play/rte-player#callback_playerobserver_onstatechanged) 回调，报告播放器状态为 `PAUSED`。

### 异常 [#api_player_pause_player__exception]

如果该方法调用失败，SDK 会抛出带有相应错误信息的 [RteException](/zh-CN/api-reference/rtc/android/class-rteexception) 异常。你需要捕获并处理该异常。

- [Player](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_player)

## play [#api_player_play_player]

播放一个 URL 资源。

<a id="api_player_play_player__prototype"></a>

```java
public void play() throws RteException
```

#### `自从` [#api_player_play_player__detailed_desc]

自 v4.4.0 版本新增。

如果你已关闭自动播放，在使用 [openWithUrl](/zh-CN/api-reference/rtc/android/play/rte-player#api_player_openwithurl) 成功打开一个在线媒体流后，可以调用该方法开始播放。

:::info[注]
该方法当前仅支持以 `rte://` 为前缀的 URL。
:::

### 调用时机 [#api_player_play_player__timing]

仅在收到状态为 `OPEN_COMPLETED` 的 [onStateChanged](/zh-CN/api-reference/rtc/android/play/rte-player#callback_playerobserver_onstatechanged) 回调后调用该方法。

### 相关回调 [#api_player_play_player__related]

调用该方法成功后，会触发 [onStateChanged](/zh-CN/api-reference/rtc/android/play/rte-player#callback_playerobserver_onstatechanged) 回调以上报播放器状态。

### 异常 [#api_player_play_player__exception]

如果方法调用失败，SDK 会抛出包含相应错误信息的 [RteException](/zh-CN/api-reference/rtc/android/class-rteexception) 异常。你需要捕获并处理该异常。

- [Player](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_player)

## Player [#api_player_player_player]

构造一个 Player 播放器对象。

<a id="api_player_player_player__prototype"></a>

```java
public Player(Rte rte, PlayerInitialConfig initialConfig)
```

#### `自从` [#api_player_player_player__detailed_desc]

自 v4.4.0 版本新增。

### 调用时机 [#api_player_player_player__timing]

该方法必须在调用 [initMediaEngine](/zh-CN/api-reference/rtc/android/play/rte-player#api_rte_initmediaengine) 之后调用。

### 参数 [#api_player_player_player__parameters]

#### `rte`

一个 [Rte](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_rte) 对象。详见 [Rte](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_rte)。

#### `initialConfig`

播放器对象的初始化设置。你可以传入 `null`。

- [Player](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_player)

## preloadWithUrl [#api_player_preloadwithurl]

预加载 URL 资源。

<a id="api_player_preloadwithurl__prototype"></a>

```java
public static void preloadWithUrl(String url) throws RteException
```

#### `自从` [#api_player_preloadwithurl__detailed_desc]

自 v4.4.0 版本新增。

成功预加载资源可以加快使用 [openWithUrl](/zh-CN/api-reference/rtc/android/play/rte-player#api_player_openwithurl) 打开 URL 资源的速度，从而减少等待时间。

:::info[注]
该方法目前仅适用于以 `rte://` 为前缀的 URL。最多可预加载 20 个 URL，若超过限制，新预加载的 URL 会替换最早的 URL。
:::

### 适用场景 [#api_player_preloadwithurl__scenario]

在打开媒体资源前预加载，可以减少用户等待时间，提供更流畅的视听体验。

### 调用时机 [#api_player_preloadwithurl__timing]

必须在调用 [openWithUrl](/zh-CN/api-reference/rtc/android/play/rte-player#api_player_openwithurl) 之前调用该方法。

### 参数 [#api_player_preloadwithurl__parameters]

#### `url`

以 `rte://` 为前缀的 URL。各字段的详细信息参见观众端的“输入在线媒体流”。

### 异常 [#api_player_preloadwithurl__exception]

如果方法调用失败，SDK 会抛出包含相应错误信息的 [RteException](/zh-CN/api-reference/rtc/android/class-rteexception)。你需要捕获并处理该异常。

- [Player](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_player)

## registerObserver [#api_player_registerobserver_player]

注册播放器的观察者对象。

<a id="api_player_registerobserver_player__prototype"></a>

```java
public void registerObserver(PlayerObserver observer) throws RteException
```

#### `自从` [#api_player_registerobserver_player__detailed_desc]

自 v4.4.0 版本新增。

在调用此方法之前，你需要实现一个继承自 [PlayerObserver](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_playerobserver) 的接口类。

### 调用时机 [#api_player_registerobserver_player__timing]

该方法必须在调用 [Player](/zh-CN/api-reference/rtc/android/play/rte-player#api_player_player_player) 之后执行。

### 参数 [#api_player_registerobserver_player__parameters]

#### `observer`

详见 [PlayerObserver](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_playerobserver)。

### 异常 [#api_player_registerobserver_player__exception]

如果方法调用失败，SDK 会抛出异常，并返回相应的错误信息。你需要捕获异常并进行处理。

- [Player](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_player)

## removeView [#api_canvas_removeview]

移除一个渲染视图。

<a id="api_canvas_removeview__prototype"></a>

```java
public void removeView(View view, ViewConfig viewConfig) throws RteException
```

#### `自从` [#api_canvas_removeview__detailed_desc]

自 v4.4.0 版本新增。

:::info[注]
每次只能移除一个视图。
:::

### 调用时机 [#api_canvas_removeview__timing]

该方法必须在调用 [addView](/zh-CN/api-reference/rtc/android/play/rte-player#api_canvas_addview) 之后调用。

### 参数 [#api_canvas_removeview__parameters]

#### `view`

要移除的视图对象。

#### `viewConfig`

`view` 对象的设置。

### 返回值 [#api_canvas_removeview__return_values]

调用失败时，SDK 会抛出异常，并返回相应的错误信息。你需要捕获异常并进行处理。

- [Canvas](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_canvas)

## Rte [#api_rte_rte_rte]

构造一个 Rte 对象。

<a id="api_rte_rte_rte__prototype"></a>

```java
public Rte(InitialConfig initialConfig)
```

#### `自从` [#api_rte_rte_rte__detailed_desc]

自 v4.4.0 版本新增。

### 调用时机 [#api_rte_rte_rte__timing]

你必须在调用 [initMediaEngine](/zh-CN/api-reference/rtc/android/play/rte-player#api_rte_initmediaengine) 之前调用此方法。

### 参数 [#api_rte_rte_rte__parameters]

#### `initialConfig`

初始化设置。你可以传入 `null`。

- [Rte](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_rte)

## seek [#api_player_seek]

跳转到指定的播放位置。

<a id="api_player_seek__prototype"></a>

```java
public void seek(long newTime) throws RteException
```

#### `自从` [#api_player_seek__detailed_desc]

自 v4.5.1 版本新增。

### 参数 [#api_player_seek__parameters]

#### `newTime`

要跳转到的播放位置，单位为毫秒。

### 异常 [#api_player_seek__exception]

如果方法调用失败，SDK 会抛出一个 [RteException](/zh-CN/api-reference/rtc/android/class-rteexception) 异常，并返回相应的错误信息。你需要捕获异常并进行处理。

- [Player](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_player)

## setAbrFallbackLayer [#api_playerconfig_setabrfallbacklayer]

设置已订阅远端视频流的回退选项。

<a id="api_playerconfig_setabrfallbacklayer__prototype"></a>

```java
public void setAbrFallbackLayer(Constants.AbrFallbackLayer fallbackLayer) throws RteException
```

#### `自从` [#api_playerconfig_setabrfallbacklayer__detailed_desc]

自 v4.4.0 版本新增。

在网络状况不佳时，SDK 会将已订阅远端视频流的分辨率降级到指定的视频质量层。每个质量层对应特定的分辨率和码率。网络恢复后，SDK 会将视频流恢复至原始质量层。

:::info[注]
如果未开启 ABR 功能，视频流的回退质量层只能设置为 `LOW` 或 `AUDIO_ONLY`。
:::

### 适用场景 [#api_playerconfig_setabrfallbacklayer__scenario]

一般场景下，可调用该方法并将回退质量层设置为 `kRteAbrFallbackLow` 或 `kRteAbrFallbackAudioOnly`。当网络较差时，SDK 会回退为视频小流或仅接收音频流。 如果对回退视频流的分辨率有更高要求，可[联系技术支持](https://ticket.shengwang.cn/)开启 ABR 功能。开启后，可自定义每一层的分辨率。SDK 会以你指定的最低质量层的分辨率为下限，并根据网络状况在该范围内动态调整分辨率。

### 调用时机 [#api_playerconfig_setabrfallbacklayer__timing]

必须在调用 `setConfigs` 之前调用该方法。

### 参数 [#api_playerconfig_setabrfallbacklayer__parameters]

#### `fallbackLayer`

视频流的回退质量层，详见 [AbrFallbackLayer](/zh-CN/api-reference/rtc/android/enum-rteabrfallbacklayer)。

:::info[注]
自定义分辨率时，请确保视频质量层按从高到低的分辨率排序；若分辨率相同，则按帧率从高到低排序。

| 视频质量层 | 分辨率 |
| --- | --- |
| `LAYER1` | 2160p (4K) |
| `LAYER2` | 1440p (2K) |
| `LAYER3` | 1080p (HD) |
| `LAYER4` | 720p (HD) |
| `LAYER5` | 540p (SD) |
| `LAYER6` | 480p (SD) |
:::

### 异常 [#api_playerconfig_setabrfallbacklayer__exception]

调用失败时，SDK 会抛出异常，并返回相应的错误信息。你需要捕获异常并进行处理。

- [PlayerConfig](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_playerconfig)

## setAbrSubscriptionLayer [#api_playerconfig_setabrsubscriptionlayer]

设置订阅媒体流的视频质量等级。

<a id="api_playerconfig_setabrsubscriptionlayer__prototype"></a>

```java
public void setAbrSubscriptionLayer(Constants.AbrSubscriptionLayer subscriptionLayer) throws RteException
```

#### `自从` [#api_playerconfig_setabrsubscriptionlayer__detailed_desc]

自 v4.4.0 版本新增。

你可以根据实际网络状况，在所订阅的视频流中切换不同的视频质量等级。如果你需要更高级的视频分辨率切换控制，可以[联系技术支持](https://ticket.shengwang.cn/)开启 ABR（自适应码率）功能。开启后，你可以为每个视频质量等级自定义分辨率，使观众可以根据需要在多质量层级视频流的所有等级之间切换。

:::info[注]
如果未开启 ABR 功能，你只能订阅 `HIGH` 或 `LOW` 视频质量等级。
:::

### 适用场景 [#api_playerconfig_setabrsubscriptionlayer__scenario]

在网络环境不稳定的场景中，订阅端可根据实际网络状况选择合适的视频质量层级，以保障流畅的观看体验。

### 调用时机 [#api_playerconfig_setabrsubscriptionlayer__timing]

该方法必须在调用 `setConfigs` 之前调用。

### 参数 [#api_playerconfig_setabrsubscriptionlayer__parameters]

#### `subscriptionLayer`

要订阅的视频质量等级，详见 [AbrSubscriptionLayer](/zh-CN/api-reference/rtc/android/enum-rteabrsubscriptionlayer)。在自定义 `LAYER1` 到 `LAYER6` 的分辨率时，你可以参考下表，或根据实际需求进行配置： 注意：自定义分辨率时，必须按分辨率从高到低排序各视频质量等级（Layer），若分辨率相同，则按帧率从高到低排序。

| 视频质量等级 | 分辨率 |
| --- | --- |
| `LAYER1` | 2160p (4K) |
| `LAYER2` | 1440p (2K) |
| `LAYER3` | 1080p (HD) |
| `LAYER4` | 720p (HD) |
| `LAYER5` | 540p (SD) |
| `LAYER6` | 480p (SD) |

### 异常 [#api_playerconfig_setabrsubscriptionlayer__exception]

如果方法调用失败，SDK 会抛出包含相应错误信息的 [RteException](/zh-CN/api-reference/rtc/android/class-rteexception) 异常。你需要捕获并处理该异常。

- [PlayerConfig](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_playerconfig)

## setAppId [#api_config_setappid]

设置项目的声网 App ID。

<a id="api_config_setappid__prototype"></a>

```java
public void setAppId(String appId) throws RteException
```

#### `自从` [#api_config_setappid__detailed_desc]

自 v4.4.0 版本新增。

### 调用时机 [#api_config_setappid__timing]

你必须在调用 [initMediaEngine](/zh-CN/api-reference/rtc/android/play/rte-player#api_rte_initmediaengine) 之前调用此方法。

### 参数 [#api_config_setappid__parameters]

#### `appId`

声网 App ID，可从控制台获取。

### 异常 [#api_config_setappid__exception]

调用失败时，SDK 会抛出异常，并返回相应的错误信息。你需要捕获异常并进行处理。

- [Config](/zh-CN/api-reference/rtc/android/class-config)

## setAutoPlay [#api_playerconfig_setautoplay]

设置是否启用自动播放。

<a id="api_playerconfig_setautoplay__prototype"></a>

```java
public void setAutoPlay(boolean autoPlay) throws RteException
```

#### `自从` [#api_playerconfig_setautoplay__detailed_desc]

自 v4.4.0 版本新增。

你可以在调用 [openWithUrl](/zh-CN/api-reference/rtc/android/play/rte-player#api_player_openwithurl) 之前调用此方法设置是否启用自动播放。如果未设置，默认启用自动播放。

### 调用时机 [#api_playerconfig_setautoplay__timing]

此方法必须在调用 [openWithUrl](/zh-CN/api-reference/rtc/android/play/rte-player#api_player_openwithurl) 之前调用。

### 参数 [#api_playerconfig_setautoplay__parameters]

#### `autoPlay`

是否启用自动播放：

- true：（默认）启用自动播放。
- false：关闭自动播放。

### 异常 [#api_playerconfig_setautoplay__exception]

设置失败，SDK 会抛出异常，并返回相应的错误信息。你需要捕获异常并进行处理。

- [PlayerConfig](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_playerconfig)

## setCanvas [#api_player_setcanvas]

设置用于显示视频的视图窗口。

<a id="api_player_setcanvas__prototype"></a>

```java
public void setCanvas(Canvas canvas) throws RteException
```

#### `自从` [#api_player_setcanvas__detailed_desc]

自 v4.4.0 版本新增。

该方法用于设置用于显示视频的 [Canvas](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_canvas) 对象。

### 调用时机 [#api_player_setcanvas__timing]

该方法必须在调用 [Player](/zh-CN/api-reference/rtc/android/play/rte-player#api_player_player_player) 之后调用。

### 参数 [#api_player_setcanvas__parameters]

#### `canvas`

用于渲染视频帧的对象，详见 [Canvas](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_canvas)。

### 异常 [#api_player_setcanvas__exception]

如果方法调用失败，SDK 会抛出异常，并返回相应的错误信息。你需要捕获异常并进行处理。

- [Player](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_player)

## setConfigs [3/3] [#api_canvas_setconfigs_canvas]

设置播放器的视频渲染配置。

<a id="api_canvas_setconfigs_canvas__prototype"></a>

```java
public void setConfigs(CanvasConfig config) throws RteException
```

#### `自从` [#api_canvas_setconfigs_canvas__detailed_desc]

自 v4.4.0 版本新增。

### 调用时机 [#api_canvas_setconfigs_canvas__timing]

该方法需要在调用 [openWithUrl](/zh-CN/api-reference/rtc/android/play/rte-player#api_player_openwithurl) 之前调用。

### 参数 [#api_canvas_setconfigs_canvas__parameters]

#### `config`

用于设置 [Canvas](/zh-CN/api-reference/rtc/android/play/rte-player#api_canvas_canvas_canvas) 对象的配置，详见 [CanvasConfig](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_canvasconfig)。

### 异常 [#api_canvas_setconfigs_canvas__exception]

调用失败时，SDK 会抛出异常，并返回相应的错误信息。你需要捕获异常并进行处理。

- [Canvas](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_canvas)

## setConfigs [2/3] [#api_player_setconfigs_player]

设置播放器配置。

<a id="api_player_setconfigs_player__prototype"></a>

```java
public void setConfigs(PlayerConfig config) throws RteException
```

#### `自从` [#api_player_setconfigs_player__detailed_desc]

自 v4.4.0 版本新增。

你可以调用此方法配置播放器，例如启用自动播放、订阅不同分辨率和码率的视频流。

### 调用时机 [#api_player_setconfigs_player__timing]

此方法必须在调用 [Player](/zh-CN/api-reference/rtc/android/play/rte-player#api_player_player_player) 之后调用。

### 参数 [#api_player_setconfigs_player__parameters]

#### `config`

播放器配置对象，详见 [PlayerConfig](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_playerconfig)。

### 异常 [#api_player_setconfigs_player__exception]

如果方法调用失败，SDK 会抛出包含相应错误信息的 [RteException](/zh-CN/api-reference/rtc/android/class-rteexception)。你需要捕获并处理该异常。

- [Player](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_player)

## setConfigs [1/3] [#api_rte_setconfigs_rte]

配置 RTE 对象的信息。

<a id="api_rte_setconfigs_rte__prototype"></a>

```java
public void setConfigs(Config config) throws RteException
```

#### `自从` [#api_rte_setconfigs_rte__detailed_desc]

自 v4.4.0 版本新增。

### 调用时机 [#api_rte_setconfigs_rte__timing]

该方法必须在调用 [Rte](/zh-CN/api-reference/rtc/android/play/rte-player#api_rte_rte_rte) 之后调用。

### 参数 [#api_rte_setconfigs_rte__parameters]

#### `config`

用于设置 `RTE` 对象的配置信息，详见 [Config](/zh-CN/api-reference/rtc/android/class-config)。

### 异常 [#api_rte_setconfigs_rte__exception]

如果方法调用失败，SDK 会抛出 [RteException](/zh-CN/api-reference/rtc/android/class-rteexception) 并返回相应的错误信息。你需要捕获并处理该异常。

- [Rte](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_rte)

## setJsonParameter [#api_config_setjsonparameter_config]

通过 JSON 配置 SDK 的技术预览或定制化功能。

<a id="api_config_setjsonparameter_config__prototype"></a>

```java
public void setJsonParameter(String jsonParameter) throws RteException
```

#### `自从` [#api_config_setjsonparameter_config__detailed_desc]

自 v4.4.0 版本新增。

### 适用场景 [#api_config_setjsonparameter_config__scenario]

当你需要设置私有参数或使用定制化功能时，可以调用此方法。

### 调用时机 [#api_config_setjsonparameter_config__timing]

此方法必须在调用 `setConfigs` 之前调用。

### 参数 [#api_config_setjsonparameter_config__parameters]

#### `jsonParameter`

JSON 字符串格式的参数。

### 异常 [#api_config_setjsonparameter_config__exception]

如果方法调用失败，SDK 会抛出包含相应错误信息的 [RteException](/zh-CN/api-reference/rtc/android/class-rteexception)。你需要捕获并处理该异常。

- [Config](/zh-CN/api-reference/rtc/android/class-config)

## setLoopCount [#api_playerconfig_setloopcount]

设置媒体文件在播放期间循环播放的次数。

<a id="api_playerconfig_setloopcount__prototype"></a>

```java
public void setLoopCount(int count) throws RteException
```

#### `自从` [#api_playerconfig_setloopcount__detailed_desc]

自 v4.5.1 版本新增。

### 参数 [#api_playerconfig_setloopcount__parameters]

#### `count`

媒体文件循环播放的次数：

- `1`：播放一次。
- `2`：播放两次。
- `-1`：无限循环播放，直到调用 [stop](/zh-CN/api-reference/rtc/android/play/rte-player#api_player_stop_player) 停止播放。

### 异常 [#api_playerconfig_setloopcount__exception]

设置失败，SDK 会抛出异常，并返回相应的错误信息。你需要捕获异常并进行处理。

- [PlayerConfig](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_playerconfig)

## setVideoMirrorMode [#api_canvasconfig_setmirrormode]

设置视频镜像模式。

<a id="api_canvasconfig_setmirrormode__prototype"></a>

```java
public void setVideoMirrorMode(Constants.VideoMirrorMode mode) throws RteException
```

#### `自从` [#api_canvasconfig_setmirrormode__detailed_desc]

自 v4.4.0 版本新增。

你必须在调用 `setConfigs` 方法之前调用此方法。

### 调用时机 [#api_canvasconfig_setmirrormode__timing]

你必须在调用 `setConfigs` 之前调用此方法。

### 参数 [#api_canvasconfig_setmirrormode__parameters]

#### `mode`

镜像模式，详见 [VideoMirrorMode](/zh-CN/api-reference/rtc/android/enum-rtevideomirrormode)。默认值为 `AUTO`，由 SDK 自动决定镜像模式。

### 异常 [#api_canvasconfig_setmirrormode__exception]

方法调用失败时，SDK 会抛出异常，并返回相应的错误信息。你需要捕获异常并进行处理。

- [CanvasConfig](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_canvasconfig)

## setPlaybackSpeed [#api_playerconfig_setplaybackspeed]

设置播放速度参数。

<a id="api_playerconfig_setplaybackspeed__prototype"></a>

```java
public void setPlaybackSpeed(int speed) throws RteException
```

#### `自从` [#api_playerconfig_setplaybackspeed__detailed_desc]

自 v4.5.1 版本新增。

你可以在调用 [openWithUrl](/zh-CN/api-reference/rtc/android/play/rte-player#api_player_openwithurl) 后调用该方法。

### 参数 [#api_playerconfig_setplaybackspeed__parameters]

#### `speed`

播放速度。有效范围为 [50, 400]。

### 异常 [#api_playerconfig_setplaybackspeed__exception]

设置播放速度失败，SDK 会抛出异常，并返回相应的错误信息。你需要捕获异常并进行处理。

- [PlayerConfig](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_playerconfig)

## setPlayoutVolume [#api_playerconfig_setplayoutvolume]

设置播放音量参数。

<a id="api_playerconfig_setplayoutvolume__prototype"></a>

```java
public void setPlayoutVolume(int volume) throws RteException
```

#### `自从` [#api_playerconfig_setplayoutvolume__detailed_desc]

自 v4.5.1 版本新增。

### 参数 [#api_playerconfig_setplayoutvolume__parameters]

#### `volume`

播放器的音量值。有效范围为 [0, 400]。

### 异常 [#api_playerconfig_setplayoutvolume__exception]

设置播放音量失败，SDK 会抛出异常，并返回相应的错误信息。你需要捕获异常并进行处理。

- [PlayerConfig](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_playerconfig)

## setVideoRenderMode [#api_canvasconfig_setrendermode_canvasconfig]

设置视频渲染模式。

<a id="api_canvasconfig_setrendermode_canvasconfig__prototype"></a>

```java
public void setVideoRenderMode(Constants.VideoRenderMode mode) throws RteException
```

#### `自从` [#api_canvasconfig_setrendermode_canvasconfig__detailed_desc]

自 v4.4.0 版本新增。

### 参数 [#api_canvasconfig_setrendermode_canvasconfig__parameters]

#### `mode`

要设置的视频渲染模式，详见 [VideoRenderMode](/zh-CN/api-reference/rtc/android/enum-rtevideorendermode)。

:::info[注]
默认值为 `VideoRenderMode.HIDDEN`。
:::

### 异常 [#api_canvasconfig_setrendermode_canvasconfig__exception]

[RteException](/zh-CN/api-reference/rtc/android/class-rteexception)： `RteException.errorCode()` 可能返回以下错误码：

- `ErrorCode.INVALID_ARGUMENT`： `mode` 参数设置为非法值。

- [CanvasConfig](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_canvasconfig)

## state [#api_rteplayerinfo_state]

获取当前播放器状态。

<a id="api_rteplayerinfo_state__prototype"></a>

```java
public int state()
```

### <span id="api_rteplayerinfo_state__detailed_desc"></span>返回值 [#api_rteplayerinfo_state__return_values]

方法调用成功，返回当前播放器的状态。

- [PlayerInfo](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_rteplayerinfo)

## stop [#api_player_stop_player]

停止播放媒体资源。

<a id="api_player_stop_player__prototype"></a>

```java
public void stop() throws RteException
```

#### `自从` [#api_player_stop_player__detailed_desc]

自 v4.4.0 版本新增。

如果使用 [openWithUrl](/zh-CN/api-reference/rtc/android/play/rte-player#api_player_openwithurl) 打开 URL 流失败，你需要先调用该方法，然后再调用 [openWithUrl](/zh-CN/api-reference/rtc/android/play/rte-player#api_player_openwithurl) 重新打开 URL。

### 调用时机 [#api_player_stop_player__timing]

该方法必须在调用 [openWithUrl](/zh-CN/api-reference/rtc/android/play/rte-player#api_player_openwithurl) 之后调用。

### 相关回调 [#api_player_stop_player__related]

调用成功后，该方法会触发 [onStateChanged](/zh-CN/api-reference/rtc/android/play/rte-player#callback_playerobserver_onstatechanged) 回调，并上报播放器状态为 `STOPPED`。

### 异常 [#api_player_stop_player__exception]

如果方法调用失败，SDK 会抛出异常，并返回相应的错误信息。你需要捕获异常并进行处理。

- [Player](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_player)

## streamCount [#api_rteplayerinfo_streamcount]

获取当前播放源中的流的数量。

<a id="api_rteplayerinfo_streamcount__prototype"></a>

```java
public int streamCount()
```

<a id="api_rteplayerinfo_streamcount__detailed_desc"></a>

该方法仅在打开非 RTE URL 时有效。

### 返回值 [#api_rteplayerinfo_streamcount__return_values]

方法调用成功，返回流的数量。

- [PlayerInfo](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_rteplayerinfo)

## switchWithUrl [#api_player_switchwithurl]

在播放过程中切换到新的 URL。

<a id="api_player_switchwithurl__prototype"></a>

```java
public void switchWithUrl(String url, boolean syncPts, AsyncCallback callback)
```

#### `自从` [#api_player_switchwithurl__detailed_desc]

自 v4.5.1 版本新增。

当 SDK 返回播放器状态为 `OPEN_COMPLETED` 时，必须调用该方法。

:::info[注]
该方法仅在播放器打开非 RTE URL 时有效。
:::

### 参数 [#api_player_switchwithurl__parameters]

#### `url`

要切换的 URL 资源。

#### `syncPts`

- true：同步播放位置。
- false：（默认）不同步播放位置。

#### `callback`

异步通知切换操作结果的回调。你可以通过该回调的 `err` 参数得知操作的结果或错误码信息，详见 [AsyncCallback](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_asynccallback)。

- [Player](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_player)

## unregisterObserver [#api_player_unregisterobserver_player]

取消注册媒体播放器事件观察者对象。

<a id="api_player_unregisterobserver_player__prototype"></a>

```java
public void unregisterObserver(PlayerObserver observer) throws RteException
```

#### `自从` [#api_player_unregisterobserver_player__detailed_desc]

自 v4.4.0 版本新增。

调用 [registerObserver](/zh-CN/api-reference/rtc/android/play/rte-player#api_player_registerobserver_player) 注册媒体播放器事件观察者对象后，如需取消注册，请调用该方法。

### 调用时机 [#api_player_unregisterobserver_player__timing]

该方法必须在调用 [registerObserver](/zh-CN/api-reference/rtc/android/play/rte-player#api_player_registerobserver_player) 之后调用。

### 参数 [#api_player_unregisterobserver_player__parameters]

#### `observer`

详见 [PlayerObserver](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_playerobserver)。

### 异常 [#api_player_unregisterobserver_player__exception]

如果方法调用失败，SDK 会抛出异常，并返回相应的错误信息。你需要捕获异常并进行处理。

- [Player](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_player)

## videoHeight [#api_rteplayerinfo_videoheight]

获取视频分辨率的高度。

<a id="api_rteplayerinfo_videoheight__prototype"></a>

```java
public int videoHeight()
```

#### `自从` [#api_rteplayerinfo_videoheight__detailed_desc]

自 v4.5.1 版本新增。

### 返回值 [#api_rteplayerinfo_videoheight__return_values]

方法调用成功，返回视频分辨率的高度，单位为像素。

- [PlayerInfo](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_rteplayerinfo)

## videoWidth [#api_rteplayerinfo_videowidth]

获取视频分辨率的宽度。

<a id="api_rteplayerinfo_videowidth__prototype"></a>

```java
public int videoWidth()
```

#### `自从` [#api_rteplayerinfo_videowidth__detailed_desc]

自 v4.5.1 版本新增。

### 返回值 [#api_rteplayerinfo_videowidth__return_values]

方法调用成功，返回视频分辨率的宽度，单位为像素。

- [PlayerInfo](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_rteplayerinfo)

## onAudioVolumeIndication [#callback_playerobserver_onaudiovolumeindication_playerobserver]

播放器音量变化回调。

<a id="callback_playerobserver_onaudiovolumeindication_playerobserver__prototype"></a>

```java
public void onAudioVolumeIndication(int volume)
```

#### `自从` [#callback_playerobserver_onaudiovolumeindication_playerobserver__detailed_desc]

自 v4.4.0 版本新增。

### 触发时机 [#callback_playerobserver_onaudiovolumeindication_playerobserver__timing]

该回调在播放器音量发生变化时被触发。

### 参数 [#callback_playerobserver_onaudiovolumeindication_playerobserver__parameters]

#### `volume`

播放器当前的音量，取值范围为 [0, 225]。

- [PlayerObserver](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_playerobserver)

## onEvent [#callback_playerobserver_onevent]

播放器事件回调。

<a id="callback_playerobserver_onevent__prototype"></a>

```java
public void onEvent(int event)
```

#### `自从` [#callback_playerobserver_onevent__detailed_desc]

自 v4.4.0 版本新增。

如果你需要监听播放器事件，必须先调用 [registerObserver](/zh-CN/api-reference/rtc/android/play/rte-player#api_player_registerobserver_player) 注册播放器观察者对象。

### 触发时机 [#callback_playerobserver_onevent__timing]

该回调在播放器事件发生变化时被触发。

### 参数 [#callback_playerobserver_onevent__parameters]

#### `event`

播放器事件。

:::info[注]
详见 [PlayerEvent](/zh-CN/api-reference/rtc/android/enum-rteplayerevent)。
:::

- [PlayerObserver](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_playerobserver)

## onMetadata [2/2] [#callback_playerobserver_onmetadata_playerobserver]

接收媒体流附加信息的回调。

<a id="callback_playerobserver_onmetadata_playerobserver__prototype"></a>

```java
public void onMetadata(int type, byte[] data)
```

#### `自从` [#callback_playerobserver_onmetadata_playerobserver__detailed_desc]

自 v4.4.0 版本新增。

调用 [registerObserver](/zh-CN/api-reference/rtc/android/play/rte-player#api_player_registerobserver_player) 注册播放器观察者对象后，才能接收媒体流中的附加信息。

### 触发时机 [#callback_playerobserver_onmetadata_playerobserver__timing]

该回调在 SDK 解析播放器正在播放的媒体流中的附加信息后触发，报告数据类型及其具体内容。

### 参数 [#callback_playerobserver_onmetadata_playerobserver__parameters]

#### `type`

附加信息的类型：

- 0：SEI（Supplemental Enhancement Information）类型。

#### `data`

解析后的附加信息。

- [PlayerObserver](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_playerobserver)

## onPlayerInfoUpdated [2/2] [#callback_playerobserver_onplayerinfoupdated_playerobserver]

播放器和媒体流信息发生变化回调。

<a id="callback_playerobserver_onplayerinfoupdated_playerobserver__prototype"></a>

```java
public void onPlayerInfoUpdated(PlayerInfo info)
```

#### `自从` [#callback_playerobserver_onplayerinfoupdated_playerobserver__detailed_desc]

自 v4.4.0 版本新增。

你可以调用 [registerObserver](/zh-CN/api-reference/rtc/android/play/rte-player#api_player_registerobserver_player) 注册播放器观察者对象来获取播放器和媒体流的信息，也可以直接调用 [getInfo](/zh-CN/api-reference/rtc/android/play/rte-player#api_player_getinfo) 方法获取信息。

### 触发时机 [#callback_playerobserver_onplayerinfoupdated_playerobserver__timing]

该回调在播放器和媒体流信息发生变化时被触发。

### 参数 [#callback_playerobserver_onplayerinfoupdated_playerobserver__parameters]

#### `info`

播放器及媒体流的相关信息，详见 [PlayerInfo](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_rteplayerinfo)。

- [PlayerObserver](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_playerobserver)

## onPositionChanged [#callback_playerobserver_onpositionchanged_playerobserver]

播放进度变化回调。

<a id="callback_playerobserver_onpositionchanged_playerobserver__prototype"></a>

```java
public void onPositionChanged(long curr_time, long utc_time)
```

#### `自从` [#callback_playerobserver_onpositionchanged_playerobserver__detailed_desc]

自 v4.4.0 版本新增。

### 触发时机 [#callback_playerobserver_onpositionchanged_playerobserver__timing]

该回调在媒体资源播放过程中每秒触发一次。

### 参数 [#callback_playerobserver_onpositionchanged_playerobserver__parameters]

#### `curr_time`

当前播放进度，单位为毫秒。

#### `utc_time`

当前 NTP（网络时间协议）时间，单位为毫秒。

- [PlayerObserver](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_playerobserver)

## onResolutionChanged [#callback_playerobserver_onresolutionchanged]

视频分辨率变化的回调。

<a id="callback_playerobserver_onresolutionchanged__prototype"></a>

```java
public void onResolutionChanged(int width, int height)
```

#### `自从` [#callback_playerobserver_onresolutionchanged__detailed_desc]

自 v4.4.0 版本新增。

你必须先调用 [registerObserver](/zh-CN/api-reference/rtc/android/play/rte-player#api_player_registerobserver_player) 注册一个播放器观察者对象，才能监听视频流分辨率的变化。

### 触发时机 [#callback_playerobserver_onresolutionchanged__timing]

该回调在视频流的分辨率发生变化时被触发，报告当前视频的宽度和高度。

### 参数 [#callback_playerobserver_onresolutionchanged__parameters]

#### `width`

视频帧的宽度，单位为 px。

#### `height`

视频帧的高度，单位为 px。

- [PlayerObserver](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_playerobserver)

## onResult [#callback_asynccallback_onresult_asynccallback]

onResult 回调，用于报告异步操作的结果。

<a id="callback_asynccallback_onresult_asynccallback__prototype"></a>

```java
@CalledByNative void onResult(Error error);
```

#### `自从` [#callback_asynccallback_onresult_asynccallback__detailed_desc]

自 v4.4.0 版本新增。

### 触发时机 [#callback_asynccallback_onresult_asynccallback__timing]

该回调在异步操作完成时被触发，用于报告操作结果。

### 参数 [#callback_asynccallback_onresult_asynccallback__parameters]

#### `error`

状态码或错误码。详见 [Error](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_error)。

- [AsyncCallback](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_asynccallback)

## onResult [#callback_playergetstatscallback_onresult_playergetstatscallback]

异步获取媒体资源统计信息操作完成时触发的回调。

<a id="callback_playergetstatscallback_onresult_playergetstatscallback__prototype"></a>

```java
@CalledByNative void onResult(PlayerStats playerStats, Error error);
```

### <span id="callback_playergetstatscallback_onresult_playergetstatscallback__detailed_desc"></span>触发时机 [#callback_playergetstatscallback_onresult_playergetstatscallback__timing]

该回调在异步获取媒体资源信息的操作完成时被触发。

### 参数 [#callback_playergetstatscallback_onresult_playergetstatscallback__parameters]

#### `playerStats`

媒体资源的统计信息。详见 [PlayerStats](/zh-CN/api-reference/rtc/android/class-rteplayerstats)。

#### `error`

状态和错误信息。详见 [Error](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_error)。

- [PlayerGetStatsCallback](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_playergetstatscallback)

## onStateChanged [#callback_playerobserver_onstatechanged]

播放器状态变化的回调。

<a id="callback_playerobserver_onstatechanged__prototype"></a>

```java
public void onStateChanged(int old_state, int new_state, Error error)
```

#### `自从` [#callback_playerobserver_onstatechanged__detailed_desc]

自 v4.4.0 版本新增。

如果你需要监听播放器状态的变化，必须先调用 [registerObserver](/zh-CN/api-reference/rtc/android/play/rte-player#api_player_registerobserver_player) 注册播放器观察者对象。

### 触发时机 [#callback_playerobserver_onstatechanged__timing]

该回调在播放器状态发生变化时被触发。

### 参数 [#callback_playerobserver_onstatechanged__parameters]

#### `old_state`

变更前的播放器状态。详见 [PlayerState](/zh-CN/api-reference/rtc/android/enum-rteplayerstate)。

#### `new_state`

当前的播放器状态。详见 [PlayerState](/zh-CN/api-reference/rtc/android/enum-rteplayerstate)。如果当前状态为 `FAILED`，你可以通过 `error` 参数获取详细的错误信息。

#### `error`

状态或错误信息。详见 [Error](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_error)。

- [PlayerObserver](/zh-CN/api-reference/rtc/android/rtc-interface-class#class_playerobserver)
