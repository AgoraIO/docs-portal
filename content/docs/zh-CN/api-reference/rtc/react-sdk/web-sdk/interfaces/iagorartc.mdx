---
title: Interface IAgoraRTC
description: Agora Web SDK 的入口。
_migration:
  type: generated-html
  status: migrated
  sourceUrl: https://doc.shengwang.cn/api-ref/rtc/react/interfaces/iagorartc
  sourcePath: html-docs/rtc/React/interfaces/iagorartc.html
  generator: typedoc
  warnings:
    - code: source-only-link-removed
      severity: info
      message: >-
        Rendered unresolved generated-source link /api-ref/rtc/react/classes/eventemitter.html as
        local text.
    - code: source-only-link-removed
      severity: info
      message: >-
        Rendered unresolved generated-source link /api-ref/rtc/react/classes/eventemitter.html as
        local text.
    - code: source-only-link-removed
      severity: info
      message: >-
        Rendered unresolved generated-source link
        /api-ref/rtc/react/classes/eventemitter.html#getlisteners as local text.
    - code: source-only-link-removed
      severity: info
      message: >-
        Rendered unresolved generated-source link /api-ref/rtc/react/classes/eventemitter.html as
        local text.
    - code: source-only-link-removed
      severity: info
      message: >-
        Rendered unresolved generated-source link /api-ref/rtc/react/classes/eventemitter.html#off
        as local text.
    - code: source-only-link-removed
      severity: info
      message: >-
        Rendered unresolved generated-source link /api-ref/rtc/react/classes/eventemitter.html as
        local text.
    - code: source-only-link-removed
      severity: info
      message: >-
        Rendered unresolved generated-source link /api-ref/rtc/react/classes/eventemitter.html#on as
        local text.
    - code: source-only-link-removed
      severity: info
      message: >-
        Rendered unresolved generated-source link /api-ref/rtc/react/classes/eventemitter.html as
        local text.
    - code: source-only-link-removed
      severity: info
      message: >-
        Rendered unresolved generated-source link /api-ref/rtc/react/classes/eventemitter.html#on as
        local text.
    - code: source-only-link-removed
      severity: info
      message: >-
        Rendered unresolved generated-source link /api-ref/rtc/react/classes/eventemitter.html as
        local text.
    - code: source-only-link-removed
      severity: info
      message: >-
        Rendered unresolved generated-source link /api-ref/rtc/react/classes/eventemitter.html#on as
        local text.
    - code: source-only-link-removed
      severity: info
      message: >-
        Rendered unresolved generated-source link /api-ref/rtc/react/classes/eventemitter.html as
        local text.
    - code: source-only-link-removed
      severity: info
      message: >-
        Rendered unresolved generated-source link /api-ref/rtc/react/classes/eventemitter.html#on as
        local text.
    - code: source-only-link-removed
      severity: info
      message: >-
        Rendered unresolved generated-source link /api-ref/rtc/react/classes/eventemitter.html as
        local text.
    - code: source-only-link-removed
      severity: info
      message: >-
        Rendered unresolved generated-source link /api-ref/rtc/react/classes/eventemitter.html#on as
        local text.
    - code: source-only-link-removed
      severity: info
      message: >-
        Rendered unresolved generated-source link /api-ref/rtc/react/classes/eventemitter.html as
        local text.
    - code: source-only-link-removed
      severity: info
      message: >-
        Rendered unresolved generated-source link /api-ref/rtc/react/classes/eventemitter.html#on as
        local text.
    - code: source-only-link-removed
      severity: info
      message: >-
        Rendered unresolved generated-source link /api-ref/rtc/react/classes/eventemitter.html as
        local text.
    - code: source-only-link-removed
      severity: info
      message: >-
        Rendered unresolved generated-source link /api-ref/rtc/react/classes/eventemitter.html#once
        as local text.
    - code: source-only-link-removed
      severity: info
      message: >-
        Rendered unresolved generated-source link
        /doc/rtc/javascript/advanced-features/virtual-background as local text.
    - code: source-only-link-removed
      severity: info
      message: >-
        Rendered unresolved generated-source link
        /doc/rtc/javascript/advanced-features/noise-reduction as local text.
    - code: source-only-link-removed
      severity: info
      message: >-
        Rendered unresolved generated-source link
        /doc/rtc/javascript/advanced-features/image-enhancement as local text.
    - code: source-only-link-removed
      severity: info
      message: >-
        Rendered unresolved generated-source link /api-ref/rtc/react/classes/eventemitter.html as
        local text.
    - code: source-only-link-removed
      severity: info
      message: >-
        Rendered unresolved generated-source link
        /api-ref/rtc/react/classes/eventemitter.html#removealllisteners as local text.
---

### Hierarchy

- EventEmitter

  - IAgoraRTC

## Index

### Events

- [audio-context-state-changed](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#event_audio_context_state_changed)
- [autoplay-failed](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#event_autoplay_failed)
- [camera-changed](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#event_camera_changed)
- [microphone-changed](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#event_microphone_changed)
- [playback-device-changed](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#event_playback_device_changed)
- [security-policy-violation](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#event_security_policy_violation)

### Global Callback Properties

- [onAudioAutoplayFailed](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#onaudioautoplayfailed)
- [onAutoplayFailed](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#onautoplayfailed)
- [onCameraChanged](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#oncamerachanged)
- [onMicrophoneChanged](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#onmicrophonechanged)
- [onPlaybackDeviceChanged](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#onplaybackdevicechanged)
- [onSecurityPolicyViolation](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#onsecuritypolicyviolation)

### Other Properties

- [VERSION](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#version)

### Agora Core Methods

- [createClient](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#createclient)

### Local Track Methods

- [createBufferSourceAudioTrack](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#createbuffersourceaudiotrack)
- [createCameraVideoTrack](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#createcameravideotrack)
- [createCustomAudioTrack](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#createcustomaudiotrack)
- [createCustomVideoTrack](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#createcustomvideotrack)
- [createMicrophoneAndCameraTracks](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#createmicrophoneandcameratracks)
- [createMicrophoneAudioTrack](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#createmicrophoneaudiotrack)
- [createScreenVideoTrack](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#createscreenvideotrack)

### Logger Methods

- [disableLogUpload](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#disablelogupload)
- [enableLogUpload](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#enablelogupload)
- [setLogLevel](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#setloglevel)

### Media Devices Methods

- [getCameras](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#getcameras)
- [getDevices](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#getdevices)
- [getElectronScreenSources](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#getelectronscreensources)
- [getMicrophones](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#getmicrophones)
- [getPlaybackDevices](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#getplaybackdevices)

### Other Methods

- [checkAudioTrackIsActive](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#checkaudiotrackisactive)
- [checkSystemRequirements](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#checksystemrequirements)
- [checkVideoTrackIsActive](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#checkvideotrackisactive)
- [createChannelMediaRelayConfiguration](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#createchannelmediarelayconfiguration)
- [getListeners](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#getlisteners)
- [getSupportedCodec](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#getsupportedcodec)
- [off](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#off)
- [on](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#on)
- [once](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#once)
- [preload](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#preload)
- [processExternalMediaAEC](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#processexternalmediaaec)
- [registerExtensions](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#registerextensions)
- [removeAllListeners](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#removealllisteners)
- [resumeAudioContext](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#resumeaudiocontext)
- [setArea](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#setarea)

## Events

<a id="event_audio_context_state_changed"></a>

### audio-context-state-changed

- audio-context-state-changed(currState:AudioContextState|"interrupted", prevState:AudioContextState|"interrupted"|undefined):void

:::info[自从]
*4.20.0*

音频上下文 ([Audio Context](https://developer.mozilla.org/en-US/docs/Web/API/AudioContext)) 状态发生改变回调。该回调中的 `"interrupted"` 状态当前只会在 iOS 上触发。
:::

Parameters

- currState: AudioContextState|"interrupted"

  当前的状态，取值如下：

  - `AudioContextState`：枚举值的含义详见 [BaseAudioContext.state](https://developer.mozilla.org/en-US/docs/Web/API/BaseAudioContext/state)。
  - `"interrupted"`：App 的音视频播放被微信通话或系统电话打断。你可以调用 [resumeAudioContext](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#resumeaudiocontext) 方法恢复音视频播放。
- prevState: AudioContextState|"interrupted"|undefined

  之前的状态，取值如下：

  - `AudioContextState`：枚举值的含义详见 [BaseAudioContext.state](https://developer.mozilla.org/en-US/docs/Web/API/BaseAudioContext/state)。
  - `"interrupted"`：App 的音视频播放被微信通话或系统电话打断。
  - `undefined`：无先前状态。

Returns void

<a id="event_autoplay_failed"></a>

### autoplay-failed

- autoplay-failed():void

:::info[自从]
*4.18.0*

如果你需要更灵活地监听自动播放失败事件，推荐通过在 [IAgoraRTC.on](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#on) 方法中传入该回调来代替 [onAutoplayFailed](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#onautoplayfailed)。

```javascript
AgoraRTC.on("autoplay-failed", (info) => {
  console.log("Autoplay failed!", info.state, info.device);
});
```
:::

Returns void

<a id="event_camera_changed"></a>

### camera-changed

- camera-changed(deviceInfo:[DeviceInfo](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/deviceinfo)):void

:::info[自从]
*4.18.0*

如果你需要更灵活地监听视频采集设备状态变化，推荐通过在 [IAgoraRTC.on](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#on) 方法中传入该回调来代替 [onCameraChanged](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#oncamerachanged)。

```javascript
AgoraRTC.on("camera-changed", (info) => {
  console.log("Camera changed!", info.state, info.device);
});
```
:::

Parameters

- deviceInfo: [DeviceInfo](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/deviceinfo)

  设备信息，详见 [DeviceInfo](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/deviceinfo)。

Returns void

<a id="event_microphone_changed"></a>

### microphone-changed

- microphone-changed(deviceInfo:[DeviceInfo](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/deviceinfo)):void

:::info[自从]
*4.18.0*

如果你需要更灵活地监听麦克风设备变化，推荐通过在 [IAgoraRTC.on](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#on) 方法中传入该回调来代替 [onMicrophoneChanged](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#onmicrophonechanged)。

```javascript
AgoraRTC.on("microphone-changed", (info) => {
  console.log("Microphone changed!", info.state, info.device);
});
```
:::

Parameters

- deviceInfo: [DeviceInfo](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/deviceinfo)

  设备信息，详见 [DeviceInfo](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/deviceinfo)。

Returns void

<a id="event_playback_device_changed"></a>

### playback-device-changed

- playback-device-changed(deviceInfo:[DeviceInfo](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/deviceinfo)):void

:::info[自从]
*4.18.0*

如果你需要更灵活地监听音频播放设备变化，推荐通过在 [IAgoraRTC.on](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#on) 方法中传入该回调来代替 [onPlaybackDeviceChanged](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#onplaybackdevicechanged)。

```javascript
AgoraRTC.on("playback-device-changed", (info) => {
  console.log("Playback device changed!", info.state, info.device);
});
```
:::

Parameters

- deviceInfo: [DeviceInfo](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/deviceinfo)

  设备信息，详见 [DeviceInfo](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/deviceinfo)。

Returns void

<a id="event_security_policy_violation"></a>

### security-policy-violation

- security-policy-violation():void

:::info[自从]
*4.18.0*

如果你需要更灵活地监听触发 CSP 规则事件，推荐通过在 [IAgoraRTC.on](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#on) 方法中传入该回调来代替 [onSecurityPolicyViolation](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#onsecuritypolicyviolation)。

```javascript
AgoraRTC.on("security-policy-violation", (info) => {
  console.log("Security policy violation!", info.state, info.device);
});
```
:::

Returns void

## Global Callback Properties

<a id="onaudioautoplayfailed"></a>

### Optional onAudioAutoplayFailed

onAudioAutoplayFailed: function

音频轨道自动播放失败回调。

### deprecated

自 4.6.0 起废弃，请改用 [onAutoplayFailed](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#onautoplayfailed)。

该回调在音频轨道自动播放失败的时候触发。如果短时间内有多个音频轨道对象都调用了 `play`，SDK 会触发多次 `onAudioAutoplayFailed`。

音频自动播放失败是浏览器的[自动播放策略](https://developer.mozilla.org/en-US/docs/Web/Media/Autoplay_guide#Autoplay_and_autoplay_blocking)导致的，视频轨道的播放不受此影响。

在 Agora Web SDK 中，只要用户和页面发生过一次点击交互，就会自动解锁浏览器音频自动播放的限制，所以针对音频自动播放有两种解决方案：

- 如果不希望收到 `onAudioAutoplayFailed` 回调，就确保在调用 `RemoteAudioTrack.play` 和 `LocalAudioTrack.play` 之前用户已经和页面发生了点击交互。
- 如果无法保证在调用 `RemoteAudioTrack.play` 和 `LocalAudioTrack.play` 之前用户已经和页面发生点击交互，就监听 `onAudioAutoplayFailed` 回调，通过回调在页面上显示一个按钮引导用户点击。

> 无论使用何种方案，只要浏览器启用了自动播放策略，都需要用户至少触发一次点击交互操作才能播放音频。随着用户使用某个页面的次数变多，浏览器会在这个页面上默认关闭自动播放策略，此时不需要任何交互也可以播放音频了，但是我们无法通过 JavaScript 去感知浏览器这个行为。

下面的示例代码演示了当检测到自动播放失败后的处理：在页面上动态显示一个按钮让用户点击。

```javascript
 let isAudioAutoplayFailed = false;
 AgoraRTC.onAudioAutoplayFailed = () => {
  if (isAudioAutoplayFailed) return;

  isAudioAutoplayFailed = true;
  const btn = document.createElement("button");
  btn.innerText = "Click me to resume the audio playback";
  btn.onClick = () => {
    isAudioAutoplayFailed = false;
    btn.remove();
  };
  document.body.append(btn);
};
```

> 需要注意的是，短时间内可能会有多个音频轨道对象都调用了 `play`，此时会触发多次 `onAudioAutoplayFailed`。示例代码中通过 `isAudioAutoplayFailed` 这个对象防止创建重复的按钮对象。

#### Type declaration

- - ():void

  - Returns void

<a id="onautoplayfailed"></a>

### Optional onAutoplayFailed

onAutoplayFailed: function

音频或视频轨道自动播放失败回调。

:::info[自从]
*4.6.0*

SDK 在音频或视频轨道自动播放失败时触发该回调。

- 音频自动播放失败是由浏览器的[自动播放策略](https://developer.mozilla.org/en-US/docs/Web/Media/Autoplay_guide#Autoplay_and_autoplay_blocking)导致的。
- 在大部分浏览器中，纯视频不受到自动播放策略的限制，但是在低电量模式下的 iOS Safari 浏览器中以及开启自定义自动播放限制的 iOS `WKWebView` 中（如 iOS 微信浏览器），纯视频的自动播放也会受到限制。

在 Agora Web SDK 中，只要用户和页面发生过一次点击交互，就会自动解锁浏览器音频或视频的自动播放限制，所以针对自动播放有两种解决方案：

- 如果不希望收到 `onAutoplayFailed` 回调，就确保在调用 `RemoteTrack.play` 和 `LocalTrack.play` 之前用户已经和页面发生了点击交互。
- 如果无法保证在调用 `RemoteTrack.play` 和 `LocalTrack.play` 之前用户已经和页面发生点击交互，就监听 `onAutoplayFailed` 回调，通过回调在页面上显示一个按钮引导用户点击。

> 无论使用何种方案，只要浏览器启用了自动播放策略，都需要用户至少触发一次点击交互操作才能播放音频或视频。随着用户使用某个页面的次数变多，浏览器会在这个页面上默认关闭自动播放策略，此时不需要任何交互也可以播放音频和视频了，但是我们无法通过 JavaScript 去感知浏览器这个行为。

下面的示例代码演示了当检测到自动播放失败后的处理：在页面上动态显示一个按钮让用户点击。

```javascript
 AgoraRTC.onAutoplayFailed = () => {
  const btn = document.createElement("button");
  btn.innerText = "Click me to resume the audio/video playback";
  btn.onClick = () => {
    btn.remove();
  };
  document.body.append(btn);
```

> 由于自动播放失败后在用户通过手势点击恢复播放之前，`onAutoplayFailed` 只会触发一次，因此在业务代码中不需要像 `onAudioAutoplayFailed` 回调一样维护 `isAutoplayFailed` 状态。
:::

#### Type declaration

- - ():void

  - Returns void

<a id="oncamerachanged"></a>

### Optional onCameraChanged

onCameraChanged: function

视频采集设备状态变化回调。

该回调提示有摄像头被添加或移除。

> 注意事项：当该回调提示有设备被添加时，如果该设备是虚拟设备，可能无法采集到音频或视频。

```javascript
AgoraRTC.onCameraChanged = (info) => {
  console.log("camera changed!", info.state, info.device);
};
```

**Parameters**

- **deviceInfo**：设备信息，详见 [DeviceInfo](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/deviceinfo)。

#### Type declaration

- - (deviceInfo:[DeviceInfo](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/deviceinfo)):void

  - Parameters

    - deviceInfo: [DeviceInfo](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/deviceinfo)

    Returns void

<a id="onmicrophonechanged"></a>

### Optional onMicrophoneChanged

onMicrophoneChanged: function

音频采集设备状态变化回调。

该回调提示有麦克风被添加或移除。

> 注意事项：当该回调提示有设备被添加时，如果该设备是虚拟设备，可能无法采集到音频或视频。

```javascript
AgoraRTC.onMicrophoneChanged = (info) => {
  console.log("microphone changed!", info.state, info.device);
};
```

**Parameters**

- **deviceInfo**：设备信息，详见 [DeviceInfo](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/deviceinfo)。

#### Type declaration

- - (deviceInfo:[DeviceInfo](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/deviceinfo)):void

  - Parameters

    - deviceInfo: [DeviceInfo](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/deviceinfo)

    Returns void

<a id="onplaybackdevicechanged"></a>

### Optional onPlaybackDeviceChanged

onPlaybackDeviceChanged: function

:::info[自从]
*4.1.0*

音频播放设备状态变化回调。

该回调提示有音频播放设备被添加或移除。

```javascript
AgoraRTC.onPlaybackDeviceChanged = (info) => {
  console.log("speaker changed!", info.state, info.device);
};
```

**Parameters**

- **deviceInfo**：设备信息，详见 [DeviceInfo](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/deviceinfo)。
:::

#### Type declaration

- - (deviceInfo:[DeviceInfo](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/deviceinfo)):void

  - Parameters

    - deviceInfo: [DeviceInfo](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/deviceinfo)

    Returns void

<a id="onsecuritypolicyviolation"></a>

### Optional onSecurityPolicyViolation

onSecurityPolicyViolation: function

:::info[自从]
*4.15.0*

触发 CSP 规则回调。

当声网服务相关的资源加载或请求发送因为触发浏览器的 CSP（Content Security Policy，内容安全策略）规则而失败时，SDK 会触发该回调。
 收到该回调后，请及时调整你的 CSP 设置，确保能正常体验声网的服务。
:::

#### Type declaration

- - (event:SecurityPolicyViolationEvent):void

  - Parameters

    - event: SecurityPolicyViolationEvent

    Returns void

## Other Properties

<a id="version"></a>

### VERSION

VERSION: string

Agora Web SDK 的版本信息。

## Agora Core Methods

<a id="createclient"></a>

### createClient

- createClient(config:[ClientConfig](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/clientconfig)):[IAgoraRTCClient](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartcclient)

- 创建一个客户端对象用于通话/直播管理，通常来说这是使用 Agora Web SDK 的第一步。

  Parameters

  - config: [ClientConfig](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/clientconfig)

    客户端的配置，包括通话场景、编码格式等，默认使用 `vp8` 编码，`rtc` 通话场景。详见 [ClientConfig](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/clientconfig)。

  Returns [IAgoraRTCClient](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartcclient)

## Local Track Methods

<a id="createbuffersourceaudiotrack"></a>

### createBufferSourceAudioTrack

- createBufferSourceAudioTrack(config:[BufferSourceAudioTrackInitConfig](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/buffersourceaudiotrackinitconfig)):Promise&lt;[IBufferSourceAudioTrack](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/ibuffersourceaudiotrack)&gt;

- 通过音频文件或者 [AudioBuffer](https://developer.mozilla.org/zh-CN/docs/Web/API/AudioBuffer) 对象创建一个音频轨道。

该方法支持本地和在线音频文件。音频文件支持以下音频格式：

- MP3
- AAC
- 浏览器支持的其他音频格式

  Parameters

  - config: [BufferSourceAudioTrackInitConfig](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/buffersourceaudiotrackinitconfig)

    通过该配置指定文件路径、缓存策略和编码配置。

  Returns Promise&lt;[IBufferSourceAudioTrack](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/ibuffersourceaudiotrack)&gt;

  返回的音频轨道对象相对于普通的音频轨道对象增加了一些控制音频播放流程的方法。包括播放、暂停、跳转、实时播放状态获取等。

<a id="createcameravideotrack"></a>

### createCameraVideoTrack

- createCameraVideoTrack(config?:[CameraVideoTrackInitConfig](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/cameravideotrackinitconfig)):Promise&lt;[ICameraVideoTrack](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/icameravideotrack)&gt;

- 通过摄像头采集的视频创建一个视频轨道。

  Parameters

  - Optional config: [CameraVideoTrackInitConfig](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/cameravideotrackinitconfig)

    采集视频的配置，包括采集设备、编码配置等。详见 [CameraVideoTrackInitConfig](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/cameravideotrackinitconfig)。

  Returns Promise&lt;[ICameraVideoTrack](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/icameravideotrack)&gt;

<a id="createcustomaudiotrack"></a>

### createCustomAudioTrack

- createCustomAudioTrack(config:[CustomAudioTrackInitConfig](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/customaudiotrackinitconfig)):[ILocalAudioTrack](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/ilocalaudiotrack)

- 创建一个自定义的音频轨道。

你可以使用这个方法将自己维护的 [MediaStreamTrack](https://developer.mozilla.org/en-US/docs/Web/API/MediaStreamTrack) 转换成一个可以用于 SDK 的音频轨道。

  Parameters

  - config: [CustomAudioTrackInitConfig](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/customaudiotrackinitconfig)

    自定义音频的配置，详见 [CustomAudioTrackInitConfig](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/customaudiotrackinitconfig)。

  Returns [ILocalAudioTrack](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/ilocalaudiotrack)

<a id="createcustomvideotrack"></a>

### createCustomVideoTrack

- createCustomVideoTrack(config:[CustomVideoTrackInitConfig](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/customvideotrackinitconfig)):[ILocalVideoTrack](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/ilocalvideotrack)

- 创建一个自定义的视频轨道。

你可以使用这个方法将自己维护的 [MediaStreamTrack](https://developer.mozilla.org/en-US/docs/Web/API/MediaStreamTrack) 转换成一个可以用于 SDK 的视频轨道。

  Parameters

  - config: [CustomVideoTrackInitConfig](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/customvideotrackinitconfig)

    自定义视频轨道的配置，详见 [CustomVideoTrackInitConfig](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/customvideotrackinitconfig)。

    > 自 v4.17.1 起，自定义视频轨道配置 [config](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/customvideotrackinitconfig) 除了支持设置码率，还支持设置分辨率和帧率。

  Returns [ILocalVideoTrack](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/ilocalvideotrack)

<a id="createmicrophoneandcameratracks"></a>

### createMicrophoneAndCameraTracks

- createMicrophoneAndCameraTracks(audioConfig?:[MicrophoneAudioTrackInitConfig](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/microphoneaudiotrackinitconfig), videoConfig?:[CameraVideoTrackInitConfig](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/cameravideotrackinitconfig)):Promise&lt;[[IMicrophoneAudioTrack](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/imicrophoneaudiotrack),[ICameraVideoTrack](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/icameravideotrack)]&gt;

- 同时创建麦克风音频轨道和摄像头视频轨道。

通过麦克风采集的音频创建一个音频轨道，同时通过摄像头采集的视频创建一个视频轨道。

> 调用该方法和分别调用 [createMicrophoneAudioTrack](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#createmicrophoneaudiotrack) 以及 [createCameraVideoTrack](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#createcameravideotrack) 的区别是：
>
> - 调用该方法会同时要求麦克风和摄像头的授权，用户只需授权一次。
> - 分别创建音频轨道和视频轨道需要用户分别对麦克风和摄像头进行授权，也就是说用户需要授权两次。

  Parameters

  - Optional audioConfig: [MicrophoneAudioTrackInitConfig](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/microphoneaudiotrackinitconfig)

    采集音频的配置，包括采集设备、编码配置等。
  - Optional videoConfig: [CameraVideoTrackInitConfig](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/cameravideotrackinitconfig)

    采集视频的配置，包括采集设备、编码配置等。

  Returns Promise&lt;[[IMicrophoneAudioTrack](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/imicrophoneaudiotrack),[ICameraVideoTrack](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/icameravideotrack)]&gt;

<a id="createmicrophoneaudiotrack"></a>

### createMicrophoneAudioTrack

- createMicrophoneAudioTrack(config?:[MicrophoneAudioTrackInitConfig](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/microphoneaudiotrackinitconfig)):Promise&lt;[IMicrophoneAudioTrack](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/imicrophoneaudiotrack)&gt;

- 通过麦克风采集的音频创建一个音频轨道。

  Parameters

  - Optional config: [MicrophoneAudioTrackInitConfig](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/microphoneaudiotrackinitconfig)

    麦克风采集音频的配置，包括采集设备、音频编码配置等，详见 [MicrophoneAudioTrackInitConfig](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/microphoneaudiotrackinitconfig)。

  Returns Promise&lt;[IMicrophoneAudioTrack](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/imicrophoneaudiotrack)&gt;

<a id="createscreenvideotrack"></a>

### createScreenVideoTrack

- createScreenVideoTrack(config:[ScreenVideoTrackInitConfig](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/screenvideotrackinitconfig), withAudio:"enable"):Promise&lt;[[ILocalVideoTrack](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/ilocalvideotrack),[ILocalAudioTrack](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/ilocalaudiotrack)]&gt;
- createScreenVideoTrack(config:[ScreenVideoTrackInitConfig](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/screenvideotrackinitconfig), withAudio:"disable"):Promise&lt;[ILocalVideoTrack](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/ilocalvideotrack)&gt;
- createScreenVideoTrack(config:[ScreenVideoTrackInitConfig](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/screenvideotrackinitconfig), withAudio?:"enable"|"disable"|"auto"):Promise&lt;[[ILocalVideoTrack](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/ilocalvideotrack),[ILocalAudioTrack](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/ilocalaudiotrack)]|[ILocalVideoTrack](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/ilocalvideotrack)&gt;

- 创建用于屏幕共享的视频轨道。

  Parameters

  - config: [ScreenVideoTrackInitConfig](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/screenvideotrackinitconfig)

    屏幕共享的视频配置，包括编码配置、采集配置等。
  - withAudio: "enable"

    屏幕共享时是否同时分享**屏幕共享输入源**的音频。

    - `enable`: 分享音频。
    - `disable`: （默认）不分享音频。
    - `auto`: 根据浏览器是否支持决定是否分享音频。

      > 注意事项：
      >
      > - 该功能仅适用于支持 Web SDK 的浏览器桌面端，不支持移动端。具体支持列表详见[浏览器兼容性和已知问题](/zh-CN/realtime-media/rtc/reference/browser-compatibility)。
      > - 关于各操作系统平台的浏览器版本及功能支持的补充说明如下：
      >
      >   - 在 macOS 平台上，Chrome 74 及以上版本支持音视频共享，仅支持在共享 Chrome 标签页时分享音频。Firefox 和 Safari 14 及以上版本支持窗口和屏幕共享，不支持分享音频。
      >   - 在 Windows 平台上，Chrome 74 及以上版本和 Edge 支持在共享屏幕和浏览器标签页时分享音频，不支持在共享应用窗口时分享音频。Firefox 支持窗口和屏幕共享，不支持分享音频。
      >   - 在 ChromeOS 平台上，Chrome 支持在共享屏幕和共享浏览器标签页时分享音频，不支持在共享应用窗口时分享音频。
      > - 设置了 `withAudio` 为 `enable` 后，还需要在屏幕共享的弹出框上，勾选**分享音频**才能真正生效。

  Returns Promise&lt;[[ILocalVideoTrack](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/ilocalvideotrack),[ILocalAudioTrack](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/ilocalaudiotrack)]&gt;

  - 如果 `withAudio` 设为 `enable`，返回包含屏幕共享视频轨道和一个音频轨道的列表。如果用户没有勾选**分享音频**，SDK 会抛出错误。
  - 如果 `withAudio` 设为 `disable`，返回屏幕共享的视频轨道。
  - 如果 `withAudio` 设为 `auto`，在支持屏幕共享音频的浏览器上 SDK 会默认尝试分享音频。

    - 如果用户勾选了**分享音频**，会返回包含屏幕共享视频轨道和一个音频轨道的列表。
    - 如果用户没有勾选**分享音频**，只返回屏幕共享视频轨道。
- 创建用于屏幕共享的视频轨道。

  Parameters

  - config: [ScreenVideoTrackInitConfig](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/screenvideotrackinitconfig)

    屏幕共享的视频配置，包括编码配置、采集配置等。
  - withAudio: "disable"

    屏幕共享时是否同时分享**屏幕共享输入源**的音频。

    - `enable`: 分享音频。
    - `disable`: （默认）不分享音频。
    - `auto`: 根据浏览器是否支持决定是否分享音频。

      > 注意事项：
      >
      > - 该功能仅适用于支持 Web SDK 的浏览器桌面端，不支持移动端。具体支持列表详见[浏览器兼容性和已知问题](/zh-CN/realtime-media/rtc/reference/browser-compatibility)。
      > - 关于各操作系统平台的浏览器版本及功能支持的补充说明如下：
      >
      >   - 在 macOS 平台上，Chrome 74 及以上版本支持音视频共享，仅支持在共享 Chrome 标签页时分享音频。Firefox 和 Safari 14 及以上版本支持窗口和屏幕共享，不支持分享音频。
      >   - 在 Windows 平台上，Chrome 74 及以上版本和 Edge 支持在共享屏幕和浏览器标签页时分享音频，不支持在共享应用窗口时分享音频。Firefox 支持窗口和屏幕共享，不支持分享音频。
      >   - 在 ChromeOS 平台上，Chrome 支持在共享屏幕和共享浏览器标签页时分享音频，不支持在共享应用窗口时分享音频。
      > - 设置了 `withAudio` 为 `enable` 后，还需要在屏幕共享的弹出框上，勾选**分享音频**才能真正生效。

  Returns Promise&lt;[ILocalVideoTrack](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/ilocalvideotrack)&gt;

  - 如果 `withAudio` 设为 `enable`，返回包含屏幕共享视频轨道和一个音频轨道的列表。如果用户没有勾选**分享音频**，SDK 会抛出错误。
  - 如果 `withAudio` 设为 `disable`，返回屏幕共享的视频轨道。
  - 如果 `withAudio` 设为 `auto`，在支持屏幕共享音频的浏览器上 SDK 会默认尝试分享音频。

    - 如果用户勾选了**分享音频**，会返回包含屏幕共享视频轨道和一个音频轨道的列表。
    - 如果用户没有勾选**分享音频**，只返回屏幕共享视频轨道。
- 创建用于屏幕共享的视频轨道。

  Parameters

  - config: [ScreenVideoTrackInitConfig](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/screenvideotrackinitconfig)

    屏幕共享的视频配置，包括编码配置、采集配置等。
  - Optional withAudio: "enable"|"disable"|"auto"

    屏幕共享时是否同时分享**屏幕共享输入源**的音频。

    - `enable`: 分享音频。
    - `disable`: （默认）不分享音频。
    - `auto`: 根据浏览器是否支持决定是否分享音频。

      > 注意事项：
      >
      > - 该功能仅适用于支持 Web SDK 的浏览器桌面端，不支持移动端。具体支持列表详见[浏览器兼容性和已知问题](/zh-CN/realtime-media/rtc/reference/browser-compatibility)。
      > - 关于各操作系统平台的浏览器版本及功能支持的补充说明如下：
      >
      >   - 在 macOS 平台上，Chrome 74 及以上版本支持音视频共享，仅支持在共享 Chrome 标签页时分享音频。Firefox 和 Safari 14 及以上版本支持窗口和屏幕共享，不支持分享音频。
      >   - 在 Windows 平台上，Chrome 74 及以上版本和 Edge 支持在共享屏幕和浏览器标签页时分享音频，不支持在共享应用窗口时分享音频。Firefox 支持窗口和屏幕共享，不支持分享音频。
      >   - 在 ChromeOS 平台上，Chrome 支持在共享屏幕和共享浏览器标签页时分享音频，不支持在共享应用窗口时分享音频。
      > - 设置了 `withAudio` 为 `enable` 后，还需要在屏幕共享的弹出框上，勾选**分享音频**才能真正生效。

  Returns Promise&lt;[[ILocalVideoTrack](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/ilocalvideotrack),[ILocalAudioTrack](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/ilocalaudiotrack)]|[ILocalVideoTrack](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/ilocalvideotrack)&gt;

  - 如果 `withAudio` 设为 `enable`，返回包含屏幕共享视频轨道和一个音频轨道的列表。如果用户没有勾选**分享音频**，SDK 会抛出错误。
  - 如果 `withAudio` 设为 `disable`，返回屏幕共享的视频轨道。
  - 如果 `withAudio` 设为 `auto`，在支持屏幕共享音频的浏览器上 SDK 会默认尝试分享音频。

    - 如果用户勾选了**分享音频**，会返回包含屏幕共享视频轨道和一个音频轨道的列表。
    - 如果用户没有勾选**分享音频**，只返回屏幕共享视频轨道。

## Logger Methods

<a id="disablelogupload"></a>

### disableLogUpload

- disableLogUpload():void

- 关闭日志上传。

日志上传默认是关闭状态，如果你调用了开启日志上传（[enableLogUpload](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#enablelogupload))，可以通过本方法停止上传日志。

  Returns void

<a id="enablelogupload"></a>

### enableLogUpload

- enableLogUpload():void

- 开启日志上传。开启后 SDK 的日志会上传到声网的服务器。

日志上传功能默认为关闭状态，如果你需要开启此功能，请确保在所有方法之前调用本方法。

> 如果没有成功加入频道，则服务器上无法查看日志信息。

  Returns void

<a id="setloglevel"></a>

### setLogLevel

- setLogLevel(level:number):void

- 设置 SDK 的日志输出级别。

选择一个级别，你就可以看到在该级别及该级别以上所有级别的日志信息。

  Parameters

  - level: number

    SDK 日志输出级别。按照输出日志最全到最少排列：

    - 0: DEBUG。输出所有的 SDK 日志。
    - 1: INFO。输出 INFO、WARNING 和 ERROR 级别的日志。
    - 2: WARNING。输出 WARNING 和 ERROR 级别的日志。
    - 3: ERROR。输出 ERROR 级别的日志。
    - 4: NONE。不输出日志。

    例如，如果你输入代码 `AgoraRTC.setLogLevel(1);`，就可以看到 INFO，WARNING 和 ERROR 级别的日志信息。

  Returns void

## Media Devices Methods

<a id="getcameras"></a>

### getCameras

- getCameras(skipPermissionCheck?:boolean):Promise&lt;MediaDeviceInfo[]&gt;

- 该方法枚举可用的视频输入设备，比如摄像头。

调用成功后 SDK 会通过 [MediaDeviceInfo](https://developer.mozilla.org/en-US/docs/Web/API/MediaDeviceInfo) 对象返回可用的视频输入设备。

> 调用本方法会暂时打开摄像头以触发浏览器的媒体设备权限申请。在 Chrome 67+，Firefox 70+ 以及 Safari 12+ 等浏览器上没有媒体设备权限时，无法准确获取到设备信息。

  Parameters

  - Optional skipPermissionCheck: boolean

    是否跳过权限检查。你可以通过将该参数设置成 `true` 来跳过媒体设备权限申请步骤，但是 SDK 无法保证可以通过本方法获取准确的媒体设备信息。

    - `true`: 跳过权限检查。
    - `false`:（默认）不跳过权限检查。

  Returns Promise&lt;MediaDeviceInfo[]&gt;

<a id="getdevices"></a>

### getDevices

- getDevices(skipPermissionCheck?:boolean):Promise&lt;MediaDeviceInfo[]&gt;

- 该方法枚举可用的媒体输入和输出设备，比如麦克风、摄像头、耳机等。
 调用成功后 SDK 会通过 [MediaDeviceInfo](https://developer.mozilla.org/en-US/docs/Web/API/MediaDeviceInfo) 对象返回可用的媒体设备。

> 注意事项：
>
> - 调用本方法会暂时打开摄像头和麦克风以触发浏览器的媒体设备权限申请。在 Chrome 67+，Firefox 70+ 以及 Safari 12+ 等浏览器上没有媒体设备权限时，无法准确获取到设备信息。
> - 即使是对于同一设备，SDK 返回的 [MediaDeviceInfo.deviceId](https://developer.mozilla.org/en-US/docs/Web/API/MediaDeviceInfo/deviceId) 也可能会发生变化，例如用户清除 Cookie 后，`deviceId` 就会被重置。因此，声网不建议基于 `deviceId` 开发业务逻辑。

```javascript
getDevices().then(devices => {
  console.log("first device id", devices[0].deviceId);
}).catch(e => {
  console.log("get devices error!", e);
});
```

  Parameters

  - Optional skipPermissionCheck: boolean

    是否跳过权限检查。你可以通过将该参数设置成 `true` 来跳过媒体设备权限申请步骤，但是 SDK 无法保证可以通过本方法获取准确的媒体设备信息。

    - `true`: 跳过权限检查。
    - `false`:（默认）不跳过权限检查。

  Returns Promise&lt;MediaDeviceInfo[]&gt;

<a id="getelectronscreensources"></a>

### getElectronScreenSources

- getElectronScreenSources(type?:[ScreenSourceType](/zh-CN/api-reference/rtc/react-sdk/web-sdk/globals#screensourcetype)):Promise&lt;[ElectronDesktopCapturerSource](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/electrondesktopcapturersource)[]&gt;

- 在 Electron 环境下获取屏幕共享源。

调用成功后 SDK 会返回一组 [ElectronDesktopCapturerSource](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/electrondesktopcapturersource) 对象。

> 如果你的 Electron 运行环境开启了 contextIsolation 配置，调用此方法会报错。你需要自己使用 contextBridge.exposeInMainWorld 来获取视频源 id。

```text
// preload.js

const {
  contextBridge, desktopCapturer
} = require("electron");

contextBridge.exposeInMainWorld(
  "electronDesktopCapturer", {
    getSources: async (...args) => {
      const sources = await desktopCapturer.getSources(...args);
      return sources;
    }
  }
);

// renderer.js
(async () => {
  sources = await window.electronDesktopCapturer.getSources(["window", "screen"]);
  const source = sources[0];   // just for example ,you shuould make an UI for user to select the exact source.
  const screenVideoTrack = await AgoraRTC.createScreenVideoTrack({ electronScreenSourceId: source.id });
})()
```

  Parameters

  - Optional type: [ScreenSourceType](/zh-CN/api-reference/rtc/react-sdk/web-sdk/globals#screensourcetype)

    需要获取的共享源类型（窗口/应用/屏幕），详见 [ScreenSourceType](/zh-CN/api-reference/rtc/react-sdk/web-sdk/globals#screensourcetype)。如果为空则获取所有可以获取的共享源。

  Returns Promise&lt;[ElectronDesktopCapturerSource](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/electrondesktopcapturersource)[]&gt;

<a id="getmicrophones"></a>

### getMicrophones

- getMicrophones(skipPermissionCheck?:boolean):Promise&lt;MediaDeviceInfo[]&gt;

- 该方法枚举可用的音频输入设备，比如麦克风。

调用成功后 SDK 会通过 [MediaDeviceInfo](https://developer.mozilla.org/en-US/docs/Web/API/MediaDeviceInfo) 对象返回可用的音频输入设备。

> 调用本方法会暂时打开麦克风以触发浏览器的媒体设备权限申请。在 Chrome 67+，Firefox 70+ 以及 Safari 12+ 等浏览器上没有媒体设备权限时，无法准确获取到设备信息。

  Parameters

  - Optional skipPermissionCheck: boolean

    是否跳过权限检查。你可以通过将该参数设置成 `true` 来跳过媒体设备权限申请步骤，但是 SDK 无法保证可以通过本方法获取准确的媒体设备信息。

    - `true`: 跳过权限检查。
    - `false`:（默认）不跳过权限检查。

  Returns Promise&lt;MediaDeviceInfo[]&gt;

<a id="getplaybackdevices"></a>

### getPlaybackDevices

- getPlaybackDevices(skipPermissionCheck?:boolean):Promise&lt;MediaDeviceInfo[]&gt;

:::info[自从]
*4.1.0*

该方法枚举可用的音频播放设备，比如扬声器。

调用成功后 SDK 会通过 [MediaDeviceInfo](https://developer.mozilla.org/en-US/docs/Web/API/MediaDeviceInfo) 对象返回可用的音频播放设备。

> - Chrome、Firefox、Edge 浏览器上支持该方法。Safari 支持情况如下：macOS 上的 Safari 18.4 及以上版本支持该方法；iOS 上的 Safari 26 及以上版本支持该方法。
> - 调用本方法会暂时打开麦克风以触发浏览器的媒体设备权限申请。在 Chrome 67+ 和 Firefox 70+ 浏览器上，没有媒体设备权限时无法获取到准确的设备信息。
:::

Parameters

- Optional skipPermissionCheck: boolean

  是否跳过权限检查。你可以通过将该参数设置成 `true` 来跳过媒体设备权限申请步骤，但是 SDK 无法保证可以通过本方法获取准确的媒体设备信息。

  - `true`: 跳过权限检查。
  - `false`:（默认）不跳过权限检查。

Returns Promise&lt;MediaDeviceInfo[]&gt;

## Other Methods

<a id="checkaudiotrackisactive"></a>

### checkAudioTrackIsActive

- checkAudioTrackIsActive(track:[ILocalAudioTrack](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/ilocalaudiotrack)|[IRemoteAudioTrack](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iremoteaudiotrack), timeout?:number):Promise&lt;boolean&gt;

- 检测音频轨道是否活跃。

SDK 通过检测指定时间范围内音量是否发生变化来判断音频轨道是否活跃。

声网建议通过此方法在开始通话前对音频输入设备进行可用性检测。你可以将麦克风采集到的音频轨道作为参数传入该方法，判断麦克风是否正常运行。

> 注意事项：
>
> - 极为安静的环境下可能检测失败，所以声网建议你在使用此方法的时候提示用户发出声响。
> - 当音频轨道被 mute 时，会返回 `false`。
> - 不建议频繁调用此方法。

```javascript
const audioTrack = await AgoraRTC.createMicrophoneAudioTrack({ microphoneId });
AgoraRTC.checkAudioTrackIsActive(audioTrack).then(result => {
  console.log(`${ microphoneLabel } is ${ result ? "available" : "unavailable" }`);
}).catch(e => {
  console.log("check audio track error!", e);
});
```

  Parameters

  - track: [ILocalAudioTrack](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/ilocalaudiotrack)|[IRemoteAudioTrack](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iremoteaudiotrack)

    需要进行检测的本地或远端音频轨道。
  - Optional timeout: number

    音频轨道检测的超时时间，单位为毫秒，默认 5,000 毫秒。

  Returns Promise&lt;boolean&gt;

  传入该方法的音频轨道中的音量大小是否发生了变化：

  - `true`: 音量大小有变化。对于麦克风音频轨道，说明音频采集设备采集到了声音。
  - `false`: 音量大小未变化，说明音频采集设备没有采集到声音，或自定义音频轨道中没有音量变化，或音频轨道被 mute。

<a id="checksystemrequirements"></a>

### checkSystemRequirements

- checkSystemRequirements():boolean

- 检查 Agora Web SDK 对正在使用的浏览器的适配情况。

该方法必须在创建客户端对象 [createClient](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#createclient) 之前调用。

  Returns boolean

  是否支持当前浏览器。

  - `true`: 支持。
  - `false`: 不支持。

<a id="checkvideotrackisactive"></a>

### checkVideoTrackIsActive

- checkVideoTrackIsActive(track:[ILocalVideoTrack](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/ilocalvideotrack)|[IRemoteVideoTrack](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iremotevideotrack), timeout?:number):Promise&lt;boolean&gt;

- 检测视频轨道是否活跃。

SDK 通过检测指定时间范围内视频图像是否发生变动来判断视频轨道是否活跃。

声网建议通过此方法在开始通话前对视频输入设备进行可用性检测。你可以将摄像头采集到的视频轨道作为参数传入该方法，判断摄像头是否正常运行。

> 注意事项：
>
> - 当视频轨道被 mute 时，会返回 `false`。
> - 不建议频繁调用此方法。

```javascript
const videoTrack = await AgoraRTC.createCameraVideoTrack({ cameraId });
AgoraRTC.checkVideoTrackIsActive(videoTrack).then(result => {
  console.log(`${ cameraLabel } is ${ result ? "available" : "unavailable" }`);
}).catch(e => {
  console.log("check video track error!", e);
});
```

  Parameters

  - track: [ILocalVideoTrack](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/ilocalvideotrack)|[IRemoteVideoTrack](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iremotevideotrack)

    需要进行检测的本地或远端视频轨道。
  - Optional timeout: number

    视频轨道检测的超时时间，单位为毫秒，默认 5,000 毫秒。

  Returns Promise&lt;boolean&gt;

  传入该方法的视频轨道中的图像是否发生了变动：

  - `true`: 视频画面发生变动。对于摄像头视频轨道，说明视频采集设备采集到了画面。
  - `false`: 视频画面未变动，说明视频采集设备异常或被完全遮挡，或视频轨道被 mute。

<a id="createchannelmediarelayconfiguration"></a>

### createChannelMediaRelayConfiguration

- createChannelMediaRelayConfiguration():[IChannelMediaRelayConfiguration](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/ichannelmediarelayconfiguration)

- 创建跨频道媒体流转发的配置对象。

  Returns [IChannelMediaRelayConfiguration](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/ichannelmediarelayconfiguration)

<a id="getlisteners"></a>

### getListeners

- getListeners(event:string):Function[]

- Inherited from EventEmitter.getListeners

  指定一个事件名，获取当前所有监听这个事件的回调函数。

  Parameters

  - event: string

    事件名称。

  Returns Function[]

<a id="getsupportedcodec"></a>

### getSupportedCodec

- getSupportedCodec():Promise&lt;object&gt;

- 获取 Agora Web SDK 对当前浏览器支持的编解码格式。

调用该方法会返回声网服务与当前浏览器同时支持的编解码格式。目前而言，视频支持 VP8 及 H.264 格式，音频支持 OPUS 格式。

> 注意事项：
>
> - 该方法支持所有浏览器。对于不支持 WebRTC 或无法识别的浏览器环境，编解码列表返回为空。
> - 返回的音视频编码为浏览器通过 SDP 声称的编码类型，为参考值。
> - 目前部分安卓手机 H.264 与其他平台 H.264 存在无法互通或单通问题，对于这部分机型推荐使用 VP8 编码格式。

```javascript
AgoraRTC.getSupportedCodec().then(result => {
 console.log(`Supported video codec: ${result.video.join(",")}`);
 console.log(`Supported audio codec: ${result.audio.join(",")}`);
});
```

  Returns Promise&lt;object&gt;

  调用该方法会返回一个 `Promise` 对象，在 `.then(function(result)\{\})` 回调中，`result` 包含以下属性：

  - `video`: 数组类型，支持的视频编解码格式。可能含有 `"H264"`、`"VP8"` 两种取值，或为空数组。
  - `audio`: 数组类型，支持的音频编解码格式。可能含有 `"OPUS"`，或为空数组。

<a id="off"></a>

### off

- off(event:string, listener:Function):void

- Inherited from EventEmitter.off

  取消一个指定事件的监听。

  Parameters

  - event: string

    指定事件的名称。
  - listener: Function

    监听事件时传入的回调函数。

  Returns void

<a id="on"></a>

### on

- on(event:"camera-changed", listener:typeof event_camera_changed):void
- on(event:"microphone-changed", listener:typeof event_microphone_changed):void
- on(event:"playback-device-changed", listener:typeof event_playback_device_changed):void
- on(event:"autoplay-failed", listener:typeof event_autoplay_failed):void
- on(event:"security-policy-violation", listener:typeof event_security_policy_violation):void
- on(event:"audio-context-state-changed", listener:typeof event_audio_context_state_changed):void

Overrides EventEmitter.on

:::info[自从]
*4.18.0*
:::

Parameters

- event: "camera-changed"

  事件名称。
- listener: typeof event_camera_changed

  详见 [event_camera_changed](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#event_camera_changed)。

Returns void

Overrides EventEmitter.on

:::info[自从]
*4.18.0*
:::

Parameters

- event: "microphone-changed"

  事件名称。
- listener: typeof event_microphone_changed

  详见 [event_microphone_changed](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#event_microphone_changed)。

Returns void

Overrides EventEmitter.on

:::info[自从]
*4.18.0*
:::

Parameters

- event: "playback-device-changed"

  事件名称。
- listener: typeof event_playback_device_changed

  详见 [event_playback_device_changed](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#event_playback_device_changed)。

Returns void

Overrides EventEmitter.on

:::info[自从]
*4.18.0*
:::

Parameters

- event: "autoplay-failed"

  事件名称。
- listener: typeof event_autoplay_failed

  详见 [event_autoplay_failed](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#event_autoplay_failed)。

Returns void

Overrides EventEmitter.on

:::info[自从]
*4.18.0*
:::

Parameters

- event: "security-policy-violation"

  事件名称。
- listener: typeof event_security_policy_violation

  详见 [event_security_policy_violation](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#event_security_policy_violation)。

Returns void

Overrides EventEmitter.on

:::info[自从]
*4.20.0*
:::

Parameters

- event: "audio-context-state-changed"

  事件名称。
- listener: typeof event_audio_context_state_changed

  详见 [event_audio_context_state_changed](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#event_audio_context_state_changed)。

Returns void

<a id="once"></a>

### once

- once(event:string, listener:Function):void

- Inherited from EventEmitter.once

  监听一个指定的事件，当事件触发时会调用传入的回调函数。

  当监听后事件第一次触发时，该监听和回调函数就会被立刻移除，也就是只监听一次指定事件。

  Parameters

  - event: string

    指定事件的名称。
  - listener: Function

    传入的回调函数。

  Returns void

<a id="preload"></a>

### preload

- preload(appid:string, channel:string, token:string|null, uid?:[UID](/zh-CN/api-reference/rtc/react-sdk/web-sdk/globals#uid)|null):Promise&lt;void&gt;

- 使用`appid`、`channel`、`token`、`uid` 预加载频道。

调用该方法可以减少观众频繁切换频道时加入频道的时间，从而缩短观众听到主播首帧音频以及看到首帧画面的耗时，提升观众端的视频体验。

如果当前频道已经成功预加载，观众加入、离开频道后如需再次加入该频道，只要预加载时传入的 Token 仍在有效期内，则无需重新预加载。

> - 预加载的有效时间只有两分钟。
> - 为了保护页面性能，此方法采取一次性尽力而为的策略，无法保证一定成功。但预加载失败不会影响观众正常加入频道，也不会增加加入频道的耗时。
> - 系统最多缓存 10 条最新的不同预加载数据。
> - 目前此方法不支持通过 proxy 代理进行转发。

  Parameters

  - appid: string

    你的声网项目的 [App ID](/zh-CN/introduction/glossary)。
  - channel: string

    标识通话的频道名称，长度在 64 字节以内的字符串。以下为支持的字符集范围（共 89 个字符）:

    - 26 个小写字母 a-z。
    - 26 个大写字母 A-Z。
    - 10 个数字 0-9。
    - 空格。
    - "!", "#", "$", "%", "&", "(", ")", "+", "-", ":", ";", "&lt;", "=", ".", "&gt;", "?", "@", "[", "]", "^", "_", "\{", "\}", "|", "~", ","。
  - token: string|null

    用于鉴权的 token。

    - 如果你的项目没有开启 token 鉴权，这里填 `null`。
    - 安全要求不高: 你可以使用控制台生成的临时 token，详见[获取 RTC 临时 Token](/zh-CN/realtime-media/rtc/build/setup-and-access/enable-service)。
    - 安全要求高：传入从你的服务端获得的正式 token，详见[使用 Token 鉴权](/zh-CN/realtime-media/rtc/build/setup-and-access/token-authentication)。
  - Optional uid: [UID](/zh-CN/api-reference/rtc/react-sdk/web-sdk/globals#uid)|null

    标识用户的 ID。整数或字符串，ASCII 字符，需保证唯一性。如果不指定或设为 `null`，服务器会自动分配一个整数型 uid。

    - 如果使用整数作为用户 ID，需为 32 位无符号整数。建议设置范围：0 到 (232-1)。
    - 如果使用字符串作为用户 ID，长度不超过 255 个字符。

    :::warning
    为保证最佳的用户体验，声网强烈建议你不要使用字符串作为用户 ID。
    :::

    > 注意事项：
    >
    > - 一个频道内的所有用户必须使用同样类型的 `uid`，即必须都为整数或都为字符串。
    > - 使用字符串作为用户 ID 支持与 Native SDK 2.8 及以上版本互通，请确保 Native SDK 使用 User Account 加入频道。详见[使用 String 型的用户名](/zh-CN/api-reference/faq/integration/string_uid)。
    > - 为保证水晶球数据的准确性，建议自行指定 `uid` 并保证唯一性。

  Returns Promise&lt;void&gt;

<a id="processexternalmediaaec"></a>

### processExternalMediaAEC

- processExternalMediaAEC(element:HTMLMediaElement):void

:::info[自从]
*4.5.0*

对本地播放的音频启用回声消除。

在多个用户同时播放一个媒体文件的场景中，例如一起看电影，如果用户 A 在 Chrome 浏览器上通过 [HTMLMediaElement](https://developer.mozilla.org/zh-CN/docs/Web/API/HTMLMediaElement) 使用扬声器播放媒体文件，扬声器播放的声音会和人声一起被 SDK 采集，因此其他用户会听到自己本地播放的媒体音频以及用户 A 发送的媒体音频，从而产生回声。针对这种情况，你可以调用 `processExternalMediaAEC` 方法传入 `HTMLMediaElement`，对本地播放的媒体进行回声消除，提升音频体验。

```javascript
<audio crossOrigin="anonymous" src="http://www.test.com/test.mp3" id="audioDom"></audio>
<script>
  const element = document.getElementById("audioDom");
  AgoraRTC.processExternalMediaAEC(element);
</script>
```

> 注意事项：如果本地播放的媒体元素的地址是跨域地址，必须设置 [HTMLMediaElement](https://developer.mozilla.org/zh-CN/docs/Web/API/HTMLMediaElement) 中的 `crossOrigin` 属性为 `"anonymous"` 以允许跨域媒体被重采集。
:::

Parameters

- element: HTMLMediaElement

  需要进行回声消除的媒体元素，详见 [HTMLMediaElement](https://developer.mozilla.org/zh-CN/docs/Web/API/HTMLMediaElement)。

Returns void

<a id="registerextensions"></a>

### registerExtensions

- registerExtensions(extensions:IExtension&lt;any&gt;[]):void

:::info[自从]
*4.10.0*

注册插件。

声网当前支持以下插件：

- 虚拟背景插件
- AI 降噪插件
- 美颜插件
:::

Parameters

- extensions: IExtension&lt;any&gt;[]

  插件实例。

Returns void

<a id="removealllisteners"></a>

### removeAllListeners

- removeAllListeners(event?:string):void

- Inherited from EventEmitter.removeAllListeners

  指定一个事件，取消其所有的监听。

  Parameters

  - Optional event: string

    指定事件的名称，如果没有指定事件，则取消所有事件的所有监听。

  Returns void

<a id="resumeaudiocontext"></a>

### resumeAudioContext

- resumeAudioContext():void

- 恢复音视频播放。

在某些版本的 iOS 设备上，App 通话被微信通话或系统电话打断后有概率无法自动恢复。你可以调用该方法恢复 App 通话。

声网推荐你通过 [IAgoraRTC.on](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#on) 监听 `"audio-context-state-changed"` 事件，并在回调函数 [event_audio_context_state_changed](/zh-CN/api-reference/rtc/react-sdk/web-sdk/interfaces/iagorartc#event_audio_context_state_changed) 中进行以下处理：

- 当状态变为 `"interrupted"` 时，弹窗提示用户 App 通话被打断，需要点击按钮恢复通话。用户点击按钮后，调用 `resumeAudioContext`。
- 当状态变为 `"running"` 时，关闭弹窗。

  Returns void

<a id="setarea"></a>

### setArea

- setArea(area:[AREAS](/zh-CN/api-reference/rtc/react-sdk/web-sdk/enums/areas)[]|object):void

:::info[自从]
*4.2.0*

设置访问区域。

该功能为高级设置，适用于有访问安全限制的场景。

默认情况下，SDK 会就近选择声网服务器进行连接。设置访问区域之后，SDK 只会连接到指定区域内的声网服务器。

```javascript
// 指定仅访问北美的服务器。
AgoraRTC.setArea({
  areaCode:"NORTH_AMERICA"
})
```
:::

Parameters

- area: [AREAS](/zh-CN/api-reference/rtc/react-sdk/web-sdk/enums/areas)[]|object

  服务器的访问区域。区域码详见 [AREAS](/zh-CN/api-reference/rtc/react-sdk/web-sdk/enums/areas)。你可以设置 `areaCode` 参数指定单个访问区域。

Returns void
