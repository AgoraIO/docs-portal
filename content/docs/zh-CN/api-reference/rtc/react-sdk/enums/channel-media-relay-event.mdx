---
title: "ChannelMediaRelayEvent"
description: "ChannelMediaRelayEvent API reference."
---

跨频道媒体流转发过程中的事件列表，可以通过 [AgoraRTCClient.on("channel-media-relay-event")](/zh-CN/api-reference/rtc/react-sdk/interfaces/iagora-rtcclient#event_channel_media_relay_event) 获取。

## Enumeration members

<a id="network_connected"></a>

### NETWORK_CONNECTED

```ts
NETWORK_CONNECTED: = "NETWORK_CONNECTED"
```

用户与服务器建立连接。

<a id="network_disconnected"></a>

### NETWORK_DISCONNECTED

```ts
NETWORK_DISCONNECTED: = "NETWORK_DISCONNECTED"
```

网络中断导致用户与服务器连接断开。

<a id="packet_joined_dest_channel"></a>

### PACKET_JOINED_DEST_CHANNEL

```ts
PACKET_JOINED_DEST_CHANNEL: = "PACKET_JOINED_DEST_CHANNEL"
```

用户已加入目标频道。

<a id="packet_joined_src_channel"></a>

### PACKET_JOINED_SRC_CHANNEL

```ts
PACKET_JOINED_SRC_CHANNEL: = "PACKET_JOINED_SRC_CHANNEL"
```

用户已加入源频道。

<a id="packet_received_audio_from_src"></a>

### PACKET_RECEIVED_AUDIO_FROM_SRC

```ts
PACKET_RECEIVED_AUDIO_FROM_SRC: = "PACKET_RECEIVED_AUDIO_FROM_SRC"
```

服务器收到了目标频道发送的音频流。

<a id="packet_received_video_from_src"></a>

### PACKET_RECEIVED_VIDEO_FROM_SRC

```ts
PACKET_RECEIVED_VIDEO_FROM_SRC: = "PACKET_RECEIVED_VIDEO_FROM_SRC"
```

服务器收到了目标频道发送的视频流。

<a id="packet_sent_to_dest_channel"></a>

### PACKET_SENT_TO_DEST_CHANNEL

```ts
PACKET_SENT_TO_DEST_CHANNEL: = "PACKET_SENT_TO_DEST_CHANNEL"
```

SDK 开始向目标频道发送数据包。

<a id="packet_update_dest_channel"></a>

### PACKET_UPDATE_DEST_CHANNEL

```ts
PACKET_UPDATE_DEST_CHANNEL: = "PACKET_UPDATE_DEST_CHANNEL"
```

目标频道已更新。

<a id="packet_update_dest_channel_not_change"></a>

### PACKET_UPDATE_DEST_CHANNEL_NOT_CHANGE

```ts
PACKET_UPDATE_DEST_CHANNEL_NOT_CHANGE: = "PACKET_UPDATE_DEST_CHANNEL_NOT_CHANGE"
```

目标频道未更新。

<a id="packet_update_dest_channel_refused"></a>

### PACKET_UPDATE_DEST_CHANNEL_REFUSED

```ts
PACKET_UPDATE_DEST_CHANNEL_REFUSED: = "PACKET_UPDATE_DEST_CHANNEL_REFUSED"
```

内部原因导致目标频道更新失败。
