---
title: "GroupStore"
description: "GroupStore API 参考。"
---

`GroupStore` 类提供分组讨论功能相关的能力。

- [EduStoreBase](/zh-CN/api-reference/flexible-classroom/web/api-reference/classes/edu-store-base)

  - GroupStore

## 属性

<a id="groupDetails"></a>

### groupDetails

```ts
groupDetails: Map<string, GroupDetail>
```

分组详情

<a id="state"></a>

### state

```ts
state: GroupState
```

分组状态

## 访问器

<a id="currentSubRoom"></a>

### currentSubRoom

```ts
get currentSubRoom(): undefined | string
```

当前所在小组

#### 返回值

undefined | string

<a id="groupUuidByUserUuid"></a>

### groupUuidByUserUuid

```ts
get groupUuidByUserUuid(): Map<string, string>
```

用户所在组 ID 映射关系

#### 返回值

Map &lt; string, string &gt;

<a id="userByUuid"></a>

### userByUuid

```ts
get userByUuid(): Map<string, GroupUser>
```

返回 userUuid 组成的 map 数据

#### 返回值

Map &lt; string, [GroupUser](/zh-CN/api-reference/flexible-classroom/web/api-reference/interfaces/group-user) &gt;

## 方法

<a id="acceptGroupInvite"></a>

### acceptGroupInvite

```ts
acceptGroupInvite(groupUuid: string): Promise<void>
```

接受加入小组的邀请

#### 参数

| 名称 | 描述 |
| --- | --- |
| groupUuid: string | 组 ID |

#### 返回值

Promise &lt; void &gt;

<a id="addGroups"></a>

### addGroups

```ts
addGroups(groupDetails: (GroupDetail & { groupUuid?: string })[], inProgress?: boolean): Promise<void>
```

新建小组

#### 参数

| 名称 | 描述 |
| --- | --- |
| groupDetails: (GroupDetail & \{ groupUuid?: string \})[] |  |
| Optional inProgress: boolean |  |

#### 返回值

Promise &lt; void &gt;

<a id="broadcastMessage"></a>

### broadcastMessage

```ts
broadcastMessage(messageText: string): void
```

发送全体消息

#### 参数

| 名称 | 描述 |
| --- | --- |
| messageText: string | 消息 |

#### 返回值

void

<a id="joinSubRoom"></a>

### joinSubRoom

```ts
joinSubRoom(groupUuid: string): void
```

进入子房间

#### 参数

| 名称 | 描述 |
| --- | --- |
| groupUuid: string | 组 ID |

#### 返回值

void

<a id="leaveSubRoom"></a>

### leaveSubRoom

```ts
leaveSubRoom(): void
```

离开小组

#### 返回值

void

<a id="moveIntoSubRoom"></a>

### moveIntoSubRoom

```ts
moveIntoSubRoom(fromGroupUuid: string, toGroupUuid: string): void
```

用户主动从某房间移动至另一房间

#### 参数

| 名称 | 描述 |
| --- | --- |
| fromGroupUuid: string | 原组 ID |
| toGroupUuid: string | 目标组 ID |

#### 返回值

void

<a id="moveUsersToGroup"></a>

### moveUsersToGroup

```ts
moveUsersToGroup(fromGroupUuid: string, toGroupUuid: string, users: string[]): Promise<void>
```

将用户从某小组移入另一小组

#### 参数

| 名称 | 描述 |
| --- | --- |
| fromGroupUuid: string | 原组 ID |
| toGroupUuid: string | 目标组 ID |
| users: string[] | 用户 ID 列表 |

#### 返回值

Promise &lt; void &gt;

<a id="rejectGroupInvite"></a>

### rejectGroupInvite

```ts
rejectGroupInvite(groupUuid: string): Promise<void>
```

拒绝加入小组的邀请

#### 参数

| 名称 | 描述 |
| --- | --- |
| groupUuid: string | 组 ID |

#### 返回值

Promise &lt; void &gt;

<a id="removeGroupUsers"></a>

### removeGroupUsers

```ts
removeGroupUsers(fromGroupUuid: string, users: string[]): Promise<void>
```

将用户从小组中移除

#### 参数

| 名称 | 描述 |
| --- | --- |
| fromGroupUuid: string | 组 ID |
| users: string[] | 用户 ID 列表 |

#### 返回值

Promise &lt; void &gt;

<a id="removeGroups"></a>

### removeGroups

```ts
removeGroups(groups: string[]): Promise<void>
```

移除小组

#### 参数

| 名称 | 描述 |
| --- | --- |
| groups: string[] | 小组 ID 列表 |

#### 返回值

Promise &lt; void &gt;

<a id="startGroup"></a>

### startGroup

```ts
startGroup(groupDetails: GroupDetail[], syncBoardScenes?: boolean): Promise<void>
```

开启分组讨论

#### 参数

| 名称 | 描述 |
| --- | --- |
| groupDetails: GroupDetail[] | 分组配置数组 |
| Optional syncBoardScenes: boolean |  |

#### 返回值

Promise &lt; void &gt;

<a id="stopGroup"></a>

### stopGroup

```ts
stopGroup(): Promise<void>
```

结束分组讨论

#### 返回值

Promise &lt; void &gt;

<a id="updateGroupInfo"></a>

### updateGroupInfo

```ts
updateGroupInfo(groups: PatchGroup[]): Promise<void>
```

更新分组配置

#### 参数

| 名称 | 描述 |
| --- | --- |
| groups: PatchGroup[] | 分组配置数组 |

#### 返回值

Promise &lt; void &gt;

<a id="updateGroupUsers"></a>

### updateGroupUsers

```ts
updateGroupUsers(patches: PatchGroup[], sendInvitation?: boolean): Promise<void>
```

更新小组成员列表

#### 参数

| 名称 | 描述 |
| --- | --- |
| patches: PatchGroup[] | 更新配置 |
| Optional sendInvitation: boolean | 是否发送邀请 |

#### 返回值

Promise &lt; void &gt;
