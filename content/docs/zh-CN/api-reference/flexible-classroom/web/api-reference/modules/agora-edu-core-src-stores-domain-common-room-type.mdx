---
title: "agora-edu-core/src/stores/domain/common/room/type"
description: "agora-edu-core/src/stores/domain/common/room/type API 参考。"
---

## 类型别名

<a id="CarouselSetting"></a>

### CarouselSetting

```ts
CarouselSetting: { interval: number; range: number; state: number; type: number }
```

视频轮播设置

#### 类型声明

##### `interval: number`

轮播列表更新周期，单位：秒

##### `range: number`

轮播范围，哪些用户可以参与轮播 1: 全部 2: 仅设备可用用户

##### `state: number`

轮播是否开启 1: 开启 0: 关闭

##### `type: number`

轮播类型 1: 顺序 2: 随机

<a id="ClassroomSchedule"></a>

### ClassroomSchedule

```ts
ClassroomSchedule: { closeDelay: number; duration?: number; startTime?: number; state?: ClassState }
```

教室日程信息

#### 类型声明

##### `closeDelay: number`

拖堂时间，单位：秒

##### `duration?: number`

教室持续时间，单位：秒

##### `startTime?: number`

教室开始时间，单位：毫秒

##### `state?: ClassState`

教室状态

<a id="ExpandedScope"></a>

### ExpandedScope

```ts
ExpandedScope: { state: ExpandedScopeState; userUuids: string[] }
```

视频墙状态

#### 类型声明

##### `state: ExpandedScopeState`

视频墙是否开启

##### `userUuids: string[]`

视频墙用户 UUID 列表

<a id="FcrCustomMessage"></a>

### FcrCustomMessage

```ts
FcrCustomMessage: AgoraRteCustomMessage
```

<a id="FcrCustomMessageObserver"></a>

### FcrCustomMessageObserver

```ts
FcrCustomMessageObserver: AgoraRteCustomMessageObserver
```

<a id="HandUpProgress"></a>

### HandUpProgress

```ts
HandUpProgress: { payload?: any; ts: number; userName: string; userUuid: string }
```

举手进程

#### 类型声明

##### `payload?: any`

扩展信息

##### `ts: number`

举手时间戳，单位：秒

##### `userName: string`

用户姓名

##### `userUuid: string`

用户 UUID

<a id="HostingScene"></a>

### HostingScene

```ts
HostingScene: { finishType: number; reserveVideoURL: string; videoURL: string }
```

已弃用

#### 类型声明

##### `finishType: number`

##### `reserveVideoURL: string`

##### `videoURL: string`

<a id="RecordStreamingUrl"></a>

### RecordStreamingUrl

```ts
RecordStreamingUrl: { flv: string; hls: string; rtmp: string }
```

录制推流信息

#### 类型声明

##### `flv: string`

FLV URL

##### `hls: string`

HLS URL

##### `rtmp: string`

RTMP URL

<a id="TrackData"></a>

### TrackData

```ts
TrackData: { extra?: any; position: { xaxis: number; yaxis: number }; size?: { height: number; width: number } }
```

轨迹信息

#### 类型声明

##### `extra?: any`

扩展信息

##### `position: { xaxis: number; yaxis: number }`

对象坐标

##### `size?: { height: number; width: number }`

对象尺寸
