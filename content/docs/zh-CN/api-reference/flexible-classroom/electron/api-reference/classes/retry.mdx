---
title: "Retry"
description: "Retry API 参考。"
---

- Retry

## 构造函数

<a id="constructor"></a>

### constructor

```ts
new Retry(fn: "agora-rte-sdk/src/core/utils/utils".AnyFunction, args: unknown[], opts?: RetryOptions): Retry
```

#### 参数

| 名称 | 描述 |
| --- | --- |
| fn: "agora-rte-sdk/src/core/utils/utils".AnyFunction | target function |
| args: unknown[] | arguments the target function to be called with |
| Optional opts: RetryOptions | options |

#### 返回值

[Retry](/zh-CN/api-reference/flexible-classroom/electron/api-reference/classes/retry)

## 方法

<a id="abort"></a>

### abort

```ts
abort(fn: AbortCallback): Retry
```

Registers a callback to be called when the target function and all retries fail

#### 参数

| 名称 | 描述 |
| --- | --- |
| fn: AbortCallback | callback function to be called |

#### 返回值

[Retry](/zh-CN/api-reference/flexible-classroom/electron/api-reference/classes/retry)

<a id="exec"></a>

### exec

```ts
exec(): Promise<void>
```

Starts executes the target function retriesMax would be 3 and maxBackoff would be 10000(10 sceonds) by default

#### 返回值

Promise &lt; void &gt;

<a id="fail"></a>

### fail

```ts
fail(fn: AttemptFailCallback): Retry
```

Registers a callback to be called when the target function fails This callback function can be used to manage, by yourself, the retry system. It's called when an error occurred and before to retry. This method can have three behaviors: you can throw an error if it returns truthy value then normal retry system continues if it returns falsy value then the retry system stop

#### 参数

| 名称 | 描述 |
| --- | --- |
| fn: AttemptFailCallback | callback function to be called |

#### 返回值

[Retry](/zh-CN/api-reference/flexible-classroom/electron/api-reference/classes/retry)
