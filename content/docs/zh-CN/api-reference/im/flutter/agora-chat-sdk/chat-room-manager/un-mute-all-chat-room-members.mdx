---
title: "unMuteAllChatRoomMembers method"
description: "API docs for the unMuteAllChatRoomMembers method from the ChatRoomManager class, for the Dart programming language."
---

Unmutes all members.

Only the chat room owner or admin can call this method.

**Throws** A description of the exception. See [ChatError](/zh-CN/api-reference/im/flutter/agora-chat-sdk/chat-error).

## Parameters

| Name | Description |
| --- | --- |
| `roomId` | The chat room ID. |

```dart
Future<void> unMuteAllChatRoomMembers(String roomId)
```

<a id="source"></a>

## Implementation

```dart
Future<void> unMuteAllChatRoomMembers(String roomId) async {
  Map req = {"roomId": roomId};
  Map result = await _channel.invokeMethod(
    ChatMethodKeys.unMuteAllChatRoomMembers,
    req,
  );
  try {
    ChatError.hasErrorFromResult(result);
  } on ChatError catch (e) {
    throw e;
  }
}
```
