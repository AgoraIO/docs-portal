---
title: "lastMessage property"
description: "API docs for the lastMessage property from the ChatThread class, for the Dart programming language."
---

The last reply in the message thread. If it is empty, the last message is withdrawn.

```dart
ChatMessage? lastMessage final
```

<a id="source"></a>

## Implementation

```dart
final ChatMessage? lastMessage;
```
