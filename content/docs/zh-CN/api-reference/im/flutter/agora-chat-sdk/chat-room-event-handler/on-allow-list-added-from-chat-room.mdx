---
title: "onAllowListAddedFromChatRoom property"
description: "API docs for the onAllowListAddedFromChatRoom property from the ChatRoomEventHandler class, for the Dart programming language."
---

Occurs when the chat room member(s) is added to the allowlist.

```dart
(void Function?(String roomId, List<String> members)?)onAllowListAddedFromChatRoom final
```

<a id="source"></a>

## Implementation

```dart
final void Function(
  String roomId,
  List<String> members,
)? onAllowListAddedFromChatRoom;
```
