---
title: "com.herewhite.sdk.domain.CameraConfig类 参考"
description: "CameraConfig 类，用于配置视角参数。"
---

`CameraConfig` 类，用于配置视角参数。

### `自从`

2.2.0

## 成员函数说明

<a id="a59912f9cc9f27a762b2f8378239a133a"></a>

### getAnimationMode()

```cpp
AnimationMode com.herewhite.sdk.domain.CameraConfig.getAnimationMode()
```

获取视角调整时的动画模式。

##### `自从`

2.3.2

#### Returns

视角调整时的动画模式，详见 `AnimationMode`。

<a id="aad4dc6b36d223c351ca603fb451081d9"></a>

### setAnimationMode()

```cpp
void com.herewhite.sdk.domain.CameraConfig.setAnimationMode(AnimationMode animationMode)
```

设置视角调整时的动画模式。

##### `自从`

2.3.2

#### Parameters

| Name | Description |
| --- | --- |
| animationMode | 视角调整时的动画模式，详见 `AnimationMode`。 |

<a id="a2cf8cd77b196c36bf3c5d9cad1c07d53"></a>

### getCenterX()

```cpp
Double com.herewhite.sdk.domain.CameraConfig.getCenterX()
```

获取视角的中心点在世界坐标系（以白板初始化时的中心点为原点的坐标系）中的 X 轴坐标。

#### Returns

视角的中心点在世界坐标系中的 X 轴坐标。

<a id="a4aea934e2b74918ae6c477452108880b"></a>

### setCenterX()

```cpp
void com.herewhite.sdk.domain.CameraConfig.setCenterX(Double centerX)
```

设置视角的中心点在世界坐标系（以白板初始化时的中心点为原点的坐标系）中的 X 轴坐标。

#### Parameters

| Name | Description |
| --- | --- |
| centerX | 视角的中心点在世界坐标系中的 X 轴坐标。默认值为 0。 |

<a id="abba23cd673ddeb6a52f2287d52f3c3ef"></a>

### getCenterY()

```cpp
Double com.herewhite.sdk.domain.CameraConfig.getCenterY()
```

获取视角的中心点在世界坐标系（以白板初始化时的中心点为原点的坐标系）中的 Y 轴坐标。

#### Returns

视角的中心点在世界坐标系中的 Y 轴坐标。

<a id="af10b4facaec5b1b9d29fd596dbf2251e"></a>

### setCenterY()

```cpp
void com.herewhite.sdk.domain.CameraConfig.setCenterY(Double centerY)
```

设置视角的中心点在世界坐标系（以白板初始化时的中心点为原点的坐标系）中的 Y 轴坐标。

#### Parameters

| Name | Description |
| --- | --- |
| centerY | 视角的中心点在世界坐标系中的 Y 轴坐标。默认值为 0。 |

<a id="aab232fba03cf8c9fe30749c741513f22"></a>

### getScale()

```cpp
Double com.herewhite.sdk.domain.CameraConfig.getScale()
```

获取视角的缩放比例。

#### Returns

视角的缩放比例。

<a id="ab59b3dff765e941eeed2f1d63e2335d5"></a>

### setScale()

```cpp
void com.herewhite.sdk.domain.CameraConfig.setScale(Double scale)
```

设置视角的缩放比例。

#### Parameters

| Name | Description |
| --- | --- |
| scale | 视角的缩放比例。 |
