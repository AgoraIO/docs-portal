---
title: "com.herewhite.sdk.Room类 参考"
description: "Room 类，用于操作互动白板实时房间。"
---

`Room` 类，用于操作互动白板实时房间。

## 成员函数说明

<a id="af0769957f685bc185fcb6d53dec6a5cb"></a>

### getDisconnectedBySelf()

```cpp
Boolean com.herewhite.sdk.Room.getDisconnectedBySelf()
```

获取用户是否主动断开与白板房间的连接。

该方法可以避免白板 SDK 反复重连，用户不断重新加入房间。

<a id="a3d1f57d33e446dd2002f93e1f3840606"></a>

### getWritable()

```cpp
Boolean com.herewhite.sdk.Room.getWritable()
```

获取本地用户在当前互动白板实时房间是否为互动模式。

#### Returns

获取本地用户是否为互动模式：

- `true`：本地用户在当前互动白板实时房间为互动模式，即可对白板进行读写操作。
- `false`: 本地用户在当前互动白板实时房间为订阅模式，即对白板只能进行读取操作。

<a id="a43f3b32f18de3cd71c95a449deae6a6c"></a>

### getObserverId()

```cpp
Long com.herewhite.sdk.Room.getObserverId()
```

获取用户在当前房间中的用户 ID。

##### `自从`

2.4.11

#### Returns

用户 ID。

<a id="a50bf9e950f3cbce8b9ed5b250444ea70"></a>

### setGlobalState()

```cpp
void com.herewhite.sdk.Room.setGlobalState(GlobalState globalState)
```

修改互动白板实时房间的公共全局状态。

实时房间的 `globalState` 属性为公共全局变量，房间内所有用户都可以读取 `globalState`，互动模式用户可以修改 `globalState`。 修改后的 `globalState` 会立即同步给所有用户。

#### Parameters

| Name | Description |
| --- | --- |
| globalState | 房间公共全局状态，详见 `GlobalState`. |

<a id="ab2d72aca8bef76ded5aa3c3ee873902f"></a>

### setMemberState()

```cpp
void com.herewhite.sdk.Room.setMemberState(MemberState memberState)
```

修改房间内的白板工具状态。

调用该方法会立刻更新房间的 `MemberState`。 你可以调用 `getMemberState` 获取最新设置的白板工具状态。

#### Parameters

| Name | Description |
| --- | --- |
| memberState | 需要修改的白板工具状态，详见 `MemberState`。 |

<a id="ad06f67470a69830938a901baa8cab2bd"></a>

### copy()

```cpp
void com.herewhite.sdk.Room.copy()
```

复制选中内容。

##### `自从`

2.9.3

该方法会将选中的内容存储到内存中，不会粘贴到白板中。

##### `注解`

该方法仅当 `disableSerialization` 设为 `false` 时生效。

<a id="a8e1c5006771674ae3b468813e29492f4"></a>

### paste()

```cpp
void com.herewhite.sdk.Room.paste()
```

粘贴复制的内容。

##### `自从`

2.9.3

该方法会将 `copy` 方法复制的内容粘贴到白板中（用户当前的视角中间）。

##### `注解`

- 该方法仅当 `disableSerialization` 设为 `false` 时生效。
- 多次调用该方法时，不能保证粘贴的内容每次都在用户当前的视角中间，可能会出现随机偏移。

<a id="ab5415f550b394c8b19a121f5d4861426"></a>

### duplicate()

```cpp
void com.herewhite.sdk.Room.duplicate()
```

复制并粘贴选中的内容。

##### `自从`

2.9.3

该方法会将选中的内容复制并粘贴到白板中（用户当前的视角中间）。

##### `注解`

- 该方法仅当 `disableSerialization` 设为 `false` 时生效。
- 多次调用该方法时，不能保证粘贴的内容每次都在用户当前的视角中间，可能会出现随机偏移。

<a id="ab3a428118f9e090b0e62880d566af261"></a>

### deleteOperation()

```cpp
void com.herewhite.sdk.Room.deleteOperation()
```

删除选中的内容。

##### `自从`

2.9.3

<a id="a7c790db1b2fc9404d39094c5592685e6"></a>

### disableSerialization()

```cpp
void com.herewhite.sdk.Room.disableSerialization(boolean disable)
```

开启/禁止本地序列化。

##### `自从`

2.9.3

设置 `disableSerialization(true)` 后，以下方法将不生效：

- `redo`
- `undo`
- `duplicate`
- `copy`
- `paste`

##### `警告`

如果要设置 `disableSerialization(false)`，必须确保同一房间内所有用户使用的 SDK 满足以下版本要求，否则会导致 app 客户端崩溃。

- Web SDK 2.9.3 或之后版本
- Android SDK 2.9.3 或之后版本
- iOS SDK 2.9.3 或之后版本

#### Parameters

| Name | Description |
| --- | --- |
| disable | 是否禁止本地序列化：<br />- `true`：（默认）禁止开启本地序列化；<br />- `false`： 开启本地序列化，即可以对本地操作进行解析。 |

<a id="a9ad047897d74af1590316cc5d338408c"></a>

### redo()

```cpp
void com.herewhite.sdk.Room.redo()
```

重做，即回退撤销操作。

##### `自从`

2.9.3

##### `注解`

该方法仅当 `disableSerialization` 设为 `false` 时生效。

<a id="a0cd3966f1df8de10f2bb5759fa28998b"></a>

### undo()

```cpp
void com.herewhite.sdk.Room.undo()
```

撤销上一步操作。

##### `自从`

2.9.3

##### `注解`

该方法仅当 `disableSerialization` 设为 `false` 时生效。

<a id="a62f41d229dee8d67211b3eeaaa46ef6e"></a>

### setViewMode()

```cpp
void com.herewhite.sdk.Room.setViewMode(ViewMode viewMode)
```

切换视角模式。

互动白板实时房间支持对用户设置以下视角模式：

- `Broadcaster`: 主播模式。
- `Follower`：跟随模式。
- `Freedom`：（默认）自由模式。

该方法的设置会影响房间内所有用户的视角模式：

- 当房间内不存在主播模式的用户时，所有用户的视角都默认为自由模式。
- 当一个用户的视角设置为主播模式后，房间内其他所有用户（包括新加入房间的用户）的视角会被自动设置为跟随模式。
- 当跟随模式的用户进行白板操作时，其视角会自动切换为自由模式。你可以调用 `disableOperations` (true) 禁止跟随模式的用户操作白板，以保证其保持跟随模式。

该方法是异步操作。调用该方法后，你可以使用 `getBroadcastState` [2/2] 获取最新设置的视角模式。

#### Parameters

| Name | Description |
| --- | --- |
| viewMode | 视角模式，详见 `ViewMode`。 |

<a id="ac10c063697e5224ef05d341bb3987004"></a>

### disconnect() [1/2]

```cpp
void com.herewhite.sdk.Room.disconnect()
```

主动断开与互动白板实时房间实例的连接。

该方法会把与当前房间实例相关的所有资源释放掉。如果要再次加入房间，需要重新调用 `joinRoom`。

##### `注解`

调用该方法不会触发回调。如果需要收到断开连接的回调，请使用 `disconnect` [2/2]。

<a id="ad8aa1d2ce5b8e6d78094c2a9a3836dfa"></a>

### disconnect() [2/2]

```cpp
void com.herewhite.sdk.Room.disconnect(@Nullable final Promise<Object > promise)
```

主动断开与互动白板实时房间实例的连接。

该方法会把与当前房间实例相关的所有资源释放掉。如果要再次加入房间，需要重新调用 `joinRoom`。

你可以在该方法中传入 `Promise<Object>` 接口实例，以获取方法调用结果。

#### Parameters

| Name | Description |
| --- | --- |
| promise | `Promise<Object>` 接口实例，详见 `Promise`。你可以通过该接口获取 `disconnect` 的调用结果：<br />- 如果方法调用成功，则返回房间的全局状态。<br />- 如果方法调用失败，则返回错误信息。 |

<a id="a195049bf53b03d538c847e0eecf9b679"></a>

### insertImage() [1/2]

```cpp
void com.herewhite.sdk.Room.insertImage(ImageInformation imageInfo)
```

插入图片显示区域。

SDK 会根据你传入的 `ImageInformation` 在白板上设置并插入图片的显示区域。 调用该方法后，还需要调用 `completeImageUpload` 传入图片的 Url 地址，以在该显示区域插入并展示图片。

##### `注解`

你也可以调用 `insertImage` 方法同时传入图片信息和图片的 Url 地址，在白板中插入并展示图片。

#### Parameters

| Name | Description |
| --- | --- |
| imageInfo | 图片信息，详见 `ImageInformation`。 |

<a id="a355bbeefe466ac0c2c5b2cdc420ce5dc"></a>

### completeImageUpload()

```cpp
void com.herewhite.sdk.Room.completeImageUpload(String uuid, String url)
```

展示图片。

该方法可以将指定的网络图片展示到指定的图片显示区域。

##### `注解`

调用该方法前，请确保你已经调用 `insertImage` 方法在白板上插入了图片的显示区域。

#### Parameters

| Name | Description |
| --- | --- |
| uuid | 图片显示区域的 UUID, 即在 `insertImage` 方法的 `ImageInformation` 中传入的图片 UUID。 |
| url | 图片的 URL 地址。必须确保 app 客户端能访问该 URL，否则无法正常展示图片。 |

<a id="a6c85af35615b4d4696717b862fb428bc"></a>

### insertImage() [2/2]

```cpp
void com.herewhite.sdk.Room.insertImage(ImageInformationWithUrl imageInformationWithUrl)
```

插入并展示图片。

该方法封装了 `insertImage` 和 `completeImageUpload` 方法。 你可以在该方法中同时传入图片信息和图片的 URL，直接在白板中插入图片的显示区域并展示图片。

#### Parameters

| Name | Description |
| --- | --- |
| imageInformationWithUrl | 图片信息及图片的 URL 地址，详见 `ImageInformationWithUrl`。 |

<a id="a6ed25a156436b44b6405a279aeac6e6d"></a>

### insertText()

```cpp
void com.herewhite.sdk.Room.insertText(int x, int y, String text, Promise<String > promise)
```

在指定位置插入文字。

#### Parameters

| Name | Description |
| --- | --- |
| x | 第一个文字左侧边的中点在世界坐标系中的 X 轴坐标。 |
| y | 第一个文字左侧边的中点在世界坐标系中的 Y 轴坐标。 |
| text | 初始的文字内容，不传则为空。 |
| promise | `Promise<String>` 接口实例，详见 `Promise`。你可以通过该接口获取方法调用结果：<br />- 如果方法调用成功，则返回文字标识符。<br />- 如果方法调用失败，则返回错误信息。 |

<a id="a6f3389698783fc57286518b370a80357"></a>

### updateText()

```cpp
void com.herewhite.sdk.Room.updateText(String id, String text)
```

更新指定文本的内容。

#### Parameters

| Name | Description |
| --- | --- |
| id | 文字标识符，即 `insertText` 方法的回调值。 |
| text | 文字内容。 |

<a id="ac21ea599028066b2c54caece51372b77"></a>

### getGlobalState() [1/2]

```cpp
GlobalState com.herewhite.sdk.Room.getGlobalState()
```

获取房间的全局状态。

##### `自从`

2.4.0

##### `注解`

- 该方法为同步调用。
- 对于通过 `setCustomGlobalStateClass` 方法设置的自定义 `GlobalState`，在获取后，可以直接进行强转。
- 调用 `setGlobalState` 方法后，可以立刻调用该方法。

#### Returns

房间的全局状态，详见 `GlobalState`。

<a id="a9d92ee9c480ff653ea44fb269740765d"></a>

### getGlobalState() [2/2]

```cpp
void com.herewhite.sdk.Room.getGlobalState(final Promise<GlobalState > promise)
```

获取房间全局状态。

##### `**[弃用:](/zh-CN/api-reference/whiteboard/android/deprecated#_deprecated000015)**`

该方法已废弃。请使用 `getGlobalState` [1/2]。

##### `注解`

- 该方法为异步调用。
- 对于通过 `setCustomGlobalStateClass` 方法设置的自定义 `GlobalState`，在获取后，可以直接进行强转。

#### Parameters

| Name | Description |
| --- | --- |
| promise | `Promise<GlobalState>` 接口实例，详见 `Promise`。你可以通过该接口获取 `getGlobalState` 的调用结果：<br />- 如果方法调用成功，则返回 `GlobalState` 对象，详见 `GlobalState`。<br />- 如果方法调用失败，则返回错误信息。 |

<a id="adb7d0a6430be84c8ff5ce5ae67aeb5ca"></a>

### getMemberState() [1/2]

```cpp
MemberState com.herewhite.sdk.Room.getMemberState()
```

获取当前的白板工具状态。

##### `自从`

2.4.0

##### `注解`

- 该方法为同步调用。
- 调用 `setMemberState` 方法后，可以立即调用 `getMemberState` 获取最新的白板工具状态。

#### Returns

当前的白板工具状态，详见 `MemberState`。

<a id="ae6d6f0c6ae7385a00efa62732cc04543"></a>

### getMemberState() [2/2]

```cpp
void com.herewhite.sdk.Room.getMemberState(final Promise<MemberState > promise)
```

获取当前的白板工具状态。

##### `注解`

该方法为异步调用。

#### Parameters

| Name | Description |
| --- | --- |
| promise | `Promise<MemberState>` 接口实例，详见 `Promise`。你可以通过该接口获取 `getMemberState` 的调用结果：<br />- 如果方法调用成功，则返回白板工具状态，详见 `MemberState`。<br />- 如果方法调用失败，则返回错误信息。 |

<a id="a2388307121c075620ba48f71fa970483"></a>

### getRoomMembers() [1/2]

```cpp
RoomMember[] com.herewhite.sdk.Room.getRoomMembers()
```

获取房间的用户列表。

##### `注解`

- 该方法为同步调用。
- 房间的用户列表仅包含互动模式（具有读写权限）的用户，不包含订阅模式（只读权限）的用户。

#### Returns

用户列表，详见 `RoomMember`。

<a id="a48ab27e5a0dbcb3d9cc06ae161cb7b58"></a>

### getRoomMembers() [2/2]

```cpp
void com.herewhite.sdk.Room.getRoomMembers(final Promise<RoomMember[]> promise)
```

获取房间的用户列表。

##### `注解`

- 该方法为异步调用。
- 房间的用户列表仅包含互动模式（具有读写权限）的用户，不包含订阅模式（只读权限）的用户。

#### Parameters

| Name | Description |
| --- | --- |
| promise | `Promise<RoomMember []>` 接口实例，详见 `Promise`。你可以通过该接口获取 `getRoomMembers` 的调用结果：<br />- 如果方法调用成功，则返回用户列表，详见 `RoomMember`。<br />- 如果方法调用失败，则返回错误信息。 |

<a id="af6f8d5c76a3d331cd1cd9d6f15565976"></a>

### getBroadcastState() [1/2]

```cpp
BroadcastState com.herewhite.sdk.Room.getBroadcastState()
```

获取用户的视角状态。

##### `自从`

2.4.0

##### `注解`

- 该方法为同步调用。
- 调用 `setViewMode` 修改用户视角模式后，无法立刻通过 `getBroadcastState` [1/2] 获取最新的用户视角状态。 如果需要立即获取最新的用户视角状态，可以调用 `getBroadcastState` [2/2]。

#### Returns

用户视角状态，详见 `BroadcastState`。

<a id="abd40d4f3d60e963105a09eb514ecf29d"></a>

### getBroadcastState() [2/2]

```cpp
void com.herewhite.sdk.Room.getBroadcastState(final Promise<BroadcastState > promise)
```

获取用户视角状态。

##### `注解`

- 该方法为异步调用。
- 调用 `setViewMode` 修改用户视角模式后，无法立刻通过 `getBroadcastState` [1/2] 获取最新的用户视角状态。如果需要 立即获取最新的用户视角状态，可以调用 `getBroadcastState` [2/2]。

#### Parameters

| Name | Description |
| --- | --- |
| promise | `Promise<BroadcastState>` 接口实例，详见 `Promise`。你可以通过该接口获取 `getBroadcastState` 的调用结果：<br />- 如果方法调用成功，则返回用户视角状态，详见 `BroadcastState`。<br />- 如果方法调用失败，则返回错误信息。 |

<a id="a8a708f07fc9914a6a54a6c18e696ce2d"></a>

### getSceneState() [1/2]

```cpp
SceneState com.herewhite.sdk.Room.getSceneState()
```

获取房间当前场景目录下的场景状态。

##### `自从`

2.4.0

##### `注解`

- 该方法为同步调用。
- 调用以下方法修改或新增场景后，无法通过 `getSceneState` [1/2] 立即获取最新的场景状态。此时，如果需要立即获取最新的场景状态， 可以调用 `getSceneState` [2/2]。

  - `setScenePath` [1/2]
  - `setScenePath` [2/2]
  - `putScenes(String, Scene [], int)`

#### Returns

当前场景目录下的场景状态，详见 `SceneState`。

<a id="a76bd84717f518dddd9396c61f9e2f606"></a>

### getSceneState() [2/2]

```cpp
void com.herewhite.sdk.Room.getSceneState(final Promise<SceneState > promise)
```

获取房间当前场景目录下的场景状态。

##### `注解`

- 该方法为异步调用。
- 调用以下方法修改或新增场景后，你可以通过 `getSceneState` 立即获取最新的场景状态。

  - `setScenePath` [1/2]
  - `setScenePath` [2/2]
  - `putScenes(String, Scene [], int)`

#### Parameters

| Name | Description |
| --- | --- |
| promise | `Promise<SceneState>` 接口实例，详见 `Promise`。你可以通过该接口获取 `getSceneState` 的调用结果：<br />- 如果方法调用成功，则返回场景状态，详见 `SceneState`。<br />- 如果方法调用失败，则返回错误信息。 |

<a id="a5a9590e60683101fc8715c606180cd19"></a>

### getScenes() [1/2]

```cpp
Scene[] com.herewhite.sdk.Room.getScenes()
```

获取房间当前场景目录下的场景列表。

##### `自从`

2.4.0

##### `注解`

- 该方法为同步调用。
- 调用以下方法修改或新增场景后，无法通过 `getScenes` [1/2] 立即获取最新的场景列表。 此时，如果需要立即获取最新的场景列表，可以调用 `getScenes` [2/2]。

  - `setScenePath` [1/2]
  - `setScenePath` [2/2]
  - `putScenes`

#### Returns

当前场景目录下的场景列表，详见 `Scene`。

<a id="a9d9265ad1fb0e698e0c8b281dfbd66f1"></a>

### getScenes() [2/2]

```cpp
void com.herewhite.sdk.Room.getScenes(final Promise<Scene[]> promise)
```

获取房间当前场景目录下的场景列表。

##### `注解`

- 该方法为异步调用。
- 调用以下方法修改或新增场景后，可以调用 `getScenes`，立即获取最新的场景列表。

  - `setScenePath` [1/2]
  - `setScenePath` [2/2]
  - `putScenes`

#### Parameters

| Name | Description |
| --- | --- |
| promise | `Promise<Scene []>` 接口实例，详见 `Promise`。你可以通过该接口获取 `getScenes` 的调用结果：<br />- 如果方法调用成功，则返回场景列表，详见 `Scene`。<br />- 如果方法调用失败，则返回错误信息。 |

<a id="ac287ed3712b957412281f1330f7df32c"></a>

### getZoomScale() [1/2]

```cpp
double com.herewhite.sdk.Room.getZoomScale()
```

获取用户当前的视角缩放比例。

##### `自从`

2.4.0

##### `**[弃用:](/zh-CN/api-reference/whiteboard/android/deprecated#_deprecated000016)**`

该方法已废弃。

##### `注解`

- 该方法为同步调用。
- 调用 `zoomChange` 或 `moveCamera` 调整视角缩放比例后，无法通过 `getZoomScale` [1/2] 立即获取最新的缩放比例。 此时，如果需要立即获取最新的缩放比例，可以调用 `getZoomScale` [2/2]。

#### Returns

视角缩放比例。

<a id="aff289f9e1bac68426d93fdfd8f7486e7"></a>

### getZoomScale() [2/2]

```cpp
void com.herewhite.sdk.Room.getZoomScale(final Promise<Number > promise)
```

获取当前用户的视角缩放比例。

##### `**[弃用:](/zh-CN/api-reference/whiteboard/android/deprecated#_deprecated000017)**`

该方法已废弃。

##### `注解`

- 该方法为异步调用。
- 调用 `zoomChange` 或 `moveCamera` 调整视角缩放比例后，如果需要立即获取最新的缩放比例，可以调用 `getZoomScale` [2/2]。

#### Parameters

| Name | Description |
| --- | --- |
| promise | `Promise<Number>` 接口实例，详见 `Promise`。你可以通过该接口获取 `getZoomScale` 的调用结果：<br />- 如果方法调用成功，则返回视角缩放比例。<br />- 如果方法调用失败，则返回错误信息。 |

<a id="a2c0ef694235dccda2fb92b3ba141880d"></a>

### getRoomPhase() [1/2]

```cpp
RoomPhase com.herewhite.sdk.Room.getRoomPhase()
```

获取房间的连接状态。

##### `自从`

2.4.0

##### `注解`

- 该方法为同步调用。
- 调用 `disconnect` [1/2] 或 `disconnect` [2/2] 断开 SDK 与实时房间的连接后，无法立即通过 `getRoomPhase` [1/2] 获取最新的房间连接状态。 此时，你可以调用 `getRoomPhase` [2/2] 立即获取最新的房间连接状态。

#### Returns

房间的连接状态，详见 `RoomPhase`。

<a id="a6759ad742065c0e82592e8a0ed8aabee"></a>

### getRoomPhase() [2/2]

```cpp
void com.herewhite.sdk.Room.getRoomPhase(final Promise<RoomPhase > promise)
```

获取房间的连接状态。

##### `注解`

- 该方法为异步调用。
- 调用 `disconnect` [1/2] 或 `disconnect` [2/2] 断开 SDK 与实时房间的连接后，无法立即通过 `getRoomPhase` [1/2] 获取最新的房间连接状态。 此时，你可以调用 `getRoomPhase` [2/2] 立即获取最新的房间连接状态。

#### Parameters

| Name | Description |
| --- | --- |
| promise | `Promise<RoomPhase>` 接口实例，详见 `Promise`。你可以通过该接口获取 `getRoomPhase` 的调用结果：<br />- 如果方法调用成功，则返回房间连接状态，详见 `RoomPhase`。<br />- 如果方法调用失败，则返回错误信息。 |

<a id="a9578a9ae744eb64d8bd939e3af3b68db"></a>

### getRoomState() [1/2]

```cpp
RoomState com.herewhite.sdk.Room.getRoomState()
```

获取房间的所有状态。

##### `自从`

2.4.0

##### `注解`

- 该方法为同步调用。
- 修改房间的状态属性后，无法立即通过 `getRoomState` [1/2] 获取最新的房间状态。 此时，如果需要立即获取最新的房间状态，可以调用 `getRoomState` [2/2] 获取。

#### Returns

房间当前的所有状态，详见 `RoomState`。

<a id="a7c00f3eb5374d153b594fb772cf7339f"></a>

### getRoomState() [2/2]

```cpp
void com.herewhite.sdk.Room.getRoomState(final Promise<RoomState > promise)
```

获取房间的所有状态。

##### `注解`

- 该方法为异步调用。
- 修改房间的状态属性后，无法立即通过 `getRoomState` 获取最新的房间状态。此时，如果需要立即获取最新的房间状态，可以调用 `getRoomState` [2/2] 获取。

#### Parameters

| Name | Description |
| --- | --- |
| promise | `Promise<RoomState>` 接口实例，详见 `Promise`。你可以通过该接口获取 `getRoomState` 的调用结果：<br />- 如果方法调用成功，则返回房间所有状态，详见 `RoomState`。<br />- 如果方法调用失败，则返回错误信息。 |

<a id="a4dfb1c0aac90cd2678aa773629198b1e"></a>

### setScenePath() [1/2]

```cpp
void com.herewhite.sdk.Room.setScenePath(String path)
```

切换至指定的场景。

方法调用成功后，房间内的所有用户看到的白板都会切换到指定场景。

##### `注解`

- 该方法为同步调用。
- 如需获取方法调用回调，请使用 `setScenePath` [2/2]。

场景切换失败可能有以下原因：

- 路径不合法，请确保场景路径以 `/` 开头，由场景目录和场景名构成。
- 场景路径对应的场景不存在。
- 传入的路径是场景目录的路径，而不是场景路径。

#### Parameters

| Name | Description |
| --- | --- |
| path | 想要切换到的场景的场景路径，请确保场景路径以 `/` 开头，由场景目录和场景名构成，例如， `/math/classA`. |

<a id="afb89dfcb412caf3377c75522aa3ab6c0"></a>

### setScenePath() [2/2]

```cpp
void com.herewhite.sdk.Room.setScenePath(String path, final Promise<Boolean > promise)
```

切换至指定的场景。

方法调用成功后，房间内的所有用户看到的白板都会切换到指定场景。

##### `注解`

该方法为异步调用。

场景切换失败可能有以下原因：

- 路径不合法，请确保场景路径以 "/"，由场景目录和场景名构成。
- 场景路径对应的场景不存在。
- 传入的路径是场景目录的路径，而不是场景路径。

#### Parameters

| Name | Description |
| --- | --- |
| path | 想要切换到的场景的场景路径，请确保场景路径以 "/"，由场景目录和场景名构成，例如， `/math/classA`. |
| promise | `Promise<Boolean>` 接口，详见 `Promise`。你可以通过该接口获取 `setScenePath` 的调用结果：<br />- 如果方法调用成功，则返回 `true`.<br />- 如果方法调用失败，则返回错误信息。 |

<a id="a003fc53c0744799200fbac3ac6128e1a"></a>

### setSceneIndex()

```cpp
void com.herewhite.sdk.Room.setSceneIndex(Integer index, @Nullable final Promise<Boolean > promise)
```

切换至当前场景目录下的指定场景。

方法调用成功后，房间内的所有用户看到的白板都会切换到指定场景。 指定的场景必须在当前场景目录中，否则，方法调用会失败。

#### Parameters

| Name | Description |
| --- | --- |
| index | 目标场景在当前场景目录下的索引号。 |
| promise | `Promise<Boolean>` 接口，详见 `Promise`。你可以通过该接口获取 `setSceneIndex` 的调用结果：<br />- 如果方法调用成功，则返回 `true`。<br />- 如果方法调用失败，则返回错误信息。 |

<a id="ae2b7a63167b67f28d6a689d618184e4d"></a>

### putScenes()

```cpp
void com.herewhite.sdk.Room.putScenes(String dir, Scene[] scenes, int index)
```

在指定场景目录下插入多个场景。

##### `注解`

调用该方法插入多个场景后不会切换到新插入的场景。如果要切换至新插入的场景，需要调用 `setScenePath`。

**示例代码**

```text

room.putScenes("ppt", new Scene[]{new Scene("page1", new PptPage("https://white-pan.oss-cn-shanghai.aliyuncs.com/101/image/alin-rusu-1239275-unsplash_opt.jpg", 1024d, 768d))}, 0);
room.setScenePath("ppt" + "/page1");
```

#### Parameters

| Name | Description |
| --- | --- |
| dir | 场景目录的名称，必须以 `/` 开头。不能为场景路径。 |
| scenes | 由多个场景构成的数组。单个场景的字段详见 `Scene`。 |
| index | 待插入的多个场景中，第一个场景在该场景目录的索引号。如果传入的索引号大于该场景目录已有场景总数，新插入的场景会排在现有场景的最后。场景的索引号从 0 开始。 |

<a id="a7361194cdc471d597921c14d2096e145"></a>

### moveScene()

```cpp
void com.herewhite.sdk.Room.moveScene(String sourcePath, String targetDirOrPath)
```

移动场景。

成功移动场景后，场景路径也会改变。

##### `注解`

- 该方法只能移动场景，不能移动场景目录，即 `sourcePath` 只能是场景路径，不能是场景目录路径。
- 该方法支持改变指定场景在当前所属场景目录下的位置，也支持将指定场景移至其他场景目录。

#### Parameters

| Name | Description |
| --- | --- |
| sourcePath | 需要移动的场景原路径。必须为场景路径，不能是场景目录的路径。 |
| targetDirOrPath | 目标场景目录的路径或目标场景的路径：<br />- 当 `targetDirOrPath` 设置为场景目录时，表示将指定场景移至其他场景目录中，场景路径会发生改变，但是场景名称不变。<br />- 当 `targetDirOrPath` 设置为场景时，表示改变指定场景在当前场景目录的位置，场景路径和场景名都会发生改变。 |

<a id="affd9982f863d1e91168428fa2df1053a"></a>

### removeScenes()

```cpp
void com.herewhite.sdk.Room.removeScenes(String dirOrPath)
```

删除场景或者场景目录。

##### `注解`

- 互动白板实时房间内必须至少有一个场景。当删除所有的场景后，SDK 会自动生成一个路径为 `/init` 初始场景（房间初始化时的默认场景）。
- 如果删除白板当前所在场景，白板会展示被删除场景所在场景目录的最后一个场景
- 如果删除的是场景目录，则该场景目录下的所有场景都会被删除。
- 如果删除的是当前场景所在的场景目录，例如 `dirA`，SDK 会执行向上递归逻辑选择新的场景作为当前场景，规则如下：

  1. 如果当前场景目录路径下还有其他场景目录，例如 `dirB`，排在被删除的场景目录 `dirA` 后面，则将场景切换至 `dirB` 中的第一个场景（index 为 0）。
  2. 如果当前场景目录路径下 `dirA` 后不存在场景目录，则查看当前场景目录路径下是否存在场景； 如果存在，则将场景切换至当前场景目录路径下的第一个场景（index 为 0）。
  3. 如果当前场景目录路径下 `dirA` 后没有场景目录，也不存在任何场景，则查看 `dirA` 前面是否存在场景目录 `dirC`；如果存在，则选择 `dirC` 中的第一个场景（index 为 0）。 SDK 会继续向上递归执行该逻辑，直到找到新的场景。

#### Parameters

| Name | Description |
| --- | --- |
| dirOrPath | 场景目录路径或者场景路径。如果传入的是场景目录，则会删除该场景目录下的所有场景。 |

<a id="ad051bd3599948c161bcb2c983d80ef5a"></a>

### cleanScene()

```cpp
void com.herewhite.sdk.Room.cleanScene(boolean retainPpt)
```

清除当前场景的所有内容。

#### Parameters

| Name | Description |
| --- | --- |
| retainPpt | 是否保留 PPT 内容：<br />- `true`：保留 PPT。<br />- `false`：连 PPT 一起清空。 |

<a id="a2f0b17c6af0f8c2bcaef84ad600e51af"></a>

### pptNextStep()

```cpp
void com.herewhite.sdk.Room.pptNextStep()
```

播放动态 PPT 下一页。

##### `自从`

2.2.0

当前 PPT 页面的动画已全部执行完成时，SDK 会将场景切换至下一页 PPT。

<a id="aa54cbba71f9d37d3f00809337015fe03"></a>

### pptPreviousStep()

```cpp
void com.herewhite.sdk.Room.pptPreviousStep()
```

返回动态 PPT 上一页。

##### `自从`

2.2.0

当前 PPT 页面的动画全部回退完成时，SDK 会将场景切回至上一页 PPT。

<a id="a045fabaff4ea72af8ef72c04fa070225"></a>

### removePage() [1/2]

```cpp
void com.herewhite.sdk.Room.removePage(@Nullable Promise<Boolean > promise)
```

删除当前页面，多窗口下限定为主白板场景集当前页面。

##### `自从`

2.16.23

<a id="a70ef00a6a33a306cab048919218b8f34"></a>

### removePage() [2/2]

```cpp
void com.herewhite.sdk.Room.removePage(int index, @Nullable Promise<Boolean > promise)
```

删除当前场景集指定页面，多窗口下限定为主白板场景集下页面

##### `自从`

2.16.23

#### Parameters

| Name | Description |
| --- | --- |
| index | 指定页面索引号。 |

<a id="a81fddb80f2f65a70f2f5fe1e5652a5d0"></a>

### zoomChange()

```cpp
void com.herewhite.sdk.Room.zoomChange(double scale)
```

更新视角的缩放比例。

##### `**[弃用:](/zh-CN/api-reference/whiteboard/android/deprecated#_deprecated000018)**`

该方法已经废弃。请使用 `moveCamera(CameraConfig)`。

#### Parameters

| Name | Description |
| --- | --- |
| scale | 视角的缩放比例。 |

<a id="a78cb9ef60e7a4e61696dfc5e8301d064"></a>

### debugInfo()

```cpp
void com.herewhite.sdk.Room.debugInfo(final Promise<JSONObject > promise)
```

获取调试日志信息。

##### `自从`

2.6.2

#### Parameters

| Name | Description |
| --- | --- |
| promise | `Promise<JSONObject>` 接口实例，详见 `Promise`。你可以通过该接口获取 `debugInfo` 的调用结果：<br />- 如果方法调用成功，则返回调试日志信息。<br />- 如果方法调用失败，则返回错误信息。 |

<a id="a6b265bdee91f2d961373d172bb969fe0"></a>

### disableOperations()

```cpp
void com.herewhite.sdk.Room.disableOperations(final boolean disableOperations)
```

允许/禁止白板响应用户任何操作。

该方法设置是否禁止白板响应用户的操作，包括：

- `CameraTransform`：移动、缩放视角。
- `DeviceInputs`：使用白板工具输入。

#### Parameters

| Name | Description |
| --- | --- |
| disableOperations | 允许/禁止白板响应用户任何操作。<br />- `true`：不响应用户操作。<br />- `false`：（默认）响应用户操作。 |

<a id="a1cea527622d0cdf3d6eb0b01835b1370"></a>

### setWritable()

```cpp
void com.herewhite.sdk.Room.setWritable(final boolean writable, @Nullable final Promise<Boolean > promise)
```

设置用户在房间中是否为互动模式。

##### `自从`

2.6.1

#### Parameters

| Name | Description |
| --- | --- |
| writable | 用户在房间中是否为互动模式：<br />- `true`：互动模式，即具有读写权限。<br />- `false`：订阅模式，即具有只读权限。 |
| promise | `Promise<Boolean>` 接口实例，详见 `Promise`。你可以通过该接口获取 `setWritable` 的调用结果：<br />- 如果方法调用成功，则返回用户在房间中的读写状态。<br />- 如果方法调用失败，则返回错误信息。 |

<a id="a4f860e53b4cab0f574b22a2e8fdf235d"></a>

### disableEraseImage()

```cpp
void com.herewhite.sdk.Room.disableEraseImage(boolean disable)
```

关闭/开启橡皮擦擦除图片功能。

##### `自从`

2.9.3

#### Parameters

| Name | Description |
| --- | --- |
| disable | 是否关闭橡皮擦擦除图片功能：<br />- `true`：禁止橡皮擦擦除图片。<br />- `false`：（默认）允许橡皮擦擦除图片。 |

<a id="a0cadc59b5eb670a2361f5befedc90f4f"></a>

### disableCameraTransform()

```cpp
void com.herewhite.sdk.Room.disableCameraTransform(final boolean disableCameraTransform)
```

禁止/允许用户调整（移动或缩放）视角。

##### `自从`

2.2.0

#### Parameters

| Name | Description |
| --- | --- |
| disableCameraTransform | 是否禁止用户调整视角：<br />- `true`：禁止用户调整视角。<br />- `false`：（默认）允许用户调整视角。 |

<a id="a468b0fa46f8cea857e6857b9f2945101"></a>

### disableDeviceInputs()

```cpp
void com.herewhite.sdk.Room.disableDeviceInputs(final boolean disableOperations)
```

禁止/允许用户操作白板工具。

##### `自从`

2.2.0

#### Parameters

| Name | Description |
| --- | --- |
| disableOperations | 是否禁止用户操作白板工具：<br />- `true`：禁止用户操作白板工具操作。<br />- `false`：（默认）允许用户操作白板工具输入操作。 |

<a id="ae4fe2b66cc439a1f3822728a1a92ff94"></a>

### setTimeDelay()

```cpp
void com.herewhite.sdk.Room.setTimeDelay(double delaySec)
```

设置本地显示远端白板内容的延时。

调用该方法后，当本地用户接收到远端白板内容时，SDK 会根据你设置的 `timeDelay` 的值延迟显示白板内容。

在音视频传输延时较大的场景中，如使用 CDN 推送音视频流时，你可以使用该参数延迟显示接收到的远端白板内容，以确保白板内容与音视频同步。

#### Parameters

| Name | Description |
| --- | --- |
| delaySec | 延时时长，单位为秒。取值必须大于等于 0。默认值为 0。 |

<a id="ac35dafcef0ed29d406a29d7fccf0e7e1"></a>

### getTimeDelay()

```cpp
Double com.herewhite.sdk.Room.getTimeDelay()
```

获取本地显示远端白板内容的延时。

#### Returns

延时时长，单位为秒。

<a id="ae5fd629c651259844aed2869eeaa966d"></a>

### syncBlockTimestamp()

```cpp
void com.herewhite.sdk.Room.syncBlockTimestamp(long utcMs)
```

设置本地显示远端白板内容的 Unix 时间戳（ms）。

##### `自从`

2.12.26

调用该方法后，SDK 会根据你设置的 `timestamp` 的值显示接收到的远端白板内容。

在同时订阅音视频流和白板内容的场景中，你可以从音视频流附带的 SEI 帧中获取时间信息， 并调用该方法设置远端白板内容在本地的显示时间，以确保音视频流和白板内容实时同步。

#### Parameters

| Name | Description |
| --- | --- |
| utcMs | 远端白板内容在本地显示的 Unix 时间戳，单位为毫秒。 |

<a id="ae1518bd8a09077367b2b4d21e32c00d1"></a>

### dispatchMagixEvent()

```cpp
void com.herewhite.sdk.Room.dispatchMagixEvent(AkkoEvent eventEntry)
```

发送自定义事件。

##### `注解`

所有注册监听该事件的用户都会收到通知。

#### Parameters

| Name | Description |
| --- | --- |
| eventEntry | 自定义事件内容，详见 `AkkoEvent`。 |

<a id="a45539d0e6e928c2eaa16d97ba50717bd"></a>

### addApp()

```cpp
void com.herewhite.sdk.Room.addApp(WindowAppParam appParam, Promise<String > promise)
```

插入小窗应用。

多窗口模式下，PPT 或自定义插件等可以作为小窗应用插入白板，在一个新的窗口中展示。

##### `注解`

多次插入同一个小窗应用会导致插入失败，返回的 `appId` 为 `nil`。

#### Parameters

| Name | Description |
| --- | --- |
| appParam | 小窗应用属性，详见 `WindowAppParam`。 |
| promise | `Promise<String>` 接口实例，详见 `Promise`。你可以通过该接口获取方法调用结果：<br />- 如果方法调用成功，则返回小窗应用 ID。<br />- 如果方法调用失败，则返回错误信息。 |

<a id="a9039ca655718cf4ae83788e9cb3f880e"></a>

### closeApp()

```cpp
void com.herewhite.sdk.Room.closeApp(String appId, Promise<Boolean > promise)
```

关闭指定小窗应用的窗口。

该方法仅在多窗口模式下有效, 无论应用 ID 是否有效都会触发回调。

#### Parameters

| Name | Description |
| --- | --- |
| appId | 小窗应用 ID。 |
| promise | `Promise<Boolean>` 接口实例，详见 `Promise`。你可以通过该接口获取方法调用结果：<br />- 如果方法调用成功，则窗口关闭。<br />- 如果方法调用失败，则返回错误信息。 |

<a id="afb5c3bcd1384aedd25821f9450fa403c"></a>

### focusApp()

```cpp
void com.herewhite.sdk.Room.focusApp(String appId)
```

切换聚焦窗口为指定小窗应用的窗口。

该方法仅在多窗口模式下有效。

#### Parameters

| Name | Description |
| --- | --- |
| appId | 小窗应用 ID。 |

<a id="a4716222443f7501be43292b5a8682aa3"></a>

### queryApp()

```cpp
void com.herewhite.sdk.Room.queryApp(String appId, Promise<WindowAppSyncAttrs > promise)
```

查询指定小窗应用的信息。

该方法仅在多窗口下有效。

- 如果方法调用成功，则返回小窗应用相关信息，详见 `WindowAppSyncAttrs`。
- 如果方法调用失败，则返回错误信息。

#### Parameters

| Name | Description |
| --- | --- |
| appId | 小窗应用 ID。 |
| promise | `Promise<Object>` 接口实例，详见 `Promise`。你可以通过该接口获取方法调用结果： |

<a id="a1ca46777a002ee9167f669f2bbcc0de7"></a>

### queryAllApps()

```cpp
void com.herewhite.sdk.Room.queryAllApps(Promise<Map<String, WindowAppSyncAttrs > > promise)
```

查询所有小窗应用的信息。

该方法仅在多窗口下有效。

- 如果方法调用成功，则返回一个以应用 ID 为键，包含所有小窗应用信息的字典。详见 `WhiteAppSyncAttributes`。
- 如果方法调用失败，则返回错误信息。

#### Parameters

| Name | Description |
| --- | --- |
| promise | `Promise<Object>` 接口实例，详见 `Promise`。你可以通过该接口获取方法调用结果： |

<a id="a4e7b20d12d67517e01a02be0e3e50a4c"></a>

### dispatchDocsEvent()

```cpp
void com.herewhite.sdk.Room.dispatchDocsEvent(WindowDocsEvent docsEvent, Promise<Boolean > promise)
```

发送文档操作事件。

#### Parameters

| Name | Description |
| --- | --- |
| docsEvent | 事件内容，详见 `WindowDocsEvent`。 |
| promise | `Promise<Boolean>` 接口，详见 `Promise`。你可以通过该接口获取 `dispatchDocsEvent` 的调用结果：<br />- 如果方法调用成功，则返回 `true`.<br />- 如果方法调用失败，则返回错误信息。 |

<a id="a6c417c79b6bccd7eb07b88b04ef8c4eb"></a>

### setSyncMode()

```cpp
void com.herewhite.sdk.Room.setSyncMode(boolean useSyncMode)
```

默认情况下，所有同步消息会按比较平滑的速度处理，以此保证观感的流畅性。 如果设置此项为 `true`，则所有消息一经收到立刻处理，从而保证同步的实时性。

#### Parameters

| Name | Description |
| --- | --- |
| useSyncMode | 是否开启同步模式：<br />- `true`：开启。<br />- `false`：关闭。 |

<a id="a42157e1f6d64d9676c17b3b94c1c5bbe"></a>

### isDisableInitialStateCallback()

```cpp
boolean com.herewhite.sdk.Room.isDisableInitialStateCallback()
```

获取是否禁止初始化状态回调。

<a id="ad2a99dc3b812a11ddede1387edd918c1"></a>

### setDisableInitialStateCallback()

```cpp
void com.herewhite.sdk.Room.setDisableInitialStateCallback(boolean disableInitialStateCallback)
```

禁止初始化状态回调。

#### Parameters

| Name | Description |
| --- | --- |
| disableInitialStateCallback | - `true`：禁止。<br />- `false`：（默认）不禁止。 |
