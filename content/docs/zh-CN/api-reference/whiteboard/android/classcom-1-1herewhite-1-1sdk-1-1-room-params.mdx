---
title: "com.herewhite.sdk.RoomParams类 参考"
description: "RoomParams 类，用于配置实时房间的参数。"
---

`RoomParams` 类，用于配置实时房间的参数。

### `注解`

`RoomParams` 类中所有的方法都必须在 `joinRoom` 前调用；成功加入房间后，调用该类中的任何方法都不会生效。

## 构造及析构函数说明

<a id="ada87eefb4b25310cfa6df81038417f58"></a>

### RoomParams() [1/3]

```cpp
com.herewhite.sdk.RoomParams.RoomParams(String uuid, String roomToken, String uid)
```

初始化房间配置参数。

#### Parameters

| Name | Description |
| --- | --- |
| uuid | 房间 UUID， 即房间唯一标识符。传入的房间 UUID 必须和生成 [Room](/zh-CN/api-reference/whiteboard/android/classcom-1-1herewhite-1-1sdk-1-1-room) Token 时填入的房间 UUID 一致。 |
| roomToken | 用于鉴权的 [Room](/zh-CN/api-reference/whiteboard/android/classcom-1-1herewhite-1-1sdk-1-1-room) Token。 |
| uid | 自从 v2.15.0。用户唯一标识符，字符串格式，长度不能超过 1024 字节。如果你使用 2.15.0 及之后版本的 SDK，必须传入该参数。 请确保同一房间内每个用户 `uid` 的唯一性。 |

<a id="a5aa8c28b510f899e97cf2b26c56e5f48"></a>

### RoomParams() [2/3]

```cpp
com.herewhite.sdk.RoomParams.RoomParams(String uuid, String roomToken, String uid, MemberInformation memberInfo)
```

初始化房间配置参数并传入用户信息。

##### `**[弃用:](/zh-CN/api-reference/whiteboard/android/deprecated#_deprecated000021)**`

该方法已经废弃。请使用 `RoomParams` [3/3]。

#### Parameters

| Name | Description |
| --- | --- |
| uuid | 房间 UUID， 即房间唯一标识符。传入的房间 UUID 必须和生成 [Room](/zh-CN/api-reference/whiteboard/android/classcom-1-1herewhite-1-1sdk-1-1-room) Token 时填入的房间 UUID 一致。 |
| roomToken | 用于鉴权的 [Room](/zh-CN/api-reference/whiteboard/android/classcom-1-1herewhite-1-1sdk-1-1-room) Token。 |
| uid | 自从 v2.15.0。用户唯一标识符，字符串格式，长度不能超过 1024 字节。如果你使用 2.15.0 及之后版本的 SDK，必须传入该参数。 请确保同一房间内每个用户 `uid` 的唯一性。 |
| memberInfo | 自定义用户信息，详见 `MemberInformation`。 |

<a id="a625545247367efb54b7d799dd19bffc2"></a>

### RoomParams() [3/3]

```cpp
com.herewhite.sdk.RoomParams.RoomParams(String uuid, String roomToken, String uid, Object userPayload)
```

初始化房间配置参数并传入自定义的用户信息。

##### `自从`

2.0.0

#### Parameters

| Name | Description |
| --- | --- |
| uuid | 房间 UUID， 即房间唯一标识符。传入的房间 UUID 必须和生成 [Room](/zh-CN/api-reference/whiteboard/android/classcom-1-1herewhite-1-1sdk-1-1-room) Token 时填入的房间 UUID 一致。 |
| roomToken | 用于鉴权的 [Room](/zh-CN/api-reference/whiteboard/android/classcom-1-1herewhite-1-1sdk-1-1-room) Token。 |
| uid | 自从 v2.15.0。用户唯一标识符，字符串格式，长度不能超过 1024 字节。如果你使用 2.15.0 及之后版本的 SDK，必须传入该参数。 请确保同一房间内每个用户 `uid` 的唯一性。 |
| userPayload | 自定义用户信息，必须为 `WhiteObject` 子类。 |

## 成员函数说明

<a id="affd817d704b87cee8c9aa38697d459c7"></a>

### setRegion()

```cpp
void com.herewhite.sdk.RoomParams.setRegion(Region region)
```

设置数据中心。

##### `注解`

- 该方法设置的数据中心必须与要加入的互动白板实时房间所在数据中心一致，否则无法加入房间。
- 该方法与 `WhiteSdkConfiguration` 类中的 `setRegion` 方法作用相同，两个方法只需要调用其中的一个。如果同时调用，该方法会覆盖 `WhiteSdkConfiguration` 类中的 `setRegion`。

#### Parameters

| Name | Description |
| --- | --- |
| region | 数据中心，详见 `Region`。 |

<a id="a7b068e0e3ab5e20b3ba216d2e6156124"></a>

### getRegion()

```cpp
Region com.herewhite.sdk.RoomParams.getRegion()
```

获取设置的数据中心。

#### Returns

设置的数据中心，详见 `Region`。

<a id="a46ee2c4da1611653b0d20e5080bc7636"></a>

### isWritable()

```cpp
boolean com.herewhite.sdk.RoomParams.isWritable()
```

获取用户是否以互动模式加入白板房间。

#### Returns

用户是否以互动模式加入白板房间：

- `true`：以互动模式加入白板房间，即具有读写权限。
- `false`：以订阅模式加入白板房间，即具有只读权限。

<a id="a9dbc3e05cc1229492c3f91ce48b305cd"></a>

### setWritable()

```cpp
void com.herewhite.sdk.RoomParams.setWritable(boolean writable)
```

设置用户是否以互动模式加入白板房间。

用户可以以以下模式加入互动白板实时房间：

- 互动模式：对白板具有读写权限，会出现在房间的成员列表中，对其他用户可见。
- 订阅模式：对白板具有只读权限，不会出现在房间的成员列表中，对其他用户不可见。

#### Parameters

| Name | Description |
| --- | --- |
| writable | 用户是否以互动模式加入白板房间：<br />- `true`：（默认）以互动模式加入白板房间。<br />- `false`：以订阅模式加入白板房间。 |

<a id="a35f2724130ce493f7bbbdaa8665e28de"></a>

### getDisableEraseImage()

```cpp
boolean com.herewhite.sdk.RoomParams.getDisableEraseImage()
```

获取是否关闭橡皮擦擦除图片功能。

#### Returns

是否关闭橡皮擦擦除图片功能：

- `true`：橡皮擦不可以擦除图片。
- `false`：橡皮擦可以擦除图片。

<a id="a580315c5bf3976be29a97acb90261331"></a>

### setDisableEraseImage()

```cpp
void com.herewhite.sdk.RoomParams.setDisableEraseImage(boolean disableEraseImage)
```

设置是否关闭橡皮擦擦除图片功能。

默认情况下，橡皮擦可以擦除白板上的所有内容，包括图片。你可以调用 `setDisableEraseImage(true)` 设置橡皮擦不能擦除图片。

#### Parameters

| Name | Description |
| --- | --- |
| disableEraseImage | 是否关闭橡皮擦擦除图片功能：<br />- `true`：橡皮擦不可以擦除图片。<br />- `false`：（默认）橡皮擦可以擦除图片。 |

<a id="a3f7054556c1f72ac03f754538b55e5c0"></a>

### setTimeout()

```cpp
void com.herewhite.sdk.RoomParams.setTimeout(long timeout, TimeUnit timeUnit)
```

设置加入房间的超时时间。

#### Parameters

| Name | Description |
| --- | --- |
| timeout | 超时时长，默认值为 45000 毫秒。 |
| timeUnit | 时长单位，默认值为毫秒 （`MILLISECONDS` ），取值详见 [TimeUnit](https://www.android-doc.com/reference/java/util/concurrent/TimeUnit.html)。 |

<a id="a02b980237de8d8e9d91b4ebc5bf63887"></a>

### isDisableDeviceInputs()

```cpp
boolean com.herewhite.sdk.RoomParams.isDisableDeviceInputs()
```

获取是否禁止白板工具响应用户输入。

#### Returns

是否禁止白板工具响应用户输入：

- `true`：禁止白板工具响应用户输入。
- `false`：允许白板工具响应用户输入。

<a id="a0be8c01908f2826302fb50ce5c98c0e1"></a>

### setDisableDeviceInputs()

```cpp
void com.herewhite.sdk.RoomParams.setDisableDeviceInputs(boolean disableDeviceInputs)
```

开启/禁止白板工具响应用户输入。

##### `自从`

2.5.0

#### Parameters

| Name | Description |
| --- | --- |
| disableDeviceInputs | 是否禁止白板工具响应用户输入：<br />- `true`：禁止白板工具响应用户输入。<br />- `false`：（默认）允许白板工具响应用户输入。 |

<a id="a94cc0f493d873a3ba55caffb70b0f0cf"></a>

### isDisableOperations()

```cpp
boolean com.herewhite.sdk.RoomParams.isDisableOperations()
```

获取是否禁止白板响应用户的操作。

#### Returns

是否禁止白板响应用户的操作。

- `true`：禁止白板响应用户的操作。
- `false`：允许白板响应用户的操作。

<a id="acf4a08b32a536804e91c4dd22f476a04"></a>

### setDisableOperations()

```cpp
void com.herewhite.sdk.RoomParams.setDisableOperations(boolean disableOperations)
```

允许/禁止白板响应用户任何操作。

##### `自从`

2.5.0

##### `**[弃用:](/zh-CN/api-reference/whiteboard/android/deprecated#_deprecated000020)**`

该方法已废弃。请使用 `setDisableDeviceInputs` 和 `setDisableCameraTransform`。

禁止白板响应用户任何操作后，用户无法使用白板工具输入内容，也无法对白板进行视角缩放和视角移动。

#### Parameters

| Name | Description |
| --- | --- |
| disableOperations | 是否禁止白板响应用户的操作：<br />- `true`：禁止白板响应用户的操作。<br />- `false`：（默认）允许白板响应用户的操作。 |

<a id="ac4ed37a313c3087cb24170ca6435ff2f"></a>

### isDisableBezier()

```cpp
boolean com.herewhite.sdk.RoomParams.isDisableBezier()
```

获取是否关闭贝塞尔曲线优化。

#### Returns

是否关闭贝塞尔曲线优化：

- `true`: 关闭贝塞尔曲线优化。
- `false`: 开启贝塞尔曲线优化。

<a id="a6190fcaaf5dbea2b394cf5c73c9fa411"></a>

### setDisableBezier()

```cpp
void com.herewhite.sdk.RoomParams.setDisableBezier(boolean disableBezier)
```

设置是否关闭贝塞尔曲线优化。

##### `自从`

2.5.0

#### Parameters

| Name | Description |
| --- | --- |
| disableBezier | 是否关闭贝塞尔曲线优化：<br />- `true`: 关闭贝塞尔曲线优化。<br />- `false`: （默认）开启贝塞尔曲线优化。 |

<a id="ade13f69996cb32fd94120c46d8b0af0c"></a>

### isDisableCameraTransform()

```cpp
boolean com.herewhite.sdk.RoomParams.isDisableCameraTransform()
```

获取是否禁止本地用户操作白板视角。

#### Returns

是否禁止本地用户操作白板视角：

- `true`：禁止本地用户操作白板视角。
- `false`：允许本地用户操作白板视角。

<a id="a891549135084e1b843281a099c2c0561"></a>

### setDisableCameraTransform()

```cpp
void com.herewhite.sdk.RoomParams.setDisableCameraTransform(boolean disableCameraTransform)
```

禁止/允许本地用户操作白板的视角，包括缩放和移动视角。

#### Parameters

| Name | Description |
| --- | --- |
| disableCameraTransform | 是否禁止本地用户操作白板视角：<br />- `true`：禁止本地用户操作白板视角。<br />- `false`：（默认）允许本地用户操作白板视角。 |

<a id="a3b6b4fd252c282c685e217109eeb60c5"></a>

### isDisableNewPencil()

```cpp
boolean com.herewhite.sdk.RoomParams.isDisableNewPencil()
```

获取是否关闭新铅笔工具。

#### Returns

是否关闭新铅笔工具：

- `true`: 关闭新铅笔工具。
- `false`: 开启新铅笔工具。

<a id="a49c41b2dfd21f764d6e3ff63ca09f19d"></a>

### setDisableNewPencil()

```cpp
void com.herewhite.sdk.RoomParams.setDisableNewPencil(boolean disableNewPencil)
```

关闭/开启新铅笔工具。

##### `自从`

2.12.2

##### `注解`

- 在 2.12.2 版本中， `setDisableNewPencil` 的默认值为 `false`，自 2.12.3 版本起， `setDisableNewPencil` 的默认值改为 `true`。
- 为正常显示笔迹，在开启新铅笔工具前，请确保该房间内的所有用户使用如下 SDK：

  - Android SDK 2.12.3 版或之后
  - iOS SDK 2.12.3 版或之后
  - Web SDK 2.12.5 版或之后

#### Parameters

| Name | Description |
| --- | --- |
| disableNewPencil | 是否关闭新铅笔工具：<br />- `true`: （默认）关闭新铅笔工具。关闭后，SDK 对铅笔工具（`pencil` ）应用旧版笔迹平滑算法。<br />- `false`: 开启新铅笔工具。开启后，SDK 对铅笔工具应用新版笔迹平滑算法，使书写笔迹更加流畅自然，并带有笔锋效果。 |

<a id="afd4fb99429b98be70014888b7a9286b6"></a>

### getCameraBound()

```cpp
CameraBound com.herewhite.sdk.RoomParams.getCameraBound()
```

获取视角边界。

#### Returns

视角边界。

<a id="a068607b4928ceef7265db739fa4e3cb8"></a>

### setCameraBound()

```cpp
void com.herewhite.sdk.RoomParams.setCameraBound(CameraBound cameraBound)
```

设置本地用户的视角边界。

##### `自从`

2.5.0

#### Parameters

| Name | Description |
| --- | --- |
| cameraBound | 视角边界，详见 `CameraBound`。 |

<a id="a9b6f02c4298339cc91ce8f88576f3049"></a>

### getUserPayload()

```cpp
Object com.herewhite.sdk.RoomParams.getUserPayload()
```

获取自定义用户信息。

#### Returns

自定义用户信息。

<a id="a17c8130727ea71cdeca6c3c2362cc0b7"></a>

### setUserPayload()

```cpp
void com.herewhite.sdk.RoomParams.setUserPayload(Object userPayload)
```

自定义用户信息。

##### `自从`

2.0.0

你可以在 `userPayload` 中传入自定义的用户信息，例如用户ID，昵称和头像，然后调用此方法将信息发送给应用程序。

##### `注解`

为确保传入的 `userPayload` 格式正确，必须为 `WhiteObject` 的子类。

#### Parameters

| Name | Description |
| --- | --- |
| userPayload | 自定义的用户信息，必须为 key-value 结构，例如， `"avatar", "https://example.com/user.png")`。 |

<a id="abb2f190ee04cbfcb95001ee3cd29c1d1"></a>

### getMemberInfo()

```cpp
MemberInformation com.herewhite.sdk.RoomParams.getMemberInfo()
```

获取自定义的用户信息。

##### `**[弃用:](/zh-CN/api-reference/whiteboard/android/deprecated#_deprecated000022)**`

该方法已废弃。请使用 `getUserPayload`。

#### Returns

自定义用户信息，详见 `MemberInformation`。

<a id="ac3d609dd67f195eb947fa4a19abcfa37"></a>

### setMemberInfo()

```cpp
void com.herewhite.sdk.RoomParams.setMemberInfo(MemberInformation memberInfo)
```

自定义用户信息。

##### `**[弃用:](/zh-CN/api-reference/whiteboard/android/deprecated#_deprecated000023)**`

该方法已废弃。请使用 `getUserPayload`。

#### Parameters

| Name | Description |
| --- | --- |
| memberInfo | 用户信息，详见 `MemberInformation`。 |

<a id="a0f9c1507ca1f9e7c35d0b11f6801eb60"></a>

### getUuid()

```cpp
String com.herewhite.sdk.RoomParams.getUuid()
```

获取房间 UUID。

#### Returns

房间 UUID，即房间的唯一标识符。

<a id="a59c71a74ef95003eb7fe1a3a1783c290"></a>

### setUuid()

```cpp
void com.herewhite.sdk.RoomParams.setUuid(String uuid)
```

设置房间 UUID。

#### Parameters

| Name | Description |
| --- | --- |
| uuid | 房间 UUID，即房间的唯一标识符。 |

<a id="a5752788874ff70d8e732c3105be828fb"></a>

### getRoomToken()

```cpp
String com.herewhite.sdk.RoomParams.getRoomToken()
```

获取 [Room](/zh-CN/api-reference/whiteboard/android/classcom-1-1herewhite-1-1sdk-1-1-room) Token。

#### Returns

[Room](/zh-CN/api-reference/whiteboard/android/classcom-1-1herewhite-1-1sdk-1-1-room) Token。

<a id="a6630be6ab3d486467c80ae7d8ee2c4a8"></a>

### setRoomToken()

```cpp
void com.herewhite.sdk.RoomParams.setRoomToken(String roomToken)
```

设置 [Room](/zh-CN/api-reference/whiteboard/android/classcom-1-1herewhite-1-1sdk-1-1-room) Token。

#### Parameters

| Name | Description |
| --- | --- |
| roomToken | 用于鉴权的 [Room](/zh-CN/api-reference/whiteboard/android/classcom-1-1herewhite-1-1sdk-1-1-room) Token。生成该 [Room](/zh-CN/api-reference/whiteboard/android/classcom-1-1herewhite-1-1sdk-1-1-room) Token 的房间 UUID 必须和上面传入的房间 UUID 一致。 |

<a id="a366cbea6a84e8c73e8425153fd40fe16"></a>

### setUseNativeWebSocket()

```cpp
void com.herewhite.sdk.RoomParams.setUseNativeWebSocket(boolean nativeWebSocket)
```

开启全链路加速功能。

Agora 互动白板服务集成了 [Agora 全链路加速（FPA）服务](https://docs.agora.io/cn/global-accelerator/agora_ga_overview?platform=All%20Platforms)。 集成 Agora Whiteboard SDK 后，你只需调用 `setUseNativeWebSocket(true)`，即可在互动白板应用中开启全链路加速服务，提升传输质量。

#### Parameters

| Name | Description |
| --- | --- |
| nativeWebSocket | 是否开启全链路加速：<br />- `true`：开启全链路加速。<br />- `false`：（默认）关闭全链路加速。 |

<a id="ad5a9e486b8d4729382f3e4b4d8895859"></a>

### isUseNativeWebSocket()

```cpp
boolean com.herewhite.sdk.RoomParams.isUseNativeWebSocket()
```

获取是否开启全链路加速功能。

#### Returns

是否开启全链路加速功能：

- `true`：开启。
- `false`：关闭。

<a id="a3f7bff7cbb7a7f7fb5e7b185a9bd8eac"></a>

### isUsingFloatBar()

```cpp
boolean com.herewhite.sdk.RoomParams.isUsingFloatBar()
```

获取浮动条功能是否已开启。

#### Returns

浮动条功能是否已开启：

- `true`：开启。
- `false`：关闭。

<a id="a62a0c199916003f9745a509081fc64ab"></a>

### setUsingFloatBar()

```cpp
void com.herewhite.sdk.RoomParams.setUsingFloatBar(boolean floatBar)
```

设置是否开启开启浮动条功能。

#### Parameters

| Name | Description |
| --- | --- |
| floatBar | 是否开启浮动条：<br />- `true`：开启浮动条功能。<br />- `false`：（默认）关闭浮动条功能。 |

<a id="aa8e1d1c2aa0e00f942d91c01e1bc751a"></a>

### getModulesOrigin()

```cpp
String com.herewhite.sdk.RoomParams.getModulesOrigin()
```

获取白板请求 modules 数据的地址。

#### Returns

方法调用成功时返回 modules 地址。

<a id="ab55b1761e938355bb3fecf6ea3418bb5"></a>

### setModulesOrigin()

```cpp
void com.herewhite.sdk.RoomParams.setModulesOrigin(String modulesOrigin)
```

设置白板请求 modules 数据的地址。 配置后不会请求白板默认地址。

白板渲染优化配置项。详见 `RoomOptimizeOptions`。

#### Parameters

| Name | Description |
| --- | --- |
| modulesOrigin | modules 地址。示例 [https://modules.example.com。](https://modules.example.com。) |
