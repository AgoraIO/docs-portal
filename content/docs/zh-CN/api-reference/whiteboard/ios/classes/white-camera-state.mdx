---
title: "WhiteCameraState Class Reference"
description: "视角状态。"
---

<a id="overview"></a>

视角状态。

## Methods

<a id="//api/name/centerX"></a>

### centerX

```objc
@property (nonatomic, strong) NSNumber *centerX
```

视角的中心点在白板坐标系（以白板初始化时的中心点为原点的坐标系）中的 X 轴坐标。初始值为 0。

<a id="//api/name/centerY"></a>

### centerY

```objc
@property (nonatomic, strong) NSNumber *centerY
```

视角的中心点在白板坐标系（以白板初始化时的中心点为原点的坐标系）中的 Y 轴坐标。初始值为 0。

<a id="//api/name/scale"></a>

### scale

```objc
@property (nonatomic, strong) NSNumber *scale
```

视角的缩放比例。
