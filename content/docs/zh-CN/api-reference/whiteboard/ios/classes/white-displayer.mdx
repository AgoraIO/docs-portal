---
title: "WhiteDisplayer Class Reference"
description: "该类为白板房间的基类。"
---

<a id="overview"></a>

该类为白板房间的基类。

## Methods

<a id="//api/name/)"></a>

### )

```objc
@property (nonatomic, strong) UIColor *__deprecated_msg ( "use WhiteboardView's backgroundColor property" )
```

该属性已废弃。 `WhiteboardView` 类继承自 `UIView` 类，你可以直接在 `WhiteboardView` 对象中调用 `UIView` 类的 `backgroundcolor` 属性修改白板背景色。详见 [backgroundcolor](https://developer.apple.com/documentation/uikit/uiview/1622591-backgroundcolor)。

<a id="//api/name/postIframeMessage:"></a>

### postIframeMessage:

```objc
- (void)postIframeMessage:(id)payload
```

向 iframe 插件发送的关键信息。

#### Parameters

| Name | Description |
| --- | --- |
| `payload` | 关键信息。 |

<a id="//api/name/getSceneFromScenePath:result:"></a>

### getSceneFromScenePath:result:

```objc
- (void)getSceneFromScenePath:(NSString *)scenePath result:(void ( ^ ) ( WhiteScene *_Nullable scene ))result
```

获取指定场景的信息。

#### Parameters

| Name | Description |
| --- | --- |
| `scenePath` | 场景的路径。请确保场景路径以 `/` 开头，并且由场景目录和场景名构成，例如， `/math/classA`。 |
| `result` | 方法调用结果回调。方法调用成功时，返回场景信息。详见 [WhiteScene](/zh-CN/api-reference/whiteboard/ios/classes/white-scene)。 |

<a id="//api/name/getScenePathType:result:"></a>

### getScenePathType:result:

```objc
- (void)getScenePathType:(NSString *)pathOrDir result:(void ( ^ ) ( WhiteScenePathType pathType ))result
```

查询场景路径类型。

#### Parameters

| Name | Description |
| --- | --- |
| `pathOrDir` | 场景路径类型。 |
| `result` | 回调。返回指定场景的路径类型，详见 [WhiteScenePathType](/zh-CN/api-reference/whiteboard/ios/constants/white-scene-path-type)。 |

#### Discussion

你可以在该方法中指定想要查询的场景路径，SDK 会返回该路径对应的场景类型。

<a id="//api/name/getEntireScenes:"></a>

### getEntireScenes:

```objc
- (void)getEntireScenes:(void ( ^ ) ( NSDictionary<NSString*,NSArray<WhiteScene*> *> *dict ))result
```

获取当前房间内所有场景的信息。

#### Parameters

| Name | Description |
| --- | --- |
| `result` | 回调。返回当前房间内所有场景的信息。 |

<a id="//api/name/addMagixEventListener:"></a>

### addMagixEventListener:

```objc
- (void)addMagixEventListener:(NSString *)eventName
```

注册自定义事件监听。

#### Parameters

| Name | Description |
| --- | --- |
| `eventName` | 想要监听的自定义事件名称。 |

#### Discussion

成功注册后，你可以接收到对应的自定义事件通知。

**Note:**

对于同名的自定义事件，SDK 仅支持触发一个回调。

<a id="//api/name/addHighFrequencyEventListener:fireInterval:"></a>

### addHighFrequencyEventListener:fireInterval:

```objc
- (void)addHighFrequencyEventListener:(NSString *)eventName fireInterval:(NSUInteger)millseconds
```

注册高频自定义事件监听。

#### Parameters

| Name | Description |
| --- | --- |
| `eventName` | 想要监听的自定义事件名称。 |
| `millseconds` | SDK 触发回调的频率，单位为毫秒。该参数最小值为 500 ms，如果设置为低于该值会被重置为 500 ms。 |

#### Discussion

成功注册后，你可以接收到对应的自定义事件通知。

**Note:**

对于同名的自定义事件，SDK 仅支持触发一个回调。

<a id="//api/name/removeMagixEventListener:"></a>

### removeMagixEventListener:

```objc
- (void)removeMagixEventListener:(NSString *)eventName
```

移除自定义事件监听。

#### Parameters

| Name | Description |
| --- | --- |
| `eventName` | 想要移除监听的自定义事件名称。 |

<a id="//api/name/refreshViewSize"></a>

### refreshViewSize

```objc
- (void)refreshViewSize
```

刷新白板的界面。

#### Discussion

当 `WhiteboardView` 出现改变时，需要手动调用该方法刷新白板的界面。

<a id="//api/name/convertToPointInWorld:result:"></a>

### convertToPointInWorld:result:

```objc
- (void)convertToPointInWorld:(WhitePanEvent *)point result:(void ( ^ ) ( WhitePanEvent *convertPoint ))result
```

转换白板上点的坐标。

#### Parameters

| Name | Description |
| --- | --- |
| `point` | 点在 iOS 坐标系中的坐标。详见 [WhitePanEvent](/zh-CN/api-reference/whiteboard/ios/classes/white-pan-event)。 |
| `result` | 回调。返回点在世界坐标系上的坐标，详见 [WhitePanEvent](/zh-CN/api-reference/whiteboard/ios/classes/white-pan-event)。 |

#### Discussion

该方法可以将 iOS 内部坐标系中的坐标转换为世界坐标系（以白板初始化时的中点为原点，横轴为 X 轴，正方向向右，纵轴为 Y 轴，正方向向下）坐标。

<a id="//api/name/setCameraBound:"></a>

### setCameraBound:

```objc
- (void)setCameraBound:(WhiteCameraBound *)cameraBound
```

设置视角边界。

#### Parameters

| Name | Description |
| --- | --- |
| `cameraBound` | 视角边界，详见 [WhiteCameraBound](/zh-CN/api-reference/whiteboard/ios/classes/white-camera-bound)。 |

<a id="//api/name/moveCamera:"></a>

### moveCamera:

```objc
- (void)moveCamera:(WhiteCameraConfig *)camera
```

调整视角。

#### Parameters

| Name | Description |
| --- | --- |
| `camera` | 视角的参数配置，详见 [WhiteCameraConfig](/zh-CN/api-reference/whiteboard/ios/classes/white-camera-config)。 |

#### Discussion

调用该方法后，SDK 会根据传入的参数调整视角。

<a id="//api/name/moveCameraToContainer:"></a>

### moveCameraToContainer:

```objc
- (void)moveCameraToContainer:(WhiteRectangleConfig *)rectangle
```

调整视角，以保证完整显示视觉矩形。

#### Parameters

| Name | Description |
| --- | --- |
| `rectangle` | 视觉矩形的参数设置，详见 [WhiteRectangleConfig](/zh-CN/api-reference/whiteboard/ios/classes/white-rectangle-config)。 |

<a id="//api/name/scalePptToFit:"></a>

### scalePptToFit:

```objc
- (void)scalePptToFit:(WhiteAnimationMode)mode
```

调整视角以保证完整显示 PPT 的内容。

#### Parameters

| Name | Description |
| --- | --- |
| `mode` | 视角调整时的动画模式，详见 [WhiteAnimationMode](/zh-CN/api-reference/whiteboard/ios/constants/white-animation-mode)。 |

#### Discussion

该操作为一次性操作。

#### Availability

2.5.1

**Note:**

- 如果当前用户已经调用 [setViewMode](/zh-CN/api-reference/whiteboard/ios/classes/white-room#//api/name/setViewMode:) 方法并设置为 `follower`，调用该方法可能造成当前用户与主播内容不完全一致。
- 如果没有插入 PPT，调用该方法不生效。

<a id="//api/name/scaleIframeToFit"></a>

### scaleIframeToFit

```objc
- (void)scaleIframeToFit
```

调整视角以保证完整显示 HTML5 课件。

#### Discussion

该方法为一次性操作。如果没有插入 HTML5 课件，调用该方法不生效。

#### Availability

2.12.5

**Note:**

如果当前用户已经调用 [setViewMode](/zh-CN/api-reference/whiteboard/ios/classes/white-room#//api/name/setViewMode:) 方法并设置为 `follower`，调用该方法可能造成当前用户与主播内容不完全一致。

<a id="//api/name/disableCameraTransform:"></a>

### disableCameraTransform:

```objc
- (void)disableCameraTransform:(BOOL)disable
```

禁止/允许用户调整视角。

#### Parameters

| Name | Description |
| --- | --- |
| `disable` | 是否禁止用户调整视角： `YES`: 禁止用户调整视角。 `NO`: (默认) 允许用户调整视角。 |

#### Availability

2.11.0

<a id="//api/name/getScenePreviewImage:completion:"></a>

### getScenePreviewImage:completion:

```objc
- (void)getScenePreviewImage:(NSString *)scenePath completion:(void ( ^ ) ( UIImage *_Nullable image ))completionHandler
```

获取特定场景的预览图。

#### Parameters

| Name | Description |
| --- | --- |
| `scenePath` | 场景路径。 |
| `completionHandler` | 返回指定场景的预览图。 |

<a id="//api/name/getSceneSnapshotImage:completion:"></a>

### getSceneSnapshotImage:completion:

```objc
- (void)getSceneSnapshotImage:(NSString *)scenePath completion:(void ( ^ ) ( UIImage *_Nullable image ))completionHandler
```

获取特定场景的截图。

#### Parameters

| Name | Description |
| --- | --- |
| `scenePath` | 场景路径。 |
| `completionHandler` | 方法调用结果： 如果方法调用成功，将返回指定场景的截图。 如果方法调用失败，将返回错误信息。 |

#### Discussion

**Note:**

- 如果场景中展示了图片或动态 PPT 幻灯片，请确保该图片或PPT幻灯片的存储服务器支持跨域资源共享； 否则，生成的截图中可能不会显示图片或 PPT 幻灯片。
- 为确保截图正常显示，声网推荐使用 [互动白板服务端截图功能](/doc/whiteboard/restful/whiteboard-sdk/restful-wb/operations/post-v5-rooms-uuid-screenshots)。

<a id="//api/name/getLocalSnapShotWithCompletion:"></a>

### getLocalSnapShotWithCompletion:

```objc
- (void)getLocalSnapShotWithCompletion:(void ( ^ ) ( UIImage *_Nullable image , NSError *_Nullable error ))completionHandler
```

获取当前的白板截图。

#### Parameters

| Name | Description |
| --- | --- |
| `completionHandler` | 方法调用结果： 如果方法调用成功，将返回指定场景的截图。 如果方法调用失败，将返回错误信息。 |

#### Discussion

**NOTE:** 该方法得到的截图尺寸为当前的白板视图大小。
