---
title: "AsyncModuleLoadMode"
description: "AsyncModuleLoadMode API 参考。"
---

异步模块的加载模式。

## 枚举成员

<a id="disablecache"></a>

### DisableCache

```ts title="TypeScript"
DisableCache: = "disableCache"
```

禁止缓存，每次加载模块的时候都从网上下载。

<a id="storeasbase64"></a>

### StoreAsBase64

```ts title="TypeScript"
StoreAsBase64: = "storeAsBase64"
```

将模块以 base64 字符串的形式缓存在 indexDB。

<a id="storeasblob"></a>

### StoreAsBlob

```ts title="TypeScript"
StoreAsBlob: = "storeAsBlob"
```

将模块以 blob 的形式缓存在 indexDB。
