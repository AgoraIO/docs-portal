---
title: "OriginCoordType"
description: "OriginCoordType API reference."
---

浮动画布生成的位置和绘制点的对应关系。

### `since`

2.16.48

根据绘制位置生成一个大小约为 256 x 256 px 的浮动画布，后续绘制不超过该画布的范围时，同步绘制笔迹的范围会限定在这个浮动画布中，而不是同步整个画布，从而降低性能消耗。

## Enumeration members

<a id="bottomcenter"></a>

### BottomCenter

```ts
BottomCenter: = 2
```

绘制点位于浮动画布的底部中间。

<a id="center"></a>

### Center

```ts
Center: = 5
```

绘制点位于浮动画布的中心点。

<a id="default"></a>

### Default

```ts
Default: = 0
```

默认为绘制点位于浮动画布的左上角。

<a id="leftbottom"></a>

### LeftBottom

```ts
LeftBottom: = 1
```

绘制点位于浮动画布的左下角。

<a id="leftcenter"></a>

### LeftCenter

```ts
LeftCenter: = 4
```

绘制点位于浮动画布的左侧居中。

<a id="lefttop"></a>

### LeftTop

```ts
LeftTop: = 7
```

绘制点位于浮动画布的左上角。

<a id="rightbottom"></a>

### RightBottom

```ts
RightBottom: = 3
```

绘制点位于浮动画布的右下角。

<a id="rightcenter"></a>

### RightCenter

```ts
RightCenter: = 6
```

绘制点位于浮动画布的右侧居中。

<a id="righttop"></a>

### RightTop

```ts
RightTop: = 9
```

绘制点位于浮动画布的右上角。

<a id="topcenter"></a>

### TopCenter

```ts
TopCenter: = 8
```

绘制点位于浮动画布的顶部中间。
