---
title: "Callbacks<CALLBACKS>"
description: "Callbacks<CALLBACKS> API reference."
---

回调。

- Callbacks
  - ScenesCallbacksNode

## Methods

<a id="forwardto"></a>

### forwardTo

```ts
forwardTo<NAME>(name: NAME, toCallbacks: Callbacks<CALLBACKS>): any
```

将注册的回调转到另一个 `Callbacks` 对象。

#### Parameters

| Name | Description |
| --- | --- |
| name: NAME | 注册回调的名称。 |
| toCallbacks: Callbacks&lt;CALLBACKS&gt; | 转到的 `Callbacks` 对象。 |

#### Returns

Returns any

<a id="off"></a>

### off

```ts
off<NAME>(name?: NAME, listener?: any): void
```

注销回调。

#### Parameters

| Name | Description |
| --- | --- |
| Optional name: NAME | 回调名。 |
| Optional listener: any | 回调函数。 |

#### Returns

Returns void

<a id="on"></a>

### on

```ts
on<NAME>(name: NAME, listener: any): void
```

注册一个回调。

#### Parameters

| Name | Description |
| --- | --- |
| name: NAME | 回调名。 |
| listener: any | 回调函数。 |

#### Returns

Returns void

<a id="once"></a>

### once

```ts
once<NAME>(name: NAME, listener: any): void
```

注册只使用一次的回调。

#### Parameters

| Name | Description |
| --- | --- |
| name: NAME | 回调名。 |
| listener: any | 回调函数。 |

#### Returns

Returns void
