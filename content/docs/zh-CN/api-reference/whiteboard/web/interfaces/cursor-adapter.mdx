---
title: "CursorAdapter"
description: "CursorAdapter API 参考。"
---

设置鼠标的光标适配器。

- CursorAdapter

## 属性

<a id="onaddedcursor"></a>

### onAddedCursor

```ts title="TypeScript"
onAddedCursor: ((cursor: Cursor) => void) | undefined
```

光标出现在白板上的回调。

##### `param`

光标对象。

<a id="onmovingcursor"></a>

### onMovingCursor

```ts title="TypeScript"
onMovingCursor: ((cursor: Cursor, positionX: number, positionY: number) => void) | undefined
```

光标在白板上移动的回调。

##### `param`

光标对象。

##### `param`

光标指示区域中心的 X 轴坐标（以白板左上角为原点）。

##### `param`

光标指示区域中心的 Y 轴坐标（以白板左上角为原点）。

<a id="onremovedcursor"></a>

### onRemovedCursor

```ts title="TypeScript"
onRemovedCursor: ((cursor: Cursor) => void) | undefined
```

光标从白板上消失的回调。

##### `param`

光标对象。

## 方法

<a id="createcursor"></a>

### createCursor

```ts title="TypeScript"
createCursor(memberId: number): CursorDescription & { reactNode?: any }
```

创建光标的描述。该方法在用户光标首次出现之前会被调用。

#### 参数

| 名称 | 描述 |
| --- | --- |
| memberId: number | 用户 ID。 |

#### 返回值

[CursorDescription](/zh-CN/api-reference/whiteboard/web/globals#cursordescription) & \{reactNode?: any \}

用户光标的描述。
