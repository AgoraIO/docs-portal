---
title: "ScenesCallbacksNode"
description: "ScenesCallbacksNode API 参考。"
---

场景目录的信息。

- [Callbacks](/zh-CN/api-reference/whiteboard/web/interfaces/callbacks) &lt; [ScenesCallbacks](/zh-CN/api-reference/whiteboard/web/globals#scenescallbacks) &gt;

  - ScenesCallbacksNode

## 属性

<a id="path"></a>

### path

```ts title="TypeScript"
path: string
```

场景目录的路径。

<a id="scenegroups"></a>

### sceneGroups

```ts title="TypeScript"
sceneGroups: ReadonlyArray<string>
```

场景目录下所有子场景目录的名称。

<a id="scenegroupscount"></a>

### sceneGroupsCount

```ts title="TypeScript"
sceneGroupsCount: number
```

场景目录下子场景目录的数量。

<a id="scenes"></a>

### scenes

```ts title="TypeScript"
scenes: ReadonlyArray<string>
```

场景目录下所有场景的名称。

<a id="scenescount"></a>

### scenesCount

```ts title="TypeScript"
scenesCount: number
```

场景目录下场景的数量。

## 方法

<a id="dispose"></a>

### dispose

```ts title="TypeScript"
dispose(): void
```

释放监听器对象。

#### 返回值

void

<a id="forwardto"></a>

### forwardTo

```ts title="TypeScript"
forwardTo<NAME>(name: NAME, toCallbacks: Callbacks<ScenesCallbacks>): any
```

将注册的回调转到另一个 `Callbacks` 对象。

#### 参数

| 名称 | 描述 |
| --- | --- |
| name: NAME | 注册回调的名称。 |
| toCallbacks: Callbacks&lt;ScenesCallbacks&gt; | 转到的 `Callbacks` 对象。 |

#### 返回值

any

<a id="off"></a>

### off

```ts title="TypeScript"
off<NAME>(name?: NAME, listener?: any): void
```

注销回调。

#### 参数

| 名称 | 描述 |
| --- | --- |
| Optional name: NAME | 回调名。 |
| Optional listener: any | 回调函数。 |

#### 返回值

void

<a id="on"></a>

### on

```ts title="TypeScript"
on<NAME>(name: NAME, listener: any): void
```

注册一个回调。

#### 参数

| 名称 | 描述 |
| --- | --- |
| name: NAME | 回调名。 |
| listener: any | 回调函数。 |

#### 返回值

void

<a id="once"></a>

### once

```ts title="TypeScript"
once<NAME>(name: NAME, listener: any): void
```

注册只使用一次的回调。

#### 参数

| 名称 | 描述 |
| --- | --- |
| name: NAME | 回调名。 |
| listener: any | 回调函数。 |

#### 返回值

void
