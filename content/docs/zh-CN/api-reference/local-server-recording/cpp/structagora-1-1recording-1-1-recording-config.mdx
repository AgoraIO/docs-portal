---
title: "agora::recording::RecordingConfig结构体 参考"
description: "录制的详细设置。"
---

录制的详细设置。

## 构造及析构函数说明

<a id="a511201f4e63f0fae5ef416fb98cb49af"></a>

### RecordingConfig()

```cpp
agora::recording::RecordingConfig::RecordingConfig() inline
```

<a id="acee250ba1e85c148d3a22ab21e85e28f"></a>

### ~RecordingConfig()

```cpp
virtual agora::recording::RecordingConfig::~RecordingConfig() inlinevirtual
```

## 类成员变量说明

<a id="adadf31bf660b1f3fe21fc5ca05b78f89"></a>

### isAudioOnly

```cpp
bool agora::recording::RecordingConfig::isAudioOnly
```

是否仅启用音频录制。

- true: 启用音频录制且关闭视频录制。
- false:（默认）启用音视频录制。

与 [isVideoOnly](/zh-CN/api-reference/local-server-recording/cpp/structagora-1-1recording-1-1-recording-config#a01e487ff9d854f53b9f768ce2a3210cb) 结合使用：

- 如果 [isAudioOnly](/zh-CN/api-reference/local-server-recording/cpp/structagora-1-1recording-1-1-recording-config#adadf31bf660b1f3fe21fc5ca05b78f89) 为 true，且 [isVideoOnly](/zh-CN/api-reference/local-server-recording/cpp/structagora-1-1recording-1-1-recording-config#a01e487ff9d854f53b9f768ce2a3210cb) 为 false，则仅录制音频；
- 如果 [isAudioOnly](/zh-CN/api-reference/local-server-recording/cpp/structagora-1-1recording-1-1-recording-config#adadf31bf660b1f3fe21fc5ca05b78f89) 为 false，且 [isVideoOnly](/zh-CN/api-reference/local-server-recording/cpp/structagora-1-1recording-1-1-recording-config#a01e487ff9d854f53b9f768ce2a3210cb) 为 true，则仅录制视频；
- 如果 [isAudioOnly](/zh-CN/api-reference/local-server-recording/cpp/structagora-1-1recording-1-1-recording-config#adadf31bf660b1f3fe21fc5ca05b78f89) 为 false，且 [isVideoOnly](/zh-CN/api-reference/local-server-recording/cpp/structagora-1-1recording-1-1-recording-config#a01e487ff9d854f53b9f768ce2a3210cb) 为 false，则既录制音频，也录制视频；
- 不能将 [isAudioOnly](/zh-CN/api-reference/local-server-recording/cpp/structagora-1-1recording-1-1-recording-config#adadf31bf660b1f3fe21fc5ca05b78f89) 和 [isVideoOnly](/zh-CN/api-reference/local-server-recording/cpp/structagora-1-1recording-1-1-recording-config#a01e487ff9d854f53b9f768ce2a3210cb) 同时设置为 true。

<a id="a01e487ff9d854f53b9f768ce2a3210cb"></a>

### isVideoOnly

```cpp
bool agora::recording::RecordingConfig::isVideoOnly
```

是否仅启用视频录制。

- true: 启用视频录制且关闭音频录制。
- false:（默认）启用音视频录制。

与 [isAudioOnly](/zh-CN/api-reference/local-server-recording/cpp/structagora-1-1recording-1-1-recording-config#adadf31bf660b1f3fe21fc5ca05b78f89) 结合使用：

- 如果 [isAudioOnly](/zh-CN/api-reference/local-server-recording/cpp/structagora-1-1recording-1-1-recording-config#adadf31bf660b1f3fe21fc5ca05b78f89) 为 true，且 [isVideoOnly](/zh-CN/api-reference/local-server-recording/cpp/structagora-1-1recording-1-1-recording-config#a01e487ff9d854f53b9f768ce2a3210cb) 为 false，则仅录制音频；
- 如果 [isAudioOnly](/zh-CN/api-reference/local-server-recording/cpp/structagora-1-1recording-1-1-recording-config#adadf31bf660b1f3fe21fc5ca05b78f89) 为 false，且 [isVideoOnly](/zh-CN/api-reference/local-server-recording/cpp/structagora-1-1recording-1-1-recording-config#a01e487ff9d854f53b9f768ce2a3210cb) 为 true，则仅录制视频；
- 如果 [isAudioOnly](/zh-CN/api-reference/local-server-recording/cpp/structagora-1-1recording-1-1-recording-config#adadf31bf660b1f3fe21fc5ca05b78f89) 为 false，且 [isVideoOnly](/zh-CN/api-reference/local-server-recording/cpp/structagora-1-1recording-1-1-recording-config#a01e487ff9d854f53b9f768ce2a3210cb) 为 false，则既录制音频，也录制视频；
- 不能将 [isAudioOnly](/zh-CN/api-reference/local-server-recording/cpp/structagora-1-1recording-1-1-recording-config#adadf31bf660b1f3fe21fc5ca05b78f89) 和 [isVideoOnly](/zh-CN/api-reference/local-server-recording/cpp/structagora-1-1recording-1-1-recording-config#a01e487ff9d854f53b9f768ce2a3210cb) 同时设置为 true。

<a id="a1ee4fc94e7de4c810a5ad0f4f4327299"></a>

### isMixingEnabled

```cpp
bool agora::recording::RecordingConfig::isMixingEnabled
```

是否启用合流模式。

- true: 启用合流模式录制。多个 UID 的音频混合成一个纯音频文件，多个 UID 的视频混合成一个纯视频文件。无论频道内有多少用户，都只生成 1 个混合音频文件和 1 个混合视频文件。混合文件的音频属性通过 [audioProfile](/zh-CN/api-reference/local-server-recording/cpp/structagora-1-1recording-1-1-recording-config#a0664fe3ead0bc66e31988a1cd9b98fff) 参数进行设置，视频属性通过 [mixResolution](/zh-CN/api-reference/local-server-recording/cpp/structagora-1-1recording-1-1-recording-config#aa3e5ae4cacfa51a8cfc709e1cc46877e) 参数进行设置。
- false:（默认）启用单流模式录制。一个 UID 对应一个音频文件和一个视频文件。录制文件的音频属性为：采样率固定为 48 kHz，声道数和码率与原始音频流保持一致。录制文件的视频属性与原始视频流保持一致。

<a id="afc2322e9bf7424f4e692d99967a50f6e"></a>

### autoSubscribe

```cpp
bool agora::recording::RecordingConfig::autoSubscribe
```

设置录制全部用户还是指定 UID 用户：

- true: （默认）录制所有用户。
- false: 录制指定 UID 用户。

##### `注解`

`autoSubscribe` 设为 false 后，必须设置 [subscribeVideoUids](/zh-CN/api-reference/local-server-recording/cpp/structagora-1-1recording-1-1-recording-config#addcb1762cb3604744159ee783f0567b2) 或 [subscribeAudioUids](/zh-CN/api-reference/local-server-recording/cpp/structagora-1-1recording-1-1-recording-config#aecb73c90fc5dbb8c36a13a6e5c5bd618) 参数选择音频流或视频流进行录制，否则就不会录制。

<a id="a507ace41dec60a46d04233e03fb6c2e6"></a>

### enableCloudProxy

```cpp
bool agora::recording::RecordingConfig::enableCloudProxy
```

设置是否启用云代理服务：

- true: 启用云代理服务
- false:（默认）不启用云代理服务

详见 [使用云代理](/doc/recording/cpp/advanced-features/cloud-proxy)。

<a id="a9912a5fc16b3f0737290c618846d76a7"></a>

### mixedVideoAudio

```cpp
agora::linuxsdk::MIXED_AV_CODEC_TYPE agora::recording::RecordingConfig::mixedVideoAudio
```

合流模式下将一个音频录制文件和一个视频录制文件实时混合成一个既有音频也有视频的 MP4 文件。

该参数有六种设置，详见 [MIXED_AV_CODEC_TYPE](/zh-CN/api-reference/local-server-recording/cpp/namespaceagora-1-1linuxsdk#af8f3a6529f57ccfa3621014808d1283a)。

##### `注解`

- 仅当 [isMixingEnabled](/zh-CN/api-reference/local-server-recording/cpp/structagora-1-1recording-1-1-recording-config#a1ee4fc94e7de4c810a5ad0f4f4327299) 为 true 启用合流模式时有效。
- 推荐设置 `mixedVideoAudio` 为 `AV_CODEC_MIXED_TS_ONLY(6)` 或 `AV_CODEC_MIXED_TS_AND_MP4(7)`，以保证录制进程崩溃后录制文件的可用性。如果设置为 AV_CODEC_MIXED_TS_AND_MP4(7)，录制会有一定程度的性能回退。

<a id="aa3e5ae4cacfa51a8cfc709e1cc46877e"></a>

### mixResolution

```cpp
const char* agora::recording::RecordingConfig::mixResolution
```

合流模式下设置录制文件的视频属性，格式为：width，hight，fps，kbps，分别对应录制文件的宽、高、帧率和码率。默认设置为 360 x 640, 15 fps, 500 Kbps。

##### `注解`

设置帧率时，仅支持声网标准帧率：1 fps，7 fps，10 fps，15 fps，24 fps，30 fps 和 60 fps。如果你设置成其他帧率，SDK 会采用默认帧率 15 fps。

请参考 [视频属性对照表](/doc/recording/cpp/best-practices/set-output-video) 设置分辨率、帧率、码率。

<a id="a1a52a08c95d1c1368da61902db279edb"></a>

### decryptionMode

```cpp
const char* agora::recording::RecordingConfig::decryptionMode
```

当整个频道进行加密时，录制 SDK 启用内置的解密功能。默认为 NULL。

##### `注解`

录制 SDK 的解密方式必须与频道设置的加密方式一致。

目前支持以下几种解密方式：

- "aes-128-xts"：128 位 AES 加密，XTS 模式。
- "aes-128-ecb"：128 位 AES 加密，ECB 模式。
- "aes-256-xts"：256 位 AES 加密，XTS 模式。
- "aes-128-gcm": 128 位 AES 加密，GCM 模式。
- "aes-256-gcm": 256 位 AES 加密，GCM 模式。

<a id="afcf89b565958ed7c8e305a0aa8809aa9"></a>

### secret

```cpp
const char* agora::recording::RecordingConfig::secret
```

启用解密模式后，设置的解密密码。默认为 NULL。

<a id="a697655316c967ab9d98d89846063e196"></a>

### appliteDir

```cpp
const char* agora::recording::RecordingConfig::appliteDir
```

必填，设置为 AgoraCoreServices 存放的目录。

SDK 包内 AgoraCoreServices 的默认路径为：Agora_Recording_SDK_for_Linux_FULL/bin/

<a id="aebb07961e45d095c5191e30a30aa689a"></a>

### recordFileRootDir

```cpp
const char* agora::recording::RecordingConfig::recordFileRootDir
```

设置录制文件存放的根目录。默认为 NULL。

设置该参数后，录制 SDK 会按照录制日期自动生成子目录保存录制文件。

<a id="afd78b4f136e4deeaf310061606e7bc62"></a>

### cfgFilePath

```cpp
const char* agora::recording::RecordingConfig::cfgFilePath
```

指定 JSON 配置文件的路径。默认为 NULL。示例： `--cfgFilePath /home/guest/recording_dir/cfg.json`。

在该配置文件里，你可以设置以下参数：

- `Recording_Dir`: 字符串类型，指定存放录制文件的绝对路径。指定路径后，SDK 不会自动生成子目录。

  示例： `{"Recording_Dir": "/home/guest/recording_dir/"}`，表示录制文件存放的绝对路径为 `/home/guest/recording_dir/`。
- `Chunk_Time_Span`: 字符串类型，设置直播场景中单流录制模式下录制文件切片的间隔时间，单位为秒，最小值为 10。

  示例： `{"Chunk_Time_Span": "15"}`，表示每隔 15 秒生成一个录制文件。
- `Secondary_Recorder`: 字符串类型，表示录制两种格式的音频文件， `Secondary_Recorder` 为增加的音频文件格式。

  示例： `{"Secondary_Recorder":"mp3"}`，表示生成 AAC 和 MP3 两种格式的音频文件。
- `Audio_Only_Record`: 字符串类型，表示仅录制一种格式的音频文件， `Audio_Only_Record` 为仅录制的音频文件格式。

  示例： `{"Audio_Only_Record":"mp3"}`，表示只生成 MP3 格式的音频文件。
- `Audio_Sample_Rate`: Number 类型，设置录制的音频的采样率。

  示例： `{"Audio_Sample_Rate":16000}`，表示录制音频的采样率是 16000Hz。
- `Pcm_Sample_Rate`: Number 类型，设置录制 PCM 帧格式的原始音频数据(`getAudioFrame` 为 `2` 或 `3`)的采样率。

  示例： `{"Pcm_Sample_Rate":16000}`，表示录制 PCM 帧格式的原始音频数据的采样率是 16000Hz。

##### `注解`

- `Chunk_Time_Span` 参数仅适用于直播场景下的单流录制模式。
- 要使用 `Chunk_Time_Span` 参数，必须将 `enableIntraRequest` 设为 `true`，请求发流端发送关键帧。发流端是否发送关键帧，由发流端使用的 RTC SDK 版本决定。
- 录制 SDK 只有遇到视频关键帧才会真正切片，因而实际的切片时间间隔与设置的参数会有少许偏差。
- `Audio_Sample_Rate` 可以设置为 16000Hz，32000Hz，44100Hz，48000Hz。如果设置的采样率不在上述范围内，则默认采样率为 16000Hz。
- `Pcm_Sample_Rate` 可以设置为 8000Hz, 16000Hz, 32000Hz。如果设置的采样率不在上述范围内，则配置不生效。

<a id="a723e05b00dc63e2f2fc85e3e7489e051"></a>

### decodeVideo

```cpp
agora::linuxsdk::VIDEO_FORMAT_TYPE agora::recording::RecordingConfig::decodeVideo
```

设置录制视频格式。详见 [VIDEO_FORMAT_TYPE](/zh-CN/api-reference/local-server-recording/cpp/namespaceagora-1-1linuxsdk#a6ec9426fec3c703757f31793b55c41dd)。

##### `注解`

- [VIDEO_FORMAT_TYPE](/zh-CN/api-reference/local-server-recording/cpp/namespaceagora-1-1linuxsdk#a6ec9426fec3c703757f31793b55c41dd) = 1，2，3 或 4 时， [isMixingEnabled](/zh-CN/api-reference/local-server-recording/cpp/structagora-1-1recording-1-1-recording-config#a1ee4fc94e7de4c810a5ad0f4f4327299) 不能设为 true。

<a id="ae33efa98ee00667f2e568612ecfd2a69"></a>

### decodeAudio

```cpp
agora::linuxsdk::AUDIO_FORMAT_TYPE agora::recording::RecordingConfig::decodeAudio
```

设置录制音频格式。详见 [AUDIO_FORMAT_TYPE](/zh-CN/api-reference/local-server-recording/cpp/namespaceagora-1-1linuxsdk#a97f1f8e3aa7bfe77c9c5f8d9d208bb1f)。

##### `注解`

[AUDIO_FORMAT_TYPE](/zh-CN/api-reference/local-server-recording/cpp/namespaceagora-1-1linuxsdk#a97f1f8e3aa7bfe77c9c5f8d9d208bb1f) = 1 或 2 时， [isMixingEnabled](/zh-CN/api-reference/local-server-recording/cpp/structagora-1-1recording-1-1-recording-config#a1ee4fc94e7de4c810a5ad0f4f4327299) 不能设为 true。

<a id="a8f431772bcd5056682b5a90120ed4120"></a>

### lowUdpPort

```cpp
int agora::recording::RecordingConfig::lowUdpPort
```

最低 UDP 端口。默认值为 0。最高 UDP 端口与最低 UDP 端口差值不能小于 6。

<a id="acabaa4b99e0dfa92dc8bb392700916b8"></a>

### highUdpPort

```cpp
int agora::recording::RecordingConfig::highUdpPort
```

最高 UDP 端口。默认值为 0。最高 UDP 端口与最低 UDP 端口差值不能小于 6。

<a id="aca9710dfdb0596c88f26e3c1c3daf48b"></a>

### idleLimitSec

```cpp
int agora::recording::RecordingConfig::idleLimitSec
```

设置空闲频道的超时退出时间。默认值为 300 秒。

该值需大于等于 3 秒。录制时，如果频道空闲（无用户）超过设定的时间，录制端自动退出频道，停止录制。

##### `注解`

- 该段时间也会纳入计费。
- 通信场景下，如果频道内有用户，但用户没有发流，不算作无用户状态。
- 直播场景下，如果频道内有观众但无主播，一旦无主播的状态超过 `idleLimitSec`，录制程序会自动退出。

<a id="a9f90bd61e4ebf3ab9506825cb68ef393"></a>

### captureInterval

```cpp
int agora::recording::RecordingConfig::captureInterval
```

设置视频截图的时间间隔。默认值为 5 秒。该值需大于等于 1 秒。

##### `注解`

仅当 [decodeVideo](/zh-CN/api-reference/local-server-recording/cpp/structagora-1-1recording-1-1-recording-config#a723e05b00dc63e2f2fc85e3e7489e051) 设为 3，4 或 5 时有效。

<a id="a611f4c05d64eafcc5e304b10d58b3477"></a>

### audioIndicationInterval

```cpp
int agora::recording::RecordingConfig::audioIndicationInterval
```

说话者监测的时间间隔。

- &lt;= 0:（默认）禁用说话者监测的功能。
- &gt; 0: 说话者监测的时间间隔，单位为 ms。建议设置时间间隔大于 200 ms。一旦检测到频道内有人说话，就会在 [onActiveSpeaker](/zh-CN/api-reference/local-server-recording/cpp/classagora-1-1recording-1-1-i-recording-engine-event-handler#a06e7c77ee7a3da76882922c11a1421c8) 回调中返回当前时间段内声音最大的用户的 UID，在 [onAudioVolumeIndication](/zh-CN/api-reference/local-server-recording/cpp/classagora-1-1recording-1-1-i-recording-engine-event-handler#ac07a64cb066c596c2d826c955dc1a543) 回调中返回当前时间段内所有说话者的 UID 和音量。

<a id="a15fe9c97882629e8d5d74a75720506dd"></a>

### channelProfile

```cpp
agora::linuxsdk::CHANNEL_PROFILE_TYPE agora::recording::RecordingConfig::channelProfile
```

频道模式。录制 SDK 针对不同的频道模式进行了不同的优化。目前支持的场景详见 [CHANNEL_PROFILE_TYPE](/zh-CN/api-reference/local-server-recording/cpp/namespaceagora-1-1linuxsdk#ad9f9be8fcec3408e36ec870c8eb9808e)。

<a id="a0b6b285ed59fb50b34fb8fe9c52073ed"></a>

### streamType

```cpp
agora::linuxsdk::REMOTE_VIDEO_STREAM_TYPE agora::recording::RecordingConfig::streamType
```

设置大小流。仅在 Agora Native SDK 或 Web SDK 开启双流模式时有效。详见 [REMOTE_VIDEO_STREAM_TYPE](/zh-CN/api-reference/local-server-recording/cpp/namespaceagora-1-1linuxsdk#a15775e611bf72104f8210b3ad70e107d)。

<a id="ad3427cfda0f4dc3055da823f00a1cdb7"></a>

### triggerMode

```cpp
agora::linuxsdk::TRIGGER_MODE_TYPE agora::recording::RecordingConfig::triggerMode
```

选择录制启动模式。详见 [TRIGGER_MODE_TYPE](/zh-CN/api-reference/local-server-recording/cpp/namespaceagora-1-1linuxsdk#a652d8aefc1931391ff65ae7a088b932f)。

<a id="ab995c9f2f8d90e794bf3f4566c9df136"></a>

### lang

```cpp
agora::linuxsdk::LANGUAGE_TYPE agora::recording::RecordingConfig::lang
```

编程语言，详见 [LANGUAGE_TYPE](/zh-CN/api-reference/local-server-recording/cpp/namespaceagora-1-1linuxsdk#aed12bf14a903009cb75f432d9159696f)。

<a id="a1d9c1b40a777c50e3dc951e7698b1165"></a>

### proxyType

```cpp
int agora::recording::RecordingConfig::proxyType
```

设置代理服务器类型。

- 0: 自行部署 SOCKS5 类型的代理服务器。
- 1: （默认）使用云代理服务，配置域名（推荐使用）。
- 2: 使用云代理服务，配置 IP 列表（域名受限时使用）。

设置 `proxyType` 参数后，需在 `proxyServer` 参数中进行相应设置。详见 [使用云代理服务](/doc/recording/cpp/advanced-features/cloud-proxy)。

<a id="a8b5ec73dc9ce407c360c7be21b11e111"></a>

### proxyServer

```cpp
const char* agora::recording::RecordingConfig::proxyServer
```

根据 `proxyType` 参数中选择的代理服务器类型设置代理服务器的 IP 地址（域名）和端口，从而实现在内网进行录制：

- 如 `proxyType` 为 0， `proxyServer` 设为 `"<ip>:<port>"`。
- 如 `proxyType` 为 1， `proxyServer` 设为 `"<domain>:<port>"`。
- 如 `proxyType` 为 2， `proxyServer` 设为 `"<ip1>,<ip2>,...,<ipx>:<port>"`。

详见 [使用云代理服务](/doc/recording/cpp/advanced-features/cloud-proxy)。

<a id="a0664fe3ead0bc66e31988a1cd9b98fff"></a>

### audioProfile

```cpp
agora::linuxsdk::AUDIO_PROFILE_TYPE agora::recording::RecordingConfig::audioProfile
```

设置合流模式下录制文件的音频属性。详见 [AUDIO_PROFILE_TYPE](/zh-CN/api-reference/local-server-recording/cpp/namespaceagora-1-1linuxsdk#a8e652e4f981c729da31b3dbb73b8d087)。

##### `注解`

仅当 [isMixingEnabled](/zh-CN/api-reference/local-server-recording/cpp/structagora-1-1recording-1-1-recording-config#a1ee4fc94e7de4c810a5ad0f4f4327299) 为 true 启用合流模式时有效。

<a id="abf37d73566b06c334822c77564912ac5"></a>

### defaultVideoBg

```cpp
const char* agora::recording::RecordingConfig::defaultVideoBg
```

设置合流模式下画布的默认背景图片的路径。如不设置会显示背景色。

##### `注解`

仅支持本地 JPEG 文件。

<a id="af55938564ac912abda04529f35e26dfb"></a>

### defaultUserBg

```cpp
const char* agora::recording::RecordingConfig::defaultUserBg
```

设置合流模式下用户的默认背景图片的路径。如不设置会显示背景色。

合流模式下，如果某用户没有视频流，就会显示该图片。

##### `注解`

- 仅支持本地 JPEG 文件。
- 该设置对 Agora Web SDK 的用户无效。

<a id="addcb1762cb3604744159ee783f0567b2"></a>

### subscribeVideoUids

```cpp
const char* agora::recording::RecordingConfig::subscribeVideoUids
```

录制指定 UID 的视频流。UID 组成的数组，为用逗号隔开的字符串，例如 "1","2","3"。

当 [autoSubscribe](/zh-CN/api-reference/local-server-recording/cpp/structagora-1-1recording-1-1-recording-config#afc2322e9bf7424f4e692d99967a50f6e) 设为 false 时，你可设置此参数来指定 UID 进行录制。 该参数设为 null 则表示录制所有发流用户的视频。

<a id="aecb73c90fc5dbb8c36a13a6e5c5bd618"></a>

### subscribeAudioUids

```cpp
const char* agora::recording::RecordingConfig::subscribeAudioUids
```

录制指定 UID 的音频流。UID 组成的数组，为用逗号隔开的字符串，例如 "1","2","3"。

当 [autoSubscribe](/zh-CN/api-reference/local-server-recording/cpp/structagora-1-1recording-1-1-recording-config#afc2322e9bf7424f4e692d99967a50f6e) 设为 false 时，你可设置此参数来指定 UID 进行录制。 该参数设为 null 则表示录制所有发流用户的音频。

<a id="a8029943cfe4af34d0a616d2a7db41296"></a>

### enableIntraRequest

```cpp
bool agora::recording::RecordingConfig::enableIntraRequest
```

是否启用关键帧请求。该参数默认为 true，可改善弱网下的音视频体验。如需使单流模式下录制的视频可指定播放位置，须将 `enableIntraRequest` 设为 false。

- true:（默认）启用关键帧请求。发流端通过接收关键帧请求生成关键帧。启用后，单流模式下录制的视频文件播放时无法指定播放位置。
- false: 禁用关键帧请求，频道内的所有发流端均每 2 秒发送一次关键帧。禁用后，单流模式下录制的视频可指定播放位置。

##### `注解`

如发流端使用的是 Agora RTC SDK v2.9.2 及之前的版本，通信模式下设置该参数将无效，仅在直播模式下生效。

<a id="afc5ce5091c51fd43bb3923f64987b672"></a>

### enableH265Support

```cpp
bool agora::recording::RecordingConfig::enableH265Support
```

设置是否支持录制 H.265 视频流：

- true: 支持录制 H.265 视频流。
- false: （默认）不支持录制 H.265 视频流。频道内的远端用户无法发 H.265 视频流。
