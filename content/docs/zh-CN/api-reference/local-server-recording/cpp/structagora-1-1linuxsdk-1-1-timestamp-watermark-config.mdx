---
title: "agora::linuxsdk::TimestampWatermarkConfig结构体 参考"
description: "定义动态时间戳水印设置。"
---

定义动态时间戳水印设置。

### `注解`

- 时间戳显示录制服务器的当前时间，是动态变化的，显示形式为“2019:06:18 14:30:35"。
- 时间戳颜色为黑色或白色，会根据视频画布的颜色自动调整。若视频背景较暗，则颜色为白色；若视频背景较亮，则颜色为黑色。当视频背景不均匀时，颜色为黑白相间。

## 类成员变量说明

<a id="ab059f6150f1243d1498a868329964d4e"></a>

### font_size

```cpp
uint32_t agora::linuxsdk::TimestampWatermarkConfig::font_size
```

字体大小。默认值为 10，相当于 144 dpi 设备上的 10 x 15 磅。

<a id="af2a3a0bcfddcb5fd7048792f5aff68b7"></a>

### offset_x

```cpp
uint32_t agora::linuxsdk::TimestampWatermarkConfig::offset_x
```

水印框左上角与视频画布左上角的水平距离。单位为像素（pixel），默认值为 0。

<a id="a6ac262a1dc9b152747f577b33f46a0c7"></a>

### offset_y

```cpp
uint32_t agora::linuxsdk::TimestampWatermarkConfig::offset_y
```

水印框左上角与视频画布左上角的垂直距离。单位为像素（pixel），默认值为 0。

<a id="a8357462008cac07d20f9ce4465cc3d3e"></a>

### wm_width

```cpp
uint32_t agora::linuxsdk::TimestampWatermarkConfig::wm_width
```

水印框的宽度。单位为像素（pixel），默认值为 0。

<a id="a962b3da31b34b6191d092f0904918078"></a>

### wm_height

```cpp
uint32_t agora::linuxsdk::TimestampWatermarkConfig::wm_height
```

水印框的高度。单位为像素（pixel），默认值为 0。
