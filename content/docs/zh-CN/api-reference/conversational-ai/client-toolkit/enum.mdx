---
title: "枚举类"
---

<PlatformStructured platform="android">

<a id="moduletype"></a>
## ModuleType

性能模块类型枚举。

#### 枚举值

| 枚举值 | 描述 |
| --- | --- |
| `LLM` | 用于表示 LLM 推理延迟测量。 |
| `MLLM` | 多模态大模型推理延迟测量。 |
| `TTS` | 文本转语音合成延迟测量。 |
| `Context` | 上下文处理延迟测量。 |
| `UNKNOWN` | 用于表示未知类型。 |

<a id="messagetype"></a>
## MessageType

用于区分系统中不同类型的消息。

#### 枚举值

| 枚举值 | 描述 |
| --- | --- |
| `ASSISTANT` | AI 助理转写消息。 |
| `USER` | 用户转写消息。 |
| `ERROR` | 错误消息。 |
| `METRICS` | 性能指标消息。 |
| `INTERRUPT` | 中断消息。 |
| `UNKNOWN` | 未知类型消息。 |
| `MESSAGE_RECEIPT` | 消息回执。 |

<a id="priority"></a>
## Priority

控制智能体在交互过程中处理传入消息的优先级。

#### 枚举值

| 枚举值 | 描述 |
| --- | --- |
| `INTERRUPT` | 高优先级：立即中断当前交互并处理该消息，适用于紧急或时间敏感的内容。 |
| `APPEND` | 中等优先级：在当前交互完成后排队处理该消息，适用于后续提问。 |
| `IGNORE` | 低优先级：仅在智能体空闲时处理该消息，进行中的交互期间将被丢弃，适用于可选内容。 |

<a id="agentstate"></a>
## AgentState

表示智能体当前的状态。

#### 枚举值

| 枚举值 | 描述 |
| --- | --- |
| `SILENT` | 智能体处于静默状态。 |
| `LISTENING` | 智能体处于监听状态。 |
| `THINKING` | 智能体处于处理或思考状态。 |
| `SPEAKING` | 智能体处于说话状态。 |
| `UNKNOWN` | 智能体处于未知状态。 |

<a id="transcriptrendermode"></a>
## TranscriptRenderMode

转写渲染模式。

#### 枚举值

| 枚举值 | 描述 |
| --- | --- |
| `Word` | 逐词转写渲染。 |
| `Text` | 全文转写渲染。 |

<a id="transcripttype"></a>
## TranscriptType

转录来源类型。

#### 枚举值

| 枚举值 | 描述 |
| --- | --- |
| `AGENT` | 智能体转录。 |
| `USER` | 用户转录。 |

<a id="transcriptstatus"></a>
## TranscriptStatus

表示转录的当前状态。

#### 枚举值

| 枚举值 | 描述 |
| --- | --- |
| `IN_PROGRESS` | 转录仍在生成或语音仍在进行中。 |
| `END` | 转录已正常完成。 |
| `INTERRUPTED` | 转录在完成前被中断。 |
| `UNKNOWN` | 未知状态。 |

</PlatformStructured>

<PlatformStructured platform="ios">

<a id="chatmessagetype"></a>
## ChatMessageType

消息类型枚举。

用于区分会话中的不同消息类型。

#### 枚举值

| 枚举值 | 描述 |
| --- | --- |
| `text` | （0）：文本消息类型。 |
| `image` | （1）：图片消息类型。 |
| `unknown` | （2）：未知消息类型。 |

<a id="moduletype"></a>
## ModuleType

性能监控中的 AI 模块类型。

#### 枚举值

| 枚举值 | 描述 |
| --- | --- |
| `llm` | （0）：大语言模型推理模块。 |
| `mllm` | （1）：多模态大语言模型推理模块。 |
| `tts` | （2）：文本转语音合成模块。 |
| `context` | （3）：Context 模块类型。 |
| `unknown` | （4）：未知的模块类型。 |

<a id="messagetype"></a>
## MessageType

用于区分会话系统中不同类型的消息。

#### 枚举值

| 枚举值 | 描述 |
| --- | --- |
| `metrics` | 指标消息类型。 |
| `error` | 错误消息类型。 |
| `assistant` | 智能体转写消息类型。 |
| `user` | 用户转写消息类型。 |
| `interrupt` | 中断消息类型。 |
| `state` | 状态消息类型。 |
| `imageInfo` | 图像信息消息类型。 |
| `unknown` | 未知消息类型。 |

<a id="priority"></a>
## Priority

控制智能体处理消息的优先级。

#### 枚举值

| 枚举值 | 描述 |
| --- | --- |
| `interrupt` | （0）：高优先级。智能体会立即中断当前交互并处理该消息，适用于紧急或时间敏感的消息。 |
| `append` | （1）：中等优先级。智能体会在当前交互完成后排队处理该消息，适用于后续问题。 |
| `ignore` | （2）：低优先级。如果智能体当前正在交互，该消息将被丢弃，仅在空闲时处理，适用于可选内容。 |

<a id="agentstate"></a>
## AgentState

表示智能体在对话过程中的不同状态。

#### 枚举值

| 枚举值 | 描述 |
| --- | --- |
| `idle` | （0）：空闲状态，智能体未在主动处理。 |
| `silent` | （1）：静默状态，智能体保持静默但已准备好监听。 |
| `listening` | （2）：监听状态，智能体正在主动监听用户输入。 |
| `thinking` | （3）：思考状态，智能体正在处理用户输入并生成响应。 |
| `speaking` | （4）：说话状态，智能体正在说话或输出音频内容。 |
| `unknown` | （5）：未知状态，用于未识别状态的回退处理。 |

<a id="transcriptrendermode"></a>
## TranscriptRenderMode

转写渲染模式。

#### 枚举值

| 枚举值 | 描述 |
| --- | --- |
| `words` | （0）：逐词转写渲染模式，每处理一个词时更新。 |
| `text` | （1）：逐句转写渲染模式，当完整句子准备好时更新。 |

<a id="transcriptstatus"></a>
## TranscriptStatus

表示会话流程中转录的当前状态。

用于跟踪和管理转录文本的生命周期状态。

#### 枚举值

| 枚举值 | 描述 |
| --- | --- |
| `inprogress` | （0）：转录进行中。该状态在文本正在生成或播放时设置，表示内容仍在处理或流式传输中。 |
| `end` | （1）：转录已完成。该状态在文本生成正常结束时设置，表示转录片段自然结束。 |
| `interrupted` | （2）：转录被中断。该状态在文本生成被提前停止时设置，适用于被更高优先级消息打断的情况。 |

<a id="transcripttype"></a>
## TranscriptType

区分转写文本来源类型。

通过识别转写文本来自智能体或用户，有助于管理会话流程和界面展示。

#### 枚举值

| 枚举值 | 描述 |
| --- | --- |
| `agent` | ：由智能体生成的转写文本。通常包含智能体助手的响应和发言内容，用于在会话界面中渲染智能体语音。 |
| `user` | ：来自用户的转写文本。包含将用户语音输入转换后的文本，用于在会话流程中展示用户语音内容。 |

<a id="conversationalaiapierrortype"></a>
## ConversationalAIAPIErrorType

用于区分会话式智能体系统中的不同错误类型。

#### 枚举值

| 枚举值 | 描述 |
| --- | --- |
| `unknown` | （0）：未知错误类型。 |
| `rtcError` | （2）：RTC（实时通信）相关错误。 |
| `rtmError` | （3）：RTM（实时消息）相关错误。 |

</PlatformStructured>

<PlatformStructured platform="java">

<a id="ttsvendorenum"></a>
## TTSVendorEnum

智能体加入 RTC 频道时使用的 TTS 模块厂商类型。

:::info[信息]
自 v0.3.0 版本新增。
:::

#### 枚举值

| 枚举值 | 描述 |
| --- | --- |
| `MINIMAX` | Minimax TTS 厂商。 |
| `TENCENT` | Tencent TTS 厂商。 |
| `BYTEDANCE` | Bytedance TTS 厂商。 |
| `MICROSOFT` | Microsoft TTS 厂商。 |

<a id="asrvendorenum"></a>
## ASRVendorEnum

智能体加入频道时使用的 ASR 模块厂商类型。

:::info[信息]
自 v0.7.0 版本新增。
:::

#### 枚举值

| 枚举值 | 描述 |
| --- | --- |
| `FENGMING` | Fengming ASR 厂商。 |
| `TENCENT` | Tencent ASR 厂商。 |
| `MICROSOFT` | Microsoft ASR 厂商。 |

<a id="domainarea"></a>
## DomainArea

Open API 网关端点所在的全球区域。

:::info[信息]
自 v0.3.0 版本新增。
:::

#### 枚举值

| 枚举值 | 描述 |
| --- | --- |
| `US` | 表示美国西部和东部地区。 |
| `EU` | 表示欧洲西部和中部地区。 |
| `AP` | 表示亚太地区的东南部和东北部。 |
| `CN` | 表示中国大陆的东部和北部地区。 |

<a id="convoaiserviceregionenum"></a>
## ConvoAIServiceRegionEnum

会话式智能体引擎服务的区域。

:::info[信息]
自 v0.3.0 版本新增。
:::

#### 枚举值

| 枚举值 | 描述 |
| --- | --- |
| `CHINESE_MAINLAND` | 表示会话式智能体引擎服务在中国大陆区域。 |
| `GLOBAL` | 表示会话式智能体引擎服务在除中国大陆以外的全球区域。 |

</PlatformStructured>

<PlatformStructured platform="go">

<a id="area"></a>
## Area

表示 Open API 网关端点所在的全球区域。

:::info[信息]
自 v0.7.0 版本新增。
:::

#### 枚举值

| 枚举值 | 描述 |
| --- | --- |
| `US` | 表示美国西部和东部地区。 |
| `EU` | 表示欧洲西部和中部地区。 |
| `AP` | 表示亚太地区的东南部和东北部。 |
| `CN` | 表示中国大陆的东部和北部地区。 |

<a id="level"></a>
## Level

日志消息的级别。

:::info[信息]
自 v0.7.0 版本新增。
:::

#### 枚举值

| 枚举值 | 描述 |
| --- | --- |
| `DebugLevel` | 最低日志级别，用于调试目的。 |
| `InfoLevel` | 用于记录信息性消息的日志级别。 |
| `WarningLevel` | 用于记录警告消息的日志级别。 |
| `ErrLevel` | 用于记录错误消息的日志级别。 |

<a id="serviceregion"></a>
## ServiceRegion

智能对话引擎服务所属的区域。

:::info[信息]
自 v0.7.0 版本新增。
:::

#### 枚举值

| 枚举值 | 描述 |
| --- | --- |
| `UnknownServiceRegion` | 未知区域的智能对话引擎服务。 |
| `ChineseMainlandServiceRegion` | 中国大陆区域的智能对话引擎服务。 |
| `GlobalServiceRegion` | 除中国大陆以外的全球区域的智能对话引擎服务。 |

<a id="ttsvendor"></a>
## TTSVendor

TTS 供应商类型。

:::info[信息]
自 v0.7.0 版本新增。
:::

#### 枚举值

| 枚举值 | 描述 |
| --- | --- |
| `MinimaxTTSVendor` | Minimax TTS 服务商。 |
| `TencentTTSVendor` | Tencent TTS 服务商。 |
| `BytedanceTTSVendor` | Bytedance TTS 服务商。 |
| `MicrosoftTTSVendor` | Microsoft TTS 服务商。 |

<a id="asrvendor"></a>
## ASRVendor

智能体加入 RTC 频道时使用的自动语音识别（ASR）服务商。

:::info[信息]
自 v0.12.0 版本新增。
:::

#### 枚举值

| 枚举值 | 描述 |
| --- | --- |
| `ASRVendorFengming` | 风鸣 ASR 服务商。 |
| `ASRVendorTencent` | Tencent ASR 服务商。 |
| `ASRVendorMicrosoft` | Microsoft ASR 服务商。 |

</PlatformStructured>

<PlatformStructured platform="typescript">

<a id="econversationalaiapievents"></a>
## EConversationalAIAPIEvents

`Conversational AI API` 可触发的事件类型。

:::info[信息]
自 1.6.0 版本新增。
:::

#### 枚举值

| 枚举值 | 描述 |
| --- | --- |
| `AGENT_STATE_CHANGED` | （"agent-state-changed"）：智能体状态变更事件。 |
| `AGENT_LISTENING_CHANGED ` | （"agent-listening-changed"）：智能体倾听状态变更事件。 |
| `AGENT_THINKING_CHANGED ` | （"agent-thinking-changed"）：智能体思考状态变更事件。 |
| `AGENT_SPEAKING_CHANGED ` | （"agent-speaking-changed"）：智能体说话状态变更事件。 |
| `AGENT_INTERRUPTED` | （"agent-interrupted"）：智能体被中断事件。 |
| `AGENT_METRICS` | （"agent-metrics"）：智能体指标事件。 |
| `AGENT_ERROR` | （"agent-error"）：智能体错误事件。 |
| `TRANSCRIPT_UPDATED` | （"transcript-updated"）：转录更新事件。 |
| `DEBUG_LOG` | （"debug-log"）：调试日志事件。 |
| `MESSAGE_RECEIPT_UPDATED` | 消息接收状态已更新。 |
| `MESSAGE_ERROR` | （'message-error'）：消息错误事件。 |

<a id="echatmessagepriority"></a>
## EChatMessagePriority

用于设置聊天消息处理优先级。

:::info[信息]
自 1.7.0 版本新增。
:::

#### 枚举值

| 枚举值 | 描述 |
| --- | --- |
| `INTERRUPTED` | （'interrupted'）：中断当前处理并立即处理该消息。 |
| `APPEND` | （'append'）：将消息添加到处理队列中，按顺序处理。 |
| `IGNORE` | （'ignore'）：丢弃该消息，不进行处理。 |

<a id="echatmessagetype"></a>
## EChatMessageType

对话式 AI 系统中支持的聊天消息类型。

:::info[信息]
自 1.7.0 版本新增。
:::

#### 枚举值

| 枚举值 | 描述 |
| --- | --- |
| `TEXT` | （'text'）：文本消息。 |
| `IMAGE` | （'image'）：图片消息。 |
| `UNKNOWN` | （'unknown'）：未知类型的消息。 |

<a id="emoduletype"></a>
## EModuleType

[`EModuleType`](/zh-CN/api-reference/conversational-ai/client-toolkit/enum?platform=typescript#emoduletype) 表示 AI 能力的模块类型。

:::info[信息]
自 1.6.0 版本新增。
:::

#### 枚举值

| 枚举值 | 描述 |
| --- | --- |
| `LLM` | （"llm"）：语言学习模型。 |
| `MLLM` | （"mllm"）：多模态语言学习模型。 |
| `TTS` | （"tts"）：文本转语音模块。 |
| `UNKNOWN` | （"unknown"）：未知的模块类型。 |
| `CONTEXT` | （"context"）：上下文管理模块。 |

<a id="eagentstate"></a>
## EAgentState

[`EAgentState`](/zh-CN/api-reference/conversational-ai/client-toolkit/enum?platform=typescript#eagentstate) 表示会话智能体的状态。

:::info[信息]
自 1.6.0 版本新增。
:::

#### 枚举值

| 枚举值 | 描述 |
| --- | --- |
| `IDLE` | （"idle"）：智能体准备好进行新的交互。 |
| `LISTENING` | （"listening"）：智能体正在接收用户输入。 |
| `THINKING` | （"thinking"）：智能体正在处理接收到的输入。 |
| `SPEAKING` | （"speaking"）：智能体正在输出响应。 |
| `SILENT` | （"silent"）：智能体有意地不做出响应。 |

</PlatformStructured>
