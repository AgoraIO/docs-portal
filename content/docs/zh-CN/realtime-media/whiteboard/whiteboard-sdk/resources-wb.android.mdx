---
title: "下载"
---
本文提供获取 SDK 和 Demo 的方式。

## 通过 Maven 集成 SDK

参考以下步骤使用 Maven 在项目中集成 SDK：

1. 打开根目录下的 `build.gradle` 进行以下配置：

  ```groovy
  allprojects {
      repositories {
          jcenter()
          // 添加 JitPack 仓库
          maven { url 'https://jitpack.io' }
          // 对于国内用户，如果 Gradle sync 失败，可以选择国内镜像，例如阿里云 Maven 镜像
          maven { url 'https://maven.aliyun.com/nexus/content/repositories/releases/'}
      }
  }
  ```

2. 打开 app 目录下的 `build.gradle` 进行如下配置：

  ```groovy
  dependencies {
      implementation 'com.github.netless-io:whiteboard-android:2.16.100'
  }
  ```

## 下载 SDK

本节提供最新版本的 SDK。

  :::info[信息]
  - 各版本 SDK 的发版详情请参考[发版说明](/zh-CN/realtime-media/whiteboard/whiteboard-sdk/release-notes-wb.android)。
      - 如何使用 SDK 实现加入白板房间请参考[加入白板房间](/zh-CN/realtime-media/whiteboard/whiteboard-sdk/get-started/join-room-wb.android)。
  :::

  互动白板 SDK（即 Whiteboard SDK）中提供了简单易用的 API，帮助开发者在 App 中构建各种互动白板场景。

  - SDK 名称：互动白板 SDK
  - 开发者：上海声网科技有限公司
  - 互动白板 SDK 合规使用说明请参考[互动白板 SDK 合规使用说明](/basics/security/whiteboard-sdk)，个人信息处理规则请参考 [SDK 隐私政策](https://www.shengwang.cn/SDK-privacy-policy/)。

        - [Android](https://github.com/netless-io/whiteboard-android/archive/refs/tags/2.16.100.zip)

## 获取 Demo

本节提供 Demo，你可以下载体验互动白板的最新功能。

    - Web Demo
