---
title: "Agora Skills"
description: "了解如何在声网开发者文档中 Agora Skills，包括适用场景、配置步骤、实现要点和注意事项。"
---
你可以借助 AI Coding Agent 更高效地集成声网产品。

当你希望 AI Coding Agent 在开始编码前选择正确的声网工作流时，安装 Agora Skills。Skills 可以帮助 Agent 选择合适的产品路径、Starter 和接入步骤，避免凭空猜测。

当文档查询本身不足以完成任务，难点在于“先做什么”或“该走哪条官方路径”时，优先使用 Skills。如果你也希望 Agent 在工作时查询最新文档，可以同时配置 [Shengwang Doc MCP Server](/zh-CN/introduction/mcp-integrate)。

## 安装 Agora Skills

<Tabs>
<TabsList>
  <TabsTrigger value="skills-cli">Skills CLI</TabsTrigger>
  <TabsTrigger value="manual">手动安装</TabsTrigger>
</TabsList>

<TabsContent value="skills-cli">

```bash
npx skills add AgoraIO/skills
```

当 Agent 检测到相关声网任务时，Skills 会自动激活。

</TabsContent>

<TabsContent value="manual">

克隆仓库，并将 AI Coding Agent 指向 Agora skill 文件：

```bash
git clone https://github.com/AgoraIO/skills.git ~/agora-skills
```

使用 `skills/agora/` 作为目录入口；如果你的工具需要单个根文件，也可以直接加载 `SKILL.md`。

</TabsContent>
</Tabs>

如果你手动安装 Skills，请把文件放到 AI Coding Agent 已经读取本地指令的位置：

<Tabs>
<TabsList>
  <TabsTrigger value="claude-code">Claude Code</TabsTrigger>
  <TabsTrigger value="cursor">Cursor</TabsTrigger>
  <TabsTrigger value="windsurf">Windsurf</TabsTrigger>
  <TabsTrigger value="copilot">GitHub Copilot</TabsTrigger>
  <TabsTrigger value="other-tools">其他工具</TabsTrigger>
</TabsList>

<TabsContent value="claude-code">

在 Claude Code 中运行以下命令：

- **用户级**，可在你的所有项目中使用：

  ```bash
  /plugin marketplace add AgoraIO/skills
  /plugin install agora
  ```

- **项目级**，可通过版本控制与团队共享：

  ```bash
  /plugin marketplace add AgoraIO/skills
  /plugin install agora --scope project
  ```

</TabsContent>

<TabsContent value="cursor">

将 `skills/agora/` 复制或符号链接到 `.cursor/rules/`。

</TabsContent>

<TabsContent value="windsurf">

将 `skills/agora/` 添加到 Cascade 上下文中。

</TabsContent>

<TabsContent value="copilot">

通过 `@workspace` 引用这些文件，或将它们加入 Copilot instructions。

</TabsContent>

<TabsContent value="other-tools">

将工具指向 `skills/agora/` 或顶层 `SKILL.md`。

</TabsContent>
</Tabs>

## 从提示词开始

安装 Skills 后，让 AI Coding Agent 在开始创建项目或编辑代码前选择正确的声网路径。

```text
使用 Agora Skills 和 Shengwang Doc MCP Server 帮我构建一个网页端对话式 AI Demo。
请先查询官方文档，选择合适的 Starter，并使用 Agora CLI 完成设置。
```

接下来，让 AI Coding Agent 在编辑代码前选择 Starter 和接入路径。
