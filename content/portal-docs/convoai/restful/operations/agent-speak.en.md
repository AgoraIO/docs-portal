---
title: Speak a Custom Message
description: "\"Make the specified agent speak a custom message.\r \r During a conversation with an agent, calling this API makes the agent immediately speak a custom message by using the TTS module. After the agent receives the request, the speech interrupts its current speaking and thinking process.\""
---

# Speak a Custom Message

```mdx
import RestfulRender from "@app/components/restful/RestfulRender.tsx";
import yamlObject from "@/content-assets/convoai/convoai.en.yaml";

<RestfulRender yamlObject={yamlObject} basePath="/en/docs/convoai/restful/convoai" />
```
